---
phase: 08-hardening-pre-release-audit
plan: 03
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - host/test/server.test.ts
autonomous: true
requirements: [REL-02, REL-03]
user_setup: []

must_haves:
  truths:
    - "Ten concurrent POST /annotation against a booted host yield serials 0001-0010 with no gaps or duplicates, and exactly 10 distinct .md files on disk"
    - "An 11.9 MB POST /annotation succeeds (200); a 12 MB+1 POST is rejected by the host (413 or connection reset)"
  artifacts:
    - path: "host/test/server.test.ts"
      provides: "10-concurrent serial-integrity block (D-02) + POST payload-boundary block (D-04 backstop)"
      contains: "Promise.all"
  key_links:
    - from: "host/test/server.test.ts concurrency block"
      to: "createHostServer + withSerialLock (host/src/server.ts + serial.ts)"
      via: "buildFixture/listenFixture booted server + 10 concurrent fetch"
      pattern: "Promise.all"
    - from: "host/test/server.test.ts boundary block"
      to: "host/src/security.ts readBody 12MB cap"
      via: "POST 11.9MB -> 200, POST 12MB+1 -> 413-or-reset (ECONNRESET-tolerant)"
      pattern: "ECONNRESET|413"
---

<objective>
Prove serial integrity under concurrency (D-02, REL-02, SC-2) and the host-side
payload backstop (D-04, REL-03, SC-3) with new node:test integration blocks added to
the existing host/test/server.test.ts. Reuse the proven booted-server fixture; no new
dependencies. The oversize case mirrors the existing PUT-oversize ECONNRESET tolerance.

Purpose: Deterministic, CI-able proof that the serial mutex holds at stress and that
the 12MB host cap rejects oversize POSTs.
Output: Extended host/test/server.test.ts (two new describe blocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hardening-pre-release-audit/08-CONTEXT.md
@.planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md

<interfaces>
<!-- Existing server.test.ts fixture + the PUT-oversize tolerance pattern to mirror. -->

Fixture (server.test.ts:24-63):
  const TEST_TOKEN = 'test-token-fixed-abcdef1234567890';
  buildFixture(): TestFixture { mkdtempSync -> resolveConfig({root, token: TEST_TOKEN}) -> ensureNotesDir -> createHostServer(cfg); returns { cfg, server, baseUrl, tmpRoot } }
  listenFixture(fixture): Promise<string> { server.listen(0,'127.0.0.1') -> sets fixture.baseUrl = http://127.0.0.1:<port> }
  closeFixture(fixture): teardown (mirror existing Phase-6 blocks at server.test.ts:414, :505)

Successful POST shape (server.test.ts:141-170):
  fetch(`${fixture.baseUrl}/annotation`, { method:'POST',
    headers:{ 'Content-Type':'application/json', 'X-Stikfix-Token': TEST_TOKEN },
    body: JSON.stringify({ mode:'free', comment:'…',
      page:{ url:'http://localhost:5173/test', title:'Test Page' },
      viewport:{ width:1280, height:800, devicePixelRatio:1 } }) });
  resp body: { ok: boolean; file: string; serial: string }

ECONNRESET-tolerant oversize pattern to MIRROR (server.test.ts:477-502):
  let status = null; try { const res = await fetch(...); status = res.status; } catch { /* ECONNRESET acceptable — req.destroy() */ }
  if (status !== null) assert.equal(status, 413, ...);

Chunked large-body construction (security.test.ts:101-110): build the >12MB body in 64KB chunks ('A') to avoid one giant allocation. Host cap = 12*1024*1024.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 10-concurrent POST /annotation serial-integrity block (D-02, REL-02, SC-2)</name>
  <files>host/test/server.test.ts</files>
  <read_first>
    - host/test/server.test.ts (lines 24-63 fixture; 141-170 POST shape; 414 and 505 existing describe-block before/after pattern) — reuse exactly
    - host/src/serial.ts (full file) — confirm withSerialLock serializes getNextSerial + write so no two reads see the same max
    - host/src/server.ts (lines ~120-135 handleAnnotation serial path) — confirm getNextSerial re-reads the dir inside the lock
    - .planning/phases/08-hardening-pre-release-audit/08-RESEARCH.md (Pattern 3) — the full concurrency-block example + determinism note
  </read_first>
  <action>
    Add a new describe block "concurrent POST /annotation serial integrity (REL-02/SC-2)" to server.test.ts with
    its own before(buildFixture+listenFixture) and after(closeFixture), mirroring the Phase-6 blocks at :414/:505.
    Inside, an it() fires 10 POST /annotation requests via Promise.all (constructing all fetch promises before
    awaiting any, so they hit the host near-simultaneously — the real race withSerialLock defends). Each POST uses
    the TEST_TOKEN header and the free-mode body shape (vary comment per index). Collect resp.serial from all 10,
    sort, and assert.deepEqual to ['0001','0002','0003','0004','0005','0006','0007','0008','0009','0010'] (no gaps,
    no dupes). Then readdirSync(fixture.cfg.notesDir), filter to /^\d{4}-.*\.md$/, and assert exactly 10 distinct
    .md files. Do not modify any existing test or the fixture.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - server.test.ts has a new describe block firing 10 POST /annotation via Promise.all with its own before/after
    - The test asserts sorted serials deepEqual ['0001'..'0010'] (no gaps/dupes)
    - The test asserts exactly 10 distinct .md files in the fixture notes dir
    - npm test exits 0 and the new concurrency test appears in the output as passing
    - No existing server.test.ts test was modified or removed
  </acceptance_criteria>
  <done>The 10-concurrent integration test passes deterministically, proving serial-mutex integrity end-to-end through the HTTP layer.</done>
</task>

<task type="auto">
  <name>Task 2: Add the POST /annotation payload-boundary block (D-04 host backstop, REL-03, SC-3)</name>
  <files>host/test/server.test.ts</files>
  <read_first>
    - host/test/server.test.ts (lines 477-502 PUT-oversize ECONNRESET-tolerant block) — copy this tolerance exactly
    - host/test/security.test.ts (lines 97-117 readBody 413 boundary; 101-110 chunked-write reference) — chunked large-body construction
    - host/src/security.ts (lines 42-68 readBody + the > MAX_BODY reject via req.destroy()) — confirm reset semantics
    - .planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md (section "host/test/server.test.ts", D-04 host backstop test) — exact pairing + Pitfall 2 tolerance
  </read_first>
  <action>
    Add a new describe block "POST /annotation payload-size backstop (REL-03/SC-3)" to server.test.ts with its own
    before/after fixture (or reuse an existing block's fixture if convenient — but keep it isolated). Add two it()
    cases against POST /annotation with the TEST_TOKEN header: (a) an ~11.9 MB body (a valid free-mode payload whose
    JSON encodes to just under 12*1024*1024 bytes — pad the comment field) -> assert resp.status === 200 and resp
    body ok === true; (b) a 12 MB+1 body (build via chunked construction per security.test.ts:101-110 to avoid one
    giant allocation; a comment of repeated 'A' large enough to exceed the cap) -> mirror the PUT-oversize tolerance:
    wrap fetch in try/catch, capture status; if a response came back assert.equal(status, 413); a caught ECONNRESET
    (req.destroy reset before the 413 flushed) is acceptable and must NOT fail the test. Do NOT strictly assert 413
    for the oversize case (Pitfall 2). Do not modify existing tests.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - server.test.ts has a new describe block with an 11.9 MB POST -> 200 ok:true case and a 12 MB+1 POST -> 413-or-ECONNRESET case
    - The oversize case uses the try/catch ECONNRESET-tolerant pattern (asserts 413 only if a response was received) — never strictly fails on connection reset
    - The oversize body is built via chunked construction (no single 12MB+ string allocation)
    - npm test exits 0 and both new boundary cases appear in the output
    - No existing server.test.ts test was modified or removed
  </acceptance_criteria>
  <done>The host POST payload-boundary backstop is proven: 11.9 MB succeeds, 12 MB+1 is rejected (413 or reset), tolerant of the documented connection-reset race.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network client -> host POST /annotation | Token-gated, 127.0.0.1-bound; oversize body is a resource-exhaustion vector |
| concurrent writers -> serial allocator | The in-process mutex is the integrity boundary for serial assignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Denial of Service | host readBody 12MB cap (POST path) | mitigate | This plan adds the proof: POST 12MB+1 is rejected (413 or req.destroy reset) and 11.9MB succeeds; the cap is verified, not weakened. Test tolerates the documented ECONNRESET race (Pitfall 2). |
| T-08-04 | Tampering | serial allocation under concurrency | accept (proven correct) | The withSerialLock mutex is existing, correct code; this plan proves no two concurrent POSTs collide on a serial (no gaps/dupes across 10). Disposition: verify-only, no code change to the allocator. |
</threat_model>

<verification>
- npm test exits 0 with both new blocks (concurrency + boundary) passing
- The concurrency block asserts serials 0001-0010 and exactly 10 .md files
- The boundary block tolerates ECONNRESET on the oversize case and asserts 200 on the 11.9MB case
- No existing host test modified (git diff scoped to additive blocks)
</verification>

<success_criteria>
- 10 concurrent POSTs produce 0001-0010 with no gaps/dupes (REL-02 / SC-2)
- 11.9 MB POST succeeds, 12 MB+1 POST rejected by the host (REL-03 / SC-3)
- All assertions deterministic and CI-able; no new dependencies
</success_criteria>

<output>
Create `.planning/phases/08-hardening-pre-release-audit/08-03-SUMMARY.md` when done
</output>
