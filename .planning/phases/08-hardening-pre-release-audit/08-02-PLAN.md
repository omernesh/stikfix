---
phase: 08-hardening-pre-release-audit
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - entrypoints/review.content/card.ts
autonomous: true
requirements: [REL-01, REL-03]
user_setup: []

must_haves:
  truths:
    - "Both card.ts Send call-sites (_doSend free + _doElementSend element) route their toast string through mapSendOutcome with no change to any emitted toast text or trigger behavior"
    - "An oversize payload is blocked by the pre-flight check before the SW round-trip and surfaces a clear toast"
    - "The chrome.runtime.lastError || !resp dead-channel guard remains at both call-sites (SW-eviction safety net preserved)"
  artifacts:
    - path: "entrypoints/review.content/card.ts"
      provides: "Consolidated Send error handling via the mapper + wired pre-flight guard"
      contains: "mapSendOutcome"
  key_links:
    - from: "entrypoints/review.content/card.ts"
      to: "lib/error-toast.ts mapSendOutcome"
      via: "import + call in both Send callbacks"
      pattern: "mapSendOutcome"
    - from: "entrypoints/review.content/card.ts"
      to: "lib/payload-size.ts exceedsBodyCap"
      via: "pre-flight call before chrome.runtime.sendMessage in both Send paths"
      pattern: "exceedsBodyCap"
---

<objective>
Consolidate the two production Send catch-sites in card.ts behind the Wave-0 mapper
(D-01 / D-01a, REL-01) WITHOUT changing a single emitted toast string or trigger, and
wire the pre-flight payload-size guard (D-04, REL-03) into both Send paths before the
SW relay. This is the no-regression refactor: working toasts are sacred.

Purpose: One source of truth for Send-failure toast strings; oversize payloads blocked
before a wasted ~12MB round-trip with a deterministic toast.
Output: Modified entrypoints/review.content/card.ts (consolidation + pre-flight wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hardening-pre-release-audit/08-CONTEXT.md
@.planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md
@.planning/phases/08-hardening-pre-release-audit/08-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (Wave 0) and the card.ts call-sites. -->

From lib/error-toast.ts (created in Plan 08-01):
  export type SendOutcome =
    | { kind: 'ok'; file: string }
    | { kind: 'channel-dead'; lastErrorMessage?: string }
    | { kind: 'relay-error'; error: string };
  export interface ToastSpec { message: string; isError: boolean }
  export function mapSendOutcome(o: SendOutcome): ToastSpec;

From lib/payload-size.ts (created in Plan 08-01):
  export function exceedsBodyCap(jsonBody: string): boolean;
  export const MAX_BODY_BYTES: number;

Toast surface (toast.ts:24): showToast(container, msg, isError); bound in card.ts as showToastFn(msg, isError).

card.ts _doSend catch-site (card.ts:408-443) — three branches:
  callback(resp):
    if (chrome.runtime.lastError || !resp) { showToastFn('Extension error: ' + (chrome.runtime.lastError?.message ?? 'no response'), true); <inline control restore: sendBtn.disabled=false; sendBtn.textContent='Send'; cancelBtn.disabled=false; textarea.readOnly=false>; return; }
    if (resp.ok) { showToastFn(`wrote notes\\${resp.file}`, false); _doClose(onDismiss); onSent?.(); }
    else { showToastFn(resp.error, true); <inline control restore + re-disable rule: sendBtn.disabled = textarea.value.trim().length === 0> }

card.ts _doElementSend catch-site (card.ts:844-873) — byte-identical three branches; uses restoreControls() helper then sendBtn.disabled = textarea.value.trim().length === 0.

Pre-flight insertion points:
  _doSend: after payload assembled (~card.ts:392-405), BEFORE sendMessage at ~:408.
  _doElementSend: after payload (with screenshots[]) assembled (~card.ts:827-841), BEFORE sendMessage at ~:844.

D-04 new toast string (unconstrained by D-01a, no existing string to preserve):
  "Screenshot too large to send (over 12 MB) — remove a capture and retry"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate _doSend + _doElementSend toast strings behind mapSendOutcome (D-01 / D-01a)</name>
  <files>entrypoints/review.content/card.ts</files>
  <read_first>
    - entrypoints/review.content/card.ts (lines 380-443 _doSend and 820-873 _doElementSend) — the exact three-branch catch sites and their control-restore logic
    - entrypoints/review.content/toast.ts (lines 1-30) — showToast signature + INVARIANT C (host-derived strings stay textContent-only)
    - lib/error-toast.ts (created in Plan 08-01) — mapSendOutcome + SendOutcome + ToastSpec contract
    - .planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md (section "entrypoints/review.content/card.ts") — consolidation guidance: replace only the string-construction; keep control-restore inline; keep the dead-channel guard
  </read_first>
  <action>
    Import mapSendOutcome (and the SendOutcome type) from the lib error-toast module into card.ts. In both
    _doSend and _doElementSend Send callbacks, replace ONLY the three inline string constructions with a
    mapSendOutcome call: in the dead-channel branch build { kind: 'channel-dead', lastErrorMessage:
    chrome.runtime.lastError?.message }, in the success branch { kind: 'ok', file: resp.file }, in the
    resp.ok===false branch { kind: 'relay-error', error: resp.error }; then call showToastFn(spec.message,
    spec.isError). Do NOT remove the chrome.runtime.lastError || !resp guard (SW-eviction safety net — security
    invariant). Do NOT change any control-restore logic, the _doClose/onSent success-side effects, or the
    re-disable rule — those stay exactly as-is inline at each call-site. The emitted strings MUST be byte-identical
    to today (D-01a): dead-channel 'Extension error: ' + (... ?? 'no response'); success single-backslash
    `wrote notes\<file>`; relay-error the host string passed through. Leave the three 'Screenshot capture failed'
    capture-pipeline toasts untouched (not Send-relay errors). Do NOT touch chip.ts (Phase-3 stub, out of scope).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - card.ts imports mapSendOutcome from the lib error-toast module
    - Both _doSend and _doElementSend call mapSendOutcome for all three branches (channel-dead, ok, relay-error)
    - The literal strings 'Extension error: ' and the `wrote notes\\` success format no longer appear inline in card.ts (they now live only in lib/error-toast.ts) — grep of card.ts for 'Extension error: ' returns 0 matches
    - The chrome.runtime.lastError || !resp guard is still present at both call-sites
    - chip.ts is unchanged (git diff shows no chip.ts modification)
    - The three 'Screenshot capture failed' toasts remain in card.ts (unchanged)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Both production Send call-sites route their toast string through the mapper; emitted strings and trigger behavior are byte-identical to before; the dead-channel guard is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the pre-flight payload-size guard into both Send paths (D-04, REL-03)</name>
  <files>entrypoints/review.content/card.ts</files>
  <read_first>
    - entrypoints/review.content/card.ts (lines 388-410 _doSend payload assembly + sendMessage; lines 825-846 _doElementSend payload assembly + sendMessage) — exact insertion points and the error-branch control-restore to mirror
    - lib/payload-size.ts (created in Plan 08-01) — exceedsBodyCap(jsonBody) contract
    - .planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md (section "lib/payload-size.ts", "Insertion in card.ts") — exact insertion guidance + suggested toast string
  </read_first>
  <action>
    Import exceedsBodyCap from the lib payload-size module into card.ts. In _doSend, immediately after the payload
    object is assembled and BEFORE chrome.runtime.sendMessage, compute exceedsBodyCap(JSON.stringify(payload)); if
    true, call showToastFn("Screenshot too large to send (over 12 MB) — remove a capture and retry", true), restore
    controls exactly as the existing relay-error branch does (re-enable sendBtn/cancelBtn, clear textarea.readOnly,
    re-apply the has-text disabled rule), then return — no SW round-trip. In _doElementSend, do the same after its
    payload (with screenshots[]) is assembled and before its sendMessage, using restoreControls() then the
    sendBtn.disabled = textarea.value.trim().length === 0 rule for restore. Use JSON.stringify(payload) as the
    counting basis (same UTF-8 JSON bytes the host counts). The host 413 backstop is left intact (handled by the
    server; verified in Plan 03). This is the only new toast string and is unconstrained by D-01a.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - card.ts imports exceedsBodyCap from the lib payload-size module
    - Both _doSend and _doElementSend call exceedsBodyCap(JSON.stringify(payload)) BEFORE chrome.runtime.sendMessage
    - On overflow each path shows the over-12MB toast, restores controls (mirroring its existing error branch), and returns without calling sendMessage
    - grep of card.ts for "over 12 MB" returns the new pre-flight toast string at both Send paths
    - No existing Send behavior for under-cap payloads is altered (the sendMessage call is still reached when not oversize)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Oversize payloads are blocked in both Send paths before the SW relay with a clear toast and proper control restore; the host 413 backstop is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page/content script -> service worker | The mapper formats only already-derived strings; no new path for token/secret to reach the page |
| content script pre-flight -> host | Pre-flight is defense-in-depth; the host 12MB 413 remains authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | card.ts toast rendering via mapper | mitigate | Consolidation reproduces existing strings verbatim (D-01a); host-derived resp.error stays textContent-only (toast.ts INVARIANT C). No new interpolation of token or internal filesystem paths is introduced — grep confirms removed inline strings exist only in the tested lib mapper. |
| T-08-02 | Denial of Service | card.ts pre-flight guard | mitigate | exceedsBodyCap blocks oversize payloads before the SW round-trip; the dead-channel guard and host 413 backstop are preserved (no weakening of the existing resource guard). |
</threat_model>

<verification>
- npx tsc --noEmit exits 0
- npm run test:lib still exits 0 (mapper/pre-flight unit contracts from Plan 01 unchanged)
- git diff confirms chip.ts is untouched and the three capture-failure toasts remain
- grep confirms 'Extension error: ' no longer appears inline in card.ts and "over 12 MB" appears at both Send paths
</verification>

<success_criteria>
- Both production Send call-sites use mapSendOutcome with byte-identical emitted strings (REL-01 / D-01a)
- Oversize payloads blocked pre-flight with a clear toast in both Send paths (REL-03 / D-04)
- Dead-channel guard preserved; chip.ts and capture-failure toasts untouched (no regression)
</success_criteria>

<output>
Create `.planning/phases/08-hardening-pre-release-audit/08-02-SUMMARY.md` when done
</output>
