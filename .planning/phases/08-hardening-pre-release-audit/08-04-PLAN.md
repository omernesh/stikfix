---
phase: 08-hardening-pre-release-audit
plan: 04
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - scripts/clean-room-check.mjs
  - CLEAN-ROOM.md
  - .planning/phases/08-hardening-pre-release-audit/08-UAT.md
autonomous: true
requirements: [REL-01, REL-02, REL-03]
user_setup: []

must_haves:
  truths:
    - "node scripts/clean-room-check.mjs returns zero matches across the entire repo for the known banned tokens plus any self-audited provenance-risky constants, and exits 0 inside npm run check"
    - "08-UAT.md enumerates all five REL-01 failure paths with repro + expected verbatim toast, plus the D-05 SW-eviction + multi-note-increment regression and the D-04 oversize pre-flight check"
    - "The clean-room script does not self-trip on its own banned-token definitions (fragment construction)"
  artifacts:
    - path: "scripts/clean-room-check.mjs"
      provides: "Extended banned set + self-audited constants (D-03), still wired into npm run check"
      contains: "BANNED"
    - path: "CLEAN-ROOM.md"
      provides: "Recorded extended banned set + no-peek self-audit method + zero-match result"
      contains: "self-audit"
    - path: ".planning/phases/08-hardening-pre-release-audit/08-UAT.md"
      provides: "Manual Chrome runbook: 5 failure paths + D-05 regression + D-04 oversize"
      contains: "Host unreachable"
  key_links:
    - from: "scripts/clean-room-check.mjs"
      to: "npm run check"
      via: "already invoked in the check script chain"
      pattern: "clean-room-check"
---

<objective>
Close the GPL clean-room provenance gate (D-03, SC-4): extend scripts/clean-room-check.mjs
with the known banned tokens plus any provenance-risky constants surfaced by a NO-PEEK
self-audit of OUR repo, confirm zero matches repo-wide, and record the method + result in
CLEAN-ROOM.md. Also author the thin manual Chrome UAT runbook (08-UAT.md, D-01/D-05)
enumerating all five REL-01 failure paths, the D-05 SW-eviction + multi-note regression,
and the D-04 oversize pre-flight confirmation.

Purpose: Release-blocking provenance integrity (MIT-vs-GPL) + a durable runbook for the
runtime confirmations that cannot run in node:test.
Output: Extended clean-room-check.mjs, CLEAN-ROOM.md provenance record, 08-UAT.md runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hardening-pre-release-audit/08-CONTEXT.md
@.planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md

<interfaces>
<!-- Existing clean-room-check.mjs structure + the UAT analog. -->

Fragment construction (clean-room-check.mjs:16-24) — MUST use so the script never self-trips:
  const BANNED = [
    new RegExp('__' + 'opc' + '_', 'i'),
    new RegExp('open' + 'code', 'i'),
    new RegExp('Jodus' + 'Nodus', 'i'),
  ];
  Any NEW banned token must be split into fragments the same way (clean-room-check.mjs is NOT in SKIP_FILENAMES — it scans itself).

Skip sets (clean-room-check.mjs:32-55): SKIP_DIRS (node_modules, .git, dist, .planning, notes, private, .claude, .qmd-memory) + SKIP_FILENAMES (PRD.md, README.md, CLAUDE.md, LICENSE, CLEAN-ROOM.md). Recursive walk over process.cwd().

UAT analog (.planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md:1-37): title + Purpose/Who-runs/Time-estimate header, a fixture/scenario table, a "Before You Begin" section, numbered "Steps", cross-platform (bash + PowerShell) commands.

Five REL-01 failure paths + expected VERBATIM toasts (from RESEARCH Failure-Path Inventory):
  1. Host unreachable -> "Host unreachable: …"
  2. 401 token mismatch -> "unauthorized"
  2b. No token set for host -> 'No token set for host "…" — enter it in the popup'
  3. 413 body too large -> "Payload Too Large" (or "Host unreachable: …" via reset — both visible; D-04 pre-flight gives a deterministic toast first)
  4. SW evicted mid-flight -> "Extension error: …" (repro: chrome://extensions -> service worker -> Stop, then Send; or idle >30s)
  5. No host mapped for origin -> "No host mapped for origin: …"
D-05 regression: (a) SW idle-eviction state survival + subsequent Send still routes; (b) multi-note serial increment 0001 -> 0002.
D-04 manual: a near-12 MB screenshot Send shows the new pre-flight toast BEFORE any round-trip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend clean-room-check.mjs with banned set + NO-PEEK self-audited constants (D-03, SC-4)</name>
  <files>scripts/clean-room-check.mjs, CLEAN-ROOM.md</files>
  <read_first>
    - scripts/clean-room-check.mjs (full file) — BANNED fragment-construction (16-24), SKIP_DIRS/SKIP_FILENAMES (32-55), walk() (65-97)
    - CLEAN-ROOM.md (full file) — existing provenance record to append the extended banned set + method + result
    - lib/element-context.ts (curated computed-styles config constant) — primary self-audit target for provenance-risky magic strings/selector arrays
    - .planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md (section "scripts/clean-room-check.mjs", Pitfall 4 + Pitfall 5) — fragment trick + no-peek self-audit method
  </read_first>
  <action>
    Perform a NO-PEEK self-audit (D-03): do NOT open the GPL upstream. Scan OUR OWN repo for provenance-risky
    constants — inspect lib/element-context.ts curated computed-styles config, any unusual data-attribute names, any
    verbatim CSS-selector arrays that look copied rather than written from spec. The banned set = the three known
    tokens already present PLUS any provenance-risky constant the self-audit surfaces (if our code is genuinely
    clean-room from spec, the audit may add nothing new beyond the three known tokens — that is a valid PASS, Pitfall 5).
    For every NEW banned token added to scripts/clean-room-check.mjs BANNED array, use the fragment-construction trick
    (e.g. new RegExp('frag' + 'ment', 'i')) so the script never matches its own definition (Pitfall 4). Keep the
    script wired into npm run check (no change to the check chain). Record in CLEAN-ROOM.md: the extended banned set,
    the no-peek self-audit method, the files inspected, and the zero-match result.
  </action>
  <verify>
    <automated>node scripts/clean-room-check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/clean-room-check.mjs exits 0 with zero matches across the repo
    - Every banned token in clean-room-check.mjs (existing + any new) is defined via fragment construction so the script does not self-trip (a repo grep for the intact tokens returns only the fragment-constructed definitions)
    - CLEAN-ROOM.md records the extended banned set, the no-peek self-audit method, files inspected, and the zero-match PASS
    - The GPL upstream was NOT opened (NO-PEEK honored — no upstream file referenced or fetched)
    - clean-room-check.mjs is still invoked by npm run check
    - npm run check exits 0
  </acceptance_criteria>
  <done>The clean-room gate returns zero matches repo-wide (no-peek self-audit complete), does not self-trip, stays in npm run check, and the provenance method/result is recorded in CLEAN-ROOM.md.</done>
</task>

<task type="auto">
  <name>Task 2: Author the manual Chrome UAT runbook 08-UAT.md (D-01 / D-04 / D-05)</name>
  <files>.planning/phases/08-hardening-pre-release-audit/08-UAT.md</files>
  <read_first>
    - .planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md (full file) — structure to mirror: header, scenario table, Before You Begin, numbered cross-platform steps
    - .planning/phases/08-hardening-pre-release-audit/08-RESEARCH.md (Failure-Path Inventory + Pitfall 1) — the five paths' verbatim toasts + the SW-eviction repro method
    - .planning/phases/08-hardening-pre-release-audit/08-PATTERNS.md (section "08-UAT.md") — the authoritative required-content checklist
  </read_first>
  <action>
    Create 08-UAT.md mirroring the 07-HUMAN-UAT.md structure (title "Phase 08 — Human UAT Runbook", a
    Purpose/Who-runs/Time-estimate header, a scenario table, a "Before You Begin" section with host-start +
    Review-Mode-enter steps, then numbered Steps with cross-platform bash + PowerShell commands). Enumerate ALL
    FIVE REL-01 failure paths with repro + expected verbatim toast + a PASS checkbox each: (1) host unreachable ->
    "Host unreachable: …"; (2) 401 token mismatch -> "unauthorized"; (2b) no token for host ->
    'No token set for host "…" — enter it in the popup'; (3) 413 body too large -> "Payload Too Large" or
    "Host unreachable: …" via reset, noting D-04 pre-flight gives a deterministic toast first; (4) SW evicted
    mid-flight -> "Extension error: …", repro: chrome://extensions -> service worker -> Stop then Send, or idle
    >30s; (5) no host mapped for origin -> "No host mapped for origin: …". Add the D-05 regression section:
    (a) SW idle-eviction state survival + a subsequent Send still routes correctly; (b) multi-note serial increment
    0001 -> 0002. Add the D-02a manual rapid multi-Send (folded here, non-blocking). Add the D-04 manual check: a
    near-12 MB screenshot Send shows the pre-flight over-12-MB toast before any round-trip. This is a manual,
    non-blocking deliverable (no CI gate) per D-01/D-05.
  </action>
  <verify>
    <automated>node --input-type=module -e "import {readFileSync} from 'node:fs'; const t=readFileSync('.planning/phases/08-hardening-pre-release-audit/08-UAT.md','utf8'); const need=['Host unreachable','unauthorized','No token set for host','Payload Too Large','Extension error','No host mapped for origin','0001','0002','over 12 MB','service worker']; const miss=need.filter(s=>!t.includes(s)); if(miss.length){console.error('MISSING: '+miss.join(', '));process.exit(1)} console.log('08-UAT.md covers all required scenarios')"</automated>
  </verify>
  <acceptance_criteria>
    - 08-UAT.md exists and mirrors the 07-HUMAN-UAT.md structure (header + scenario table + Before You Begin + numbered Steps)
    - All five REL-01 failure paths are enumerated with repro + expected verbatim toast: "Host unreachable", "unauthorized", "No token set for host", "Payload Too Large", "Extension error", "No host mapped for origin"
    - The D-05 regression section covers SW idle-eviction state survival + subsequent routed Send, AND multi-note increment 0001 -> 0002
    - The D-04 manual over-12-MB pre-flight check and the SW-eviction repro (chrome://extensions service worker Stop) are present
    - The coverage verify command prints "08-UAT.md covers all required scenarios" and exits 0
  </acceptance_criteria>
  <done>08-UAT.md is a complete manual runbook covering all five REL-01 paths, the D-05 SW-eviction + multi-note regression, the D-02a rapid multi-Send, and the D-04 oversize pre-flight confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source -> release artifact | GPL-identifier leakage is a license/provenance integrity boundary (MIT deliverable) |
| audit script -> its own source | The scanner must not self-trip on banned-token definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering (provenance integrity) | scripts/clean-room-check.mjs banned set | mitigate | Extend banned set via NO-PEEK self-audit (D-03); fragment-construct every token so the scanner does not self-trip; zero matches repo-wide; keep gate in npm run check. CLEAN-ROOM.md records method + result. This is a release-blocking license gate, not a runtime threat. |
| T-08-01 | Information Disclosure | 08-UAT.md verbatim toast expectations | accept | The runbook documents the already-shipping toast strings (verified in RESEARCH that none expose the host token or internal paths beyond the user-facing filename/error). No new strings introduced here. Disposition: documentation-only, no secret surfaced. |
</threat_model>

<verification>
- node scripts/clean-room-check.mjs exits 0 with zero matches
- npm run check exits 0 (clean-room gate still wired in)
- 08-UAT.md coverage verify prints success and exits 0
- CLEAN-ROOM.md records the no-peek self-audit method + result
</verification>

<success_criteria>
- Clean-room grep returns zero matches repo-wide for the banned tokens + self-audited constants (SC-4)
- The script does not self-trip and remains in npm run check (D-03)
- 08-UAT.md fully enumerates the five REL-01 paths + D-05 regression + D-04 oversize (REL-01/REL-02/REL-03 manual confirmation)
</success_criteria>

<output>
Create `.planning/phases/08-hardening-pre-release-audit/08-04-SUMMARY.md` when done
</output>
