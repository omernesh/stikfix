---
phase: 03-extension-skeleton-sw-relay-proof
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - entrypoints/review.content/index.ts
  - entrypoints/review.content/chip.ts
  - entrypoints/review.content/styles.css
autonomous: true
requirements: [EXT-02, EXT-05, EXT-07, EXT-08, EXT-11]
must_haves:
  truths:
    - "A runtime-registered content script mounts a connection chip inside a createShadowRootUi shadow root at z-index 2147483647"
    - "The chip is draggable via pointer events and clamped to the viewport"
    - "The chip shows the routed project + notesDir, or a one-time project dropdown when the origin is unmapped"
    - "The stub Send builds a minimal valid §9.1 free-note payload and sends SEND_ANNOTATION to the SW (never fetches localhost itself)"
    - "Exit unmounts the shadow-root UI"
  artifacts:
    - path: "entrypoints/review.content/index.ts"
      provides: "defineContentScript registration:'runtime' + createShadowRootUi mount/unmount"
      contains: "registration"
    - path: "entrypoints/review.content/chip.ts"
      provides: "Chip DOM, pointer-events drag+clamp, stub Send (§9.1 payload), Exit, one-time dropdown, inline confirm/error"
      contains: "SEND_ANNOTATION"
    - path: "entrypoints/review.content/styles.css"
      provides: "Shadow-root CSS (:host all:initial, px units, z-index 2147483647)"
      contains: "2147483647"
  key_links:
    - from: "entrypoints/review.content/chip.ts"
      to: "background.ts SEND_ANNOTATION"
      via: "chrome.runtime.sendMessage with §9.1 payload"
      pattern: "SEND_ANNOTATION"
    - from: "entrypoints/review.content/index.ts"
      to: "createShadowRootUi"
      via: "WXT content-script ctx"
      pattern: "createShadowRootUi"
---

<objective>
Build the injected review UI: a WXT `registration:'runtime'` content script that mounts a connection chip inside a `createShadowRootUi` shadow root. The chip is draggable + viewport-clamped (pointer events), shows the project this tab routes to + its notes dir (or a one-time project dropdown when unmapped), carries an Exit button, and a stub Send that constructs a minimal valid §9.1 free-note payload and hands it to the SW via `SEND_ANNOTATION` — proving the CS→SW→host relay end-to-end. This plan also definitively resolves RESEARCH Open Question A4 (whether WXT auto-injects the runtime script's CSS) on its first build.

Purpose: EXT-02 (on-demand runtime injection, no static content_scripts), EXT-11 (draggable clamped chip), the content-script half of EXT-05 (the relay's origin), and EXT-07/08 (the one-time dropdown that persists origin→host). The stub Send is the relay proof — Success Criterion 3.

Output: `entrypoints/review.content/` (index.ts, chip.ts, styles.css) and a recorded A4 answer fed back to Plan 02's injection (auto-CSS vs insertCSS fallback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-RESEARCH.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-UI-SPEC.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-01-SUMMARY.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-02-SUMMARY.md

<interfaces>
<!-- Plan 01 lib + Plan 02 SW contracts. Exact message-type names in 03-01/03-02 SUMMARYs. -->
From lib/types.ts: SfxMessage (SEND_ANNOTATION, GET_ROUTE, and any SET_ROUTE/dropdown-selection message added in Plan 02), SfxResponse, AnnotationPayload
SW messages (Plan 02): GET_ROUTE {tabId,origin} -> {ok:true,route:{name,notesDir,...}} | {ok:false,error:'unmapped'};  SEND_ANNOTATION {tabId,payload} -> {ok:true,file,serial} | {ok:false,error}

<!-- §9.1 free-note payload the stub Send builds (host shape guard server.ts 106-118 requires all of these): -->
{ mode:'free', comment:'stickyfix relay proof', page:{url:location.href, title:document.title},
  viewport:{width:innerWidth, height:innerHeight, devicePixelRatio}, screenshots:[] }

<!-- WXT runtime-injection pattern (RESEARCH Pattern 2): registration:'runtime', cssInjectionMode:'ui', import './styles.css' top-level. Output path: content-scripts/review.js (+ review.css). createShadowRootUi REQUIRES ctx from main(ctx) — cannot use executeScript({func}). -->
<!-- A4: 03-01/03-02 SUMMARY note whether CSS auto-injects; THIS plan's first build is the definitive check. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runtime content script + createShadowRootUi mount; resolve A4 on first build</name>
  <files>entrypoints/review.content/index.ts, entrypoints/review.content/styles.css</files>
  <read_first>
    - 03-RESEARCH.md §"Pattern 2" (defineContentScript registration:'runtime', cssInjectionMode:'ui', top-level import './styles.css', createShadowRootUi onMount/onRemove, ctx.onInvalidated) + the "Warning" that createShadowRootUi needs ctx from main() — NOT executeScript func
    - 03-RESEARCH.md §"Pattern 9" CSS block (:host all:initial, pointer-events passthrough, px units, z-index 2147483647) + §"Pitfall 5" (no rem units) + §"Open Questions Q1 / A4 / If A4 is wrong" (the insertCSS fallback)
    - 03-RESEARCH.md §"Pitfall 4" (output path content-scripts/review.js / review.css)
    - 03-02-SUMMARY.md — whether Plan 02's ENTER_REVIEW already calls insertCSS or relies on auto-inject
  </read_first>
  <action>
    Create entrypoints/review.content/index.ts as a WXT defineContentScript with matches:['<all_urls>'], registration:'runtime' (so it is NOT added to the static manifest — EXT-02), cssInjectionMode:'ui', and a top-level import of './styles.css'. In main(ctx), await createShadowRootUi(ctx, { name:'sfx-review-ui', position:'inline', zIndex:2147483647, onMount(container){ call mountChip(container, ...) }, onRemove({unmount}){unmount()} }); then ui.mount() and ctx.onInvalidated(ui.remove). Import mountChip from chip.ts (Task 2 supplies the full version — for THIS task a minimal mount that appends a placeholder chip element is acceptable so the build succeeds).
    Create entrypoints/review.content/styles.css per Pattern 9: :host { all:initial; ... z-index:2147483647; pointer-events:none } and #sfx-chip { pointer-events:auto; ... } using px units exclusively (Pitfall 5 — NO rem). Neutral functional palette per UI-SPEC (NOT post-it styling).
    RESOLVE A4 ON THIS BUILD: run npm run build, then inspect .output/chrome-mv3/content-scripts/ — confirm review.js exists, and determine whether review.css is emitted and whether WXT auto-injects it for the runtime script into the shadow root. Record the definitive answer in this plan's SUMMARY. If CSS does NOT auto-inject for the runtime-registered script, confirm Plan 02's ENTER_REVIEW includes the scripting.insertCSS({files:['content-scripts/review.css']}) fallback — flag it in SUMMARY so Plan 02 is corrected if it relied on auto-inject.
    Keep sfx-*/stickyfix identifiers; no upstream code.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const fs=require('fs');const p='.output/chrome-mv3/content-scripts/';const m=require('./.output/chrome-mv3/manifest.json');if(!fs.existsSync(p+'review.js'))process.exit(1);if(m.content_scripts)process.exit(2);console.log('review.js emitted; review.css present:',fs.existsSync(p+'review.css'))"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0; .output/chrome-mv3/content-scripts/review.js exists (EXT-02 runtime injection path)
    - .output/chrome-mv3/manifest.json still has NO content_scripts key (the review script is runtime-only)
    - styles.css uses z-index 2147483647 and px units only (grep: no `rem` in styles.css)
    - SUMMARY records the definitive A4 answer (review.css auto-injected? yes/no) and the resulting CSS-injection decision for Plan 02
  </acceptance_criteria>
  <done>Runtime content script builds and emits content-scripts/review.js with no static content_scripts; shadow-root CSS uses px + z-index 2147483647; A4 definitively answered and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Chip — draggable+clamped, routed-project label / one-time dropdown, stub Send relay, Exit</name>
  <files>entrypoints/review.content/chip.ts</files>
  <read_first>
    - 03-RESEARCH.md §"Pattern 9" (mountChip DOM via createElement/textContent — no innerHTML; makeDraggable pointer-events with setPointerCapture + viewport clamp) + §"Pattern 10" (sendDummy: §9.1 payload, SEND_ANNOTATION, showToast on {ok}/{ok:false})
    - 03-UI-SPEC.md §"2. Connection Chip" (status dot + "→ <project> · <notesDir>" label, stub Send, Exit ×, one-time dropdown when unmapped, inline confirm/error ~1.5s, ephemeral position, keyboard-reachable Exit)
    - 03-02-SUMMARY.md — GET_ROUTE response shape (route.name/notesDir or unmapped), the SET_ROUTE/dropdown-selection message name (if added), SEND_ANNOTATION response shape
    - host/src/types.ts — AnnotationPayload shape for the §9.1 free-note payload
  </read_first>
  <behavior>
    - On mount: send GET_ROUTE for the current tab; if {ok:true} render the label "→ <project> · <notesDir>" + status dot; if {ok:false,error:'unmapped'} render a project dropdown of discovered hosts instead
    - Selecting a project from the dropdown sends the SET_ROUTE/dropdown-selection message (persists origin→host in the SW), then re-renders as the labeled chip; that origin is never asked again (EXT-07/08)
    - Stub Send: builds the §9.1 free-note payload (comment 'stickyfix relay proof'), sends SEND_ANNOTATION; on {ok:true} flashes inline "sent ✓ <file>" ~1.5s; on {ok:false} shows inline error text — never silent (REL-01 shape)
    - The chip is draggable anywhere via pointer events and clamped so it cannot leave the viewport
    - Exit (×) unmounts the shadow-root UI and exits Review Mode
  </behavior>
  <action>
    Implement entrypoints/review.content/chip.ts exporting mountChip(container, ...) and teardownChip(container). Build the chip DOM with createElement/textContent (never innerHTML with host/route strings — XSS surface, Pattern 9). On mount, message GET_ROUTE; branch to either the labeled chip (status dot + "→ name · notesDir") or the one-time project dropdown built from the discovered registry. Wire dropdown selection to the SET_ROUTE/dropdown-selection message (name per 03-02-SUMMARY) so the SW persists origin→host; then swap to the labeled view.
    Implement makeDraggable(el) with pointer events per Pattern 9: pointerdown setPointerCapture, pointermove computes new top/left clamped to [0, innerWidth-offsetWidth]/[0, innerHeight-offsetHeight]. Position is ephemeral (not persisted — UI-SPEC).
    Implement the stub Send (Pattern 10): construct the §9.1 free-note AnnotationPayload from window.location.href / document.title / innerWidth / innerHeight / devicePixelRatio with comment 'stickyfix relay proof' and screenshots:[]; chrome.runtime.sendMessage SEND_ANNOTATION; render an inline "sent ✓ <file>" confirmation (~1.5s) on success or inline error text on failure (full toast system is Phase 8 — inline is enough here, but NEVER silent).
    Implement Exit (×) to remove/unmount the shadow-root UI (and message EXIT_REVIEW so the SW clears the reviewMode pref). The chip MUST NOT fetch localhost — all HTTP goes through SEND_ANNOTATION to the SW (EXT-05). Keep sfx-*/stickyfix identifiers.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit and npm run check both exit 0 (no regression to lib/host tests)
    - chip.ts contains no `fetch(` to 127.0.0.1/localhost — the relay goes through chrome.runtime.sendMessage SEND_ANNOTATION (grep returns 0 localhost fetches)
    - Chip DOM is built with createElement/textContent, not innerHTML interpolation of route name/notesDir
    - makeDraggable clamps to window.innerWidth/innerHeight (clamp math present)
    - [MANUAL-CHROME / HUMAN-UAT] On an HTTPS-origin page with a live host, toggling Review Mode shows the draggable chip with the correct project/notesDir; clicking Send writes a stub .md "stickyfix relay proof" to the notes dir and the chip shows "sent ✓" — Success Criterion 3
    - [MANUAL-CHROME / HUMAN-UAT] An unmapped-origin tab shows the one-time dropdown; after selecting, revisiting never re-asks — Success Criterion 5
    - [MANUAL-CHROME / HUMAN-UAT] Chip CSS does not leak to/from the host page (shadow-DOM isolation) and Exit unmounts it — EXT-11
  </acceptance_criteria>
  <done>Chip mounts with routed label or one-time dropdown, is draggable+clamped, stub Send relays the §9.1 payload via the SW and shows inline confirm/error, Exit unmounts; tsc + npm run check green; no localhost fetch in the content script.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit and npm run check both exit 0; npm run build green with content-scripts/review.js emitted and no static content_scripts
- Content script contains no localhost fetch (SW is the only HTTP client — grep, joint with Plans 02/03)
- styles.css: px units only, z-index 2147483647; clean-room gate green
- A4 definitively resolved and the CSS-injection decision reconciled with Plan 02
</verification>

<success_criteria>
- On-demand runtime content script injects a shadow-root chip with NO static content_scripts (EXT-02/11)
- Stub Send proves the CS→SW→host relay (a stub .md lands on disk) on an HTTPS-origin page (EXT-05, Success Criterion 3)
- Unmapped origin shows a one-time dropdown that persists origin→host and is never re-asked (EXT-07/08, Success Criterion 5)
- Chip is draggable + viewport-clamped with Exit; shadow-DOM CSS isolation holds (EXT-11)
</success_criteria>

<output>
Create `.planning/phases/03-extension-skeleton-sw-relay-proof/03-04-SUMMARY.md` when done. Record: the definitive A4 answer (review.css auto-inject yes/no) and whether Plan 02 needed an insertCSS correction, the dropdown-selection message used, and the chip's drag/clamp + inline-confirmation approach. Flag all MANUAL-CHROME items as HUMAN-UAT for phase verification.
</output>
