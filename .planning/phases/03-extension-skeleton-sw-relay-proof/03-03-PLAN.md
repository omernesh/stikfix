---
phase: 03-extension-skeleton-sw-relay-proof
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - entrypoints/popup/index.html
  - entrypoints/popup/main.ts
  - entrypoints/popup/popup.css
autonomous: true
requirements: [EXT-03, EXT-07, EXT-09]
must_haves:
  truths:
    - "The popup lists every discovered host with project name, port, a connection dot, and a per-host token field"
    - "Editing a token persists it to chrome.storage.local and survives reopening the popup"
    - "The Review Mode toggle requests <all_urls> as the FIRST call in the click handler (user gesture), before any await"
    - "An empty registry shows the 'no hosts found on 39240-39260' guidance + a Refresh button"
    - "A routing line shows the active tab's resolved project/notesDir or 'unmapped'"
  artifacts:
    - path: "entrypoints/popup/main.ts"
      provides: "Host list render, token persistence, Review Mode toggle with permission gesture, routing line"
      contains: "permissions.request"
    - path: "entrypoints/popup/index.html"
      provides: "Popup DOM scaffold (host list container, toggle, routing line)"
      min_lines: 12
    - path: "entrypoints/popup/popup.css"
      provides: "Functional-minimal popup styling (~320px, neutral palette per UI-SPEC)"
  key_links:
    - from: "entrypoints/popup/main.ts"
      to: "background.ts ENTER_REVIEW"
      via: "chrome.runtime.sendMessage after permission grant"
      pattern: "sendMessage"
    - from: "entrypoints/popup/main.ts"
      to: "chrome.storage.local sfxTokens"
      via: "setValue on token blur/change"
      pattern: "sfxTokens|setValue"
---

<objective>
Build the action popup (vanilla DOM, ~320px) per UI-SPEC: it lists every SW-discovered host with project name, port, a reachable/stale dot, and a per-host token field that persists to chrome.storage.local; it shows the active tab's routing line; and it carries the Enter/Exit Review Mode toggle whose click is the user gesture that requests `<all_urls>` and then messages the SW to inject the content script.

Purpose: EXT-03 (host list + token entry + toggle), EXT-09 (token persistence), and the popup half of the permission-gesture constraint — `chrome.permissions.request` MUST be the first call in the click handler before any await, or Chrome silently rejects it (Pitfall 3). This is the user's control surface for the relay proof.

Output: extended `entrypoints/popup/index.html`, a complete `entrypoints/popup/main.ts`, and a functional-minimal `popup.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-RESEARCH.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-UI-SPEC.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-01-SUMMARY.md
@.planning/phases/03-extension-skeleton-sw-relay-proof/03-02-SUMMARY.md

<interfaces>
<!-- Plan 01 lib + Plan 02 SW message contracts. Exact type names in 03-01/03-02 SUMMARYs. -->
From lib/storage.ts: sfxRegistry, sfxTokens, sfxOriginMap (WXT defineItems with getValue/setValue), loadStorageState()
From lib/types.ts: HostEntry { name, port, origins[], notesDir, token }, SfxMessage, SfxResponse
SW messages (Plan 02): REFRESH_HOSTS → {ok:true, registry}, ENTER_REVIEW {tabId,origin} → {ok:true,route}|{ok:false,error}, EXIT_REVIEW {tabId}, GET_ROUTE {tabId,origin} → {ok:true,route}|{ok:false,error:'unmapped'}

<!-- Permission gesture rule (RESEARCH Pattern 3 / Pitfall 3): chrome.permissions.request({origins:['<all_urls>']}) MUST be the FIRST statement in the toggle click handler — before ANY await (including storage reads). -->

<!-- Existing popup shell: index.html has <div id="sfx-popup-root">; main.ts sets placeholder text. Replace both. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Popup DOM + host list render + token persistence + routing line</name>
  <files>entrypoints/popup/index.html, entrypoints/popup/main.ts, entrypoints/popup/popup.css</files>
  <read_first>
    - 03-UI-SPEC.md §"1. Action Popup" (full layout contract: header, host list rows, token input, empty state, routing line, states) — implement exactly this functional-minimal spec, neutral palette, NO post-it aesthetics
    - 03-RESEARCH.md §"Pattern 6" (sfx storage items) — read registry/tokens/originMap for display; write tokens
    - 03-02-SUMMARY.md — REFRESH_HOSTS / GET_ROUTE response shapes the popup consumes
    - entrypoints/popup/index.html + main.ts (current placeholder) — the shell to replace; keep the #sfx-popup-root mount + the module script tag
  </read_first>
  <behavior>
    - On open: reads sfxRegistry + sfxTokens, renders one row per host (name bold, port, connection dot), pre-filling each token field from sfxTokens
    - Editing a token field and blurring persists it via sfxTokens.setValue; reopening the popup shows the saved token (EXT-09 token persistence)
    - Empty registry renders the UI-SPEC empty state ("No hosts found on 39240-39260… npm run host -- --root <dir>") + a Refresh button that sends REFRESH_HOSTS and re-renders
    - A routing line shows the active tab's resolved "→ <project> · <notesDir>" (via GET_ROUTE) or "unmapped — pick on page"
    - Loading state shown while probing/refreshing
  </behavior>
  <action>
    Replace entrypoints/popup/index.html with the UI-SPEC scaffold: header (stickyfix wordmark + host-count summary), a host-list container, a routing-line element, and the Review Mode toggle button (toggle wiring is Task 2). Keep the #sfx-popup-root mount and the module script reference; add a <link>/import for popup.css.
    Write entrypoints/popup/main.ts (vanilla DOM, no framework — D-07): on load, read sfxRegistry/sfxTokens/sfxOriginMap, render the host rows (name, port, a green/grey connection dot, a monospace token input prefilled from sfxTokens). Wire each token input's change/blur to sfxTokens.setValue(...) then re-render the summary. Render the empty state + Refresh (sends REFRESH_HOSTS via chrome.runtime.sendMessage, then re-reads/re-renders). Query the active tab (chrome.tabs.query active+currentWindow), send GET_ROUTE, and render the routing line from the response.
    Create entrypoints/popup/popup.css: ~320px width, neutral/legible functional baseline per UI-SPEC (NO warm-paper/post-it styling — that is Phase 6). px units, clear connection-dot colors, monospace token field.
    Build DOM with createElement/textContent (no innerHTML with host-provided strings). Keep sfx-*/stickyfix namespace.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0 (popup compiles + bundles)
    - main.ts persists tokens via sfxTokens.setValue (grep: setValue present, tied to a token input event)
    - popup contains no `fetch(` to 127.0.0.1/localhost (all HTTP is the SW's job — grep returns 0)
    - Host rows are built with textContent/createElement, not innerHTML interpolation of host names
    - [MANUAL-CHROME / HUMAN-UAT] With a live host running, opening the popup lists it with a token field; entering a token and reopening shows it persisted — Success Criterion 1
  </acceptance_criteria>
  <done>Popup renders the discovered host list with persisting per-host tokens, an empty state + Refresh, and a routing line; tsc + build green; no localhost fetch in popup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Review Mode toggle with the permission-gesture constraint</name>
  <files>entrypoints/popup/main.ts</files>
  <read_first>
    - 03-RESEARCH.md §"Pattern 3" (permission request inside user gesture — the EXACT ordering: chrome.permissions.request FIRST, before any await) + §"Pitfall 3" (await-before-request silently rejects) + §"Anti-Patterns" (requesting <all_urls> from SW is impossible — only popup click qualifies)
    - 03-UI-SPEC.md §"1. Action Popup" Review Mode toggle + states (permission-denied → "grant access")
    - 03-02-SUMMARY.md — ENTER_REVIEW / EXIT_REVIEW message + response shapes
  </read_first>
  <behavior>
    - Clicking "Enter Review Mode": chrome.permissions.request({origins:['<all_urls>']}) is the FIRST statement (no await precedes it); if not granted, the toggle shows "grant access" and does not message the SW
    - On grant: queries the active tab, sends ENTER_REVIEW {tabId,origin}; on {ok:true} the toggle flips to "Exit Review Mode"; on {ok:false} shows the error inline
    - Clicking "Exit Review Mode": sends EXIT_REVIEW {tabId}; toggle flips back to "Enter Review Mode"
    - The toggle reflects the active tab's current review state on popup open (from sfxPrefs)
  </behavior>
  <action>
    Add the Review Mode toggle handler to main.ts. CRITICAL ORDERING (Pattern 3 / Pitfall 3): inside the click handler, call `const granted = await chrome.permissions.request({origins:['<all_urls>']})` as the very first line — do NOT read storage or query tabs before it, or Chrome breaks the gesture chain and silently denies. If !granted, render the "grant access" state and return. Only after a grant: chrome.tabs.query for the active tab, compute its origin, send ENTER_REVIEW, and on success flip the button to Exit. Wire the Exit branch to send EXIT_REVIEW. On popup open, set the toggle label from the active tab's sfxPrefs.reviewMode entry.
    Render the permission-denied and ENTER_REVIEW-error states inline per UI-SPEC. Keep sfx-*/stickyfix namespace.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run check` both exit 0
    - chrome.permissions.request is the first awaited call in the toggle click handler — no chrome.storage/tabs await precedes it (read main.ts: the request line appears before any other `await` in that handler)
    - The handler requests origins:['<all_urls>'] (matches optional_host_permissions from Plan 01)
    - On !granted the handler returns without sending ENTER_REVIEW
    - [MANUAL-CHROME / HUMAN-UAT] Toggling Review Mode shows the Chrome permission prompt once, then injects the content script (chip appears) — Success Criterion 2 (joint with Plan 04)
  </acceptance_criteria>
  <done>The toggle requests <all_urls> as the first gesture call, messages ENTER/EXIT_REVIEW, and reflects/updates review state; tsc + npm run check green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` and `npm run check` both exit 0; `npm run build` green
- Popup contains no localhost fetch (SW is the only HTTP client — grep)
- Permission request ordering verified (first awaited call in the toggle handler)
- clean-room gate green
</verification>

<success_criteria>
- Popup lists discovered hosts with project name + per-host token entry that persists in chrome.storage.local (EXT-03/09)
- Review Mode toggle correctly requests <all_urls> in the user gesture and drives ENTER/EXIT_REVIEW (EXT-03 toggle + permission constraint)
- Routing line + empty state + states match the functional-minimal UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/03-extension-skeleton-sw-relay-proof/03-03-SUMMARY.md` when done. Record: the popup's DOM structure, how token persistence is wired, and confirmation the permission-gesture ordering is correct (first awaited call).
</output>
