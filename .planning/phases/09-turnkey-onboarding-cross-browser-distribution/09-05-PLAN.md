---
phase: 09-turnkey-onboarding-cross-browser-distribution
plan: "05"
type: execute
wave: 4
gap_closure: true
depends_on: ["09-04"]
files_modified:
  - host/src/validate-folder.ts
  - host/src/native-host.ts
  - host/src/server.ts
  - host/src/write-note.ts
  - host/test/server.test.ts
  - host/test/validate-folder.test.ts
  - entrypoints/background.ts
  - entrypoints/review.content/chip.ts
  - lib/types.ts
requirements: [ONB-04, D-04]
must_haves:
  truths:
    - "Dropping the first note on an origin that has NO folder mapping opens the OS folder dialog; the chosen folder is persisted (origin→folder) and the note is written into <chosen>/notes"
    - "A second note on the same origin reuses the folder silently — no dialog"
    - "Cancelling the dialog surfaces a visible toast and does NOT silently drop the note (Reliability constraint)"
    - "The host re-validates every targetDir on every request (absolute + existsSync + isDirectory + system-dir deny-list) and confines writes to <targetDir>/notes; an invalid/ system targetDir → 400, never a write outside it"
    - "When no targetDir is supplied the host writes to cfg.notesDir exactly as before (back-compat — existing --root flow unchanged, no regression)"
    - "origin is always derived from chrome.tabs.get in the SW, never from the message body (anti-spoof preserved)"
  artifacts:
    - path: "host/src/validate-folder.ts"
      provides: "shared validateChosenFolder() used by BOTH native-host and server"
    - path: "host/src/server.ts"
      provides: "optional targetDir on all annotation endpoints, re-validated + confined"
    - path: "entrypoints/review.content/chip.ts"
      provides: "needs-folder → PICK_FOLDER → retry send, toast on cancel"
---

<objective>
Wire D-04 end-to-end so the first note on an unmapped origin opens the folder dialog, the chosen folder is persisted and used for the write, and subsequent notes reuse it silently. Preserve the existing --root flow as the default (no regression) and keep all Phase 8 security invariants (token gate, anti-spoof, validated/confined writes).
</objective>

<context>
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-04-SUMMARY.md
@host/src/native-host.ts
@host/src/server.ts
@host/src/write-note.ts
@host/src/config.ts
@host/src/security.ts
@entrypoints/background.ts
@lib/types.ts

<interfaces>
From host/src/native-host.ts (09-04): validateChosenFolder(folder, plat) + handlePickFolder — EXTRACT validateChosenFolder to a shared module.
From host/src/security.ts: isInsideDir(root, target).
From host/src/config.ts: ensureNotesDir(dir).
From host/src/write-note.ts: writeNote(notesDir, payload, serial); getNextSerial(notesDir); withSerialLock.
From entrypoints/background.ts: handleSendAnnotation, handlePickFolder(tabId), handleListAnnotations/Edit/Delete/Screenshot, sfxOriginMap.
From lib/storage.ts: sfxOriginMap (origin→folder already persisted by handlePickFolder in 09-04), sfxRegistry, sfxTokens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared folder validation</name>
  <files>host/src/validate-folder.ts, host/src/native-host.ts, host/test/validate-folder.test.ts</files>
  <action>
    Create host/src/validate-folder.ts exporting validateChosenFolder(folder: string, plat = process.platform): string | null — MOVE the exact logic from native-host.ts (absolute via isAbsolute + existsSync + statSync().isDirectory() + system-dir deny-list: '/','/System','/usr','/etc'; win32 'C:\\Windows','C:\\Program Files'; normalized + case-insensitive on win32). Returns the resolved absolute folder if valid, else null. native-host.ts imports validateChosenFolder from validate-folder.js (delete its local copy; behavior unchanged). Add host/test/validate-folder.test.ts covering valid dir, non-absolute, missing, file-not-dir, and each system dir → null. Keep native-host.test.ts green (it may now import from the new module path — update import if needed).
  </action>
  <verify><automated>npx tsc --noEmit -p tsconfig.host.json &amp;&amp; node --test "dist/host/test/validate-folder.test.js" "dist/host/test/native-host.test.js"</automated></verify>
  <acceptance_criteria>
    - SOURCE: validateChosenFolder lives in validate-folder.ts and is imported by native-host.ts (single source of truth).
    - TEST: validate-folder.test.js covers valid + all rejection cases; native-host.test.js still passes.
  </acceptance_criteria>
  <done>Folder validation is a shared, tested helper used by the native host (and, next, the server).</done>
</task>

<task type="auto">
  <name>Task 2: Host — optional targetDir on all annotation endpoints</name>
  <files>host/src/server.ts, host/src/write-note.ts, host/test/server.test.ts</files>
  <read_first>
    - host/src/server.ts (POST /annotation lines ~120-127; GET /annotations ~168; PUT/DELETE /annotation/:serial ~223/255; GET /screenshot ~316 — all currently hard-code cfg.notesDir)
    - host/src/validate-folder.ts (Task 1)
    - host/src/security.ts (isInsideDir), host/src/config.ts (ensureNotesDir)
  </read_first>
  <action>
    Add a private helper in server.ts: resolveNotesDir(cfg, targetDir?: string): string — if targetDir is a non-empty string: const valid = validateChosenFolder(targetDir); if (!valid) throw a 400-marker error (e.g. a typed Error the handlers map to res 400 {error:'Invalid target folder'}); const dir = join(valid, 'notes'); ensureNotesDir(dir); return dir. If targetDir is absent/empty: return cfg.notesDir (UNCHANGED back-compat path).
    Wire targetDir into EVERY annotation endpoint:
      - POST /annotation: read optional payload.targetDir (string) from the parsed JSON body; notesDir = resolveNotesDir(cfg, payload.targetDir); use it for getNextSerial + writeNote. (Do NOT persist targetDir into the note frontmatter.)
      - GET /annotations: read targetDir from the query string (?targetDir=...); notesDir = resolveNotesDir; listAnnotations(notesDir, url).
      - PUT/DELETE /annotation/:serial: read targetDir from query; resolveNotesDir; edit/deleteNote there.
      - GET /screenshot: read targetDir from query; notesDir = resolveNotesDir; resolved = join(notesDir, basename(file)); KEEP the existing isInsideDir(notesDir, resolved) confinement (now against the resolved per-request notesDir).
    Invalid targetDir → respond 400 (not 500, not a write to the wrong place). Absent targetDir → identical behavior to today.
    Security comment: every targetDir is re-validated server-side each request; writes are confined to <validated>/notes; this is the D-04 extension of the Phase 8 "confined writes" invariant from a single --root to any user-chosen validated folder.
    Tests in server.test.ts: (a) POST with valid targetDir (a tmpdir) writes the .md under <tmpdir>/notes and NOT under cfg.notesDir; (b) POST with a system-dir targetDir → 400, no file written; (c) POST with no targetDir → writes under cfg.notesDir (regression guard); (d) GET /annotations?targetDir=<tmpdir> lists from there.
  </action>
  <verify><automated>npx tsc --noEmit -p tsconfig.host.json &amp;&amp; node --test "dist/host/test/server.test.js"</automated></verify>
  <acceptance_criteria>
    - SOURCE: resolveNotesDir validates+confines; all five endpoints honor optional targetDir; absent → cfg.notesDir.
    - BEHAVIOR: valid targetDir writes there; system/invalid targetDir → 400 (no write); no targetDir → cfg.notesDir (no regression).
    - SECURITY: writes confined to <validated targetDir>/notes; screenshot path still isInsideDir-guarded against the resolved dir.
  </acceptance_criteria>
  <done>The host can write/read/edit/delete/serve notes for any validated per-request folder, defaulting to --root when none is given.</done>
</task>

<task type="auto">
  <name>Task 3: SW — thread origin→folder through the relay (precedence + targetDir)</name>
  <files>entrypoints/background.ts</files>
  <read_first>
    - entrypoints/background.ts (handleSendAnnotation, handleList/Edit/Delete/Screenshot, handlePickFolder, resolveRoute usage, sfxOriginMap)
    - lib/routing.ts (resolveRoute — existing origin→host)
  </read_first>
  <action>
    Add a helper getActivePairedHost(registry, tokens): HostEntry|null — returns a paired host (one whose token is set; for v1 there is a single paired host). 
    In handleSendAnnotation (and the list/edit/delete/screenshot handlers): after deriving origin from chrome.tabs.get (DO NOT change origin derivation), read sfxOriginMap. Resolution precedence:
      1. If originMap[origin] is a FOLDER path (the 09-04 origin→folder entries): host = getActivePairedHost(); if no paired host → {ok:false,error:'Pair with the host first'}; set targetDir = that folder; send to host:port with X-Stikfix-Token AND include targetDir (in the POST body for /annotation; as ?targetDir= query for GET/PUT/DELETE/screenshot).
      2. Else if resolveRoute(origin) finds an origin→host mapping (existing Phase 3 behavior): use it with NO targetDir (writes to host default notesDir) — UNCHANGED back-compat.
      3. Else: return {ok:false, reason:'needs-folder', origin} (structured discriminator like the existing 'unmapped' reason).
    NOTE: sfxOriginMap currently stores BOTH origin→host-name (Phase 3) and origin→folder (09-04) — disambiguate by value: a folder is an absolute path (starts with '/' or matches /^[A-Za-z]:\\/), a host name is a bare registry key. Implement an isFolderValue(v) check. Do not break the existing origin→host entries.
    Keep every async case returning true synchronously. Do NOT weaken token attachment or origin-from-tab.
  </action>
  <verify><automated>npx tsc --noEmit &amp;&amp; npm run build</automated></verify>
  <acceptance_criteria>
    - SOURCE: precedence origin→folder ▸ origin→host ▸ needs-folder; targetDir sent only for the folder case; origin-from-tab + token intact.
    - SOURCE: list/edit/delete/screenshot relays pass ?targetDir= when the origin maps to a folder.
    - BEHAVIOR: npm run build green; existing origin→host path untouched.
  </acceptance_criteria>
  <done>The SW routes folder-mapped origins to the paired host with the chosen folder, falls back to existing host routing, and signals needs-folder otherwise.</done>
</task>

<task type="auto">
  <name>Task 4: Content script — needs-folder → dialog → retry, toast on cancel</name>
  <files>entrypoints/review.content/chip.ts, lib/types.ts</files>
  <read_first>
    - entrypoints/review.content/chip.ts (the SEND_ANNOTATION call site + existing toast/error surface + how it gets its tabId via SFX_GET_TAB_ID)
    - entrypoints/background.ts (SFX_MSG.PICK_FOLDER handler returns {ok,folder} — already built)
    - lib/types.ts (SFX_MSG, MsgPickFolder, response discriminators)
  </read_first>
  <action>
    In chip.ts, where the Send result is handled: if the response is {ok:false, reason:'needs-folder'}: send SFX_MSG.PICK_FOLDER {tabId} to the SW (await). On {ok:true,folder}: show a brief toast ("Saving notes to <folder>") and AUTO-RETRY the original SEND_ANNOTATION once; on success show the normal "sent ✓". On PICK_FOLDER {ok:false} (user cancelled / invalid): show a VISIBLE toast "No folder chosen — note not saved. Drop again to pick one." and DO NOT silently drop (Reliability constraint). Only one auto-retry (avoid loops). Reuse the chip's existing toast mechanism; do not invent a new one. If lib/types.ts lacks the needs-folder reason or a PickFolder response type, add them (additive, no breaking changes).
  </action>
  <verify><automated>npx tsc --noEmit &amp;&amp; npm run build</automated></verify>
  <acceptance_criteria>
    - SOURCE: chip.ts handles reason:'needs-folder' by triggering PICK_FOLDER then retrying once; cancel path shows a visible toast (no silent drop).
    - BEHAVIOR: npm run build green; no change to the happy-path send when an origin is already mapped.
  </acceptance_criteria>
  <done>First note on a new origin prompts a folder, saves there on success, and never vanishes on cancel.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Live UAT — first-note folder dialog end-to-end</name>
  <how-to-verify>
    1. Rebuild + reload the extension; ensure the paired host is running.
    2. On a BRAND-NEW origin (one with no existing mapping), drop the first note + Send → OS folder dialog appears → pick a folder (e.g. a fresh temp dir) → confirm the note .md lands in <that folder>/notes.
    3. Drop a SECOND note on the same origin → NO dialog, note lands in the same folder silently.
    4. Drop a note on a new origin and CANCEL the dialog → a visible toast says the note was not saved (nothing silently lost).
    5. Regression: an origin already mapped to the host still writes to --root with no dialog.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step.</resume-signal>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-09-14b | Tampering | mitigate | Host re-validates every targetDir (validate-folder.ts) and confines writes to <targetDir>/notes; invalid/system dir → 400, never a write outside |
| T-09-15 | Spoofing | mitigate | origin derived from chrome.tabs.get in the SW, never the message body |
| T-08-tokengate | Info disclosure | mitigate (regression guard) | targetDir requests still require X-Stikfix-Token; no new unauthenticated surface |
| REL-01 | Reliability | mitigate | Cancelled dialog → visible toast, never a silent drop |
</threat_model>

<success_criteria>
- ONB-04 / D-04: first note on an unmapped origin → folder dialog → note in chosen folder; reused silently; cancel surfaces a toast.
- No regression: --root default path and existing origin→host routing unchanged; token gate + origin-from-tab intact.
</success_criteria>

<output>
Create 09-05-SUMMARY.md when the auto tasks are done.
</output>
