---
phase: 09-turnkey-onboarding-cross-browser-distribution
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - host/src/native-host.ts
  - entrypoints/background.ts
  - lib/types.ts
  - host/test/security-pairing.test.ts
  - host/test/native-host.test.ts
autonomous: false
requirements: [ONB-03, ONB-04]
must_haves:
  truths:
    - "On a first note from an unmapped origin, the native host opens an OS folder dialog (execFile, arg-array, no shell) and returns the chosen folder"
    - "The SW persists the resulting origin→folder mapping in sfxOriginMap and reuses it silently thereafter (mirrors the Phase 3 origin→host one-time prompt)"
    - "The chosen folder path is validated before use (path confinement)"
    - "An automated security proof shows: native-messaging allowed_origins rejects any origin other than the pinned extension ID; the HTTP host exposes NO /token or /pair endpoint and still rejects wrong/absent tokens (SC-3 / ONB-03 — Phase 8 invariants intact)"
  artifacts:
    - path: "host/src/native-host.ts"
      provides: "PICK_FOLDER branch wired to pickFolder + path validation"
    - path: "entrypoints/background.ts"
      provides: "handlePickFolder + SFX_MSG.PICK_FOLDER case + sfxOriginMap persist"
    - path: "host/test/security-pairing.test.ts"
      provides: "SC-3 hostile-origin proof: allowed_origins gate + no /token|/pair endpoint + token-gate regression"
  key_links:
    - from: "entrypoints/background.ts handlePickFolder"
      to: "com.stikfix.host (PICK_FOLDER)"
      via: "chrome.runtime.sendNativeMessage"
      pattern: "PICK_FOLDER"
    - from: "entrypoints/background.ts"
      to: "sfxOriginMap"
      via: "persist origin→folder after FOLDER_PICKED"
      pattern: "sfxOriginMap"
    - from: "host/src/native-host.ts"
      to: "host/src/folder-picker.ts pickFolder"
      via: "import + await on PICK_FOLDER"
      pattern: "pickFolder"
---

<objective>
Complete the turnkey loop with the D-04 zero-standing-config folder mapping and lock in the ONB-03 security proof. On the first note from an unmapped origin, the native host opens an OS folder dialog; the chosen folder becomes the persisted origin→folder mapping and is reused silently — mirroring the Phase 3 one-time origin→host prompt. A dedicated security test proves a scripted web origin can neither reach the native channel (structural) nor extract the token over HTTP (no /token, no /pair, token gate intact).

Purpose: D-04 removes the last manual-config step (no `--root` needed). The SC-3 proof is the explicit, testable security task the threat model requires before this phase can pass ASVS L1.
Output: native-host PICK_FOLDER wiring + SW handlePickFolder + origin→folder persistence + the SC-3 security test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-CONTEXT.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-RESEARCH.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-PATTERNS.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-VALIDATION.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-02-SUMMARY.md

<interfaces>
<!-- From Plan 01/02 — use directly. -->
From host/src/folder-picker.ts (Plan 01): pickFolder(title): Promise<string|null>; buildPickerArgs(plat,title)
From host/src/security.ts: isInsideDir(root, target): boolean
From host/src/native-msg.ts (Plan 01): sendNativeMessage, readNativeMessages
From host/src/native-host.ts (Plan 02): existing GET_TOKEN dispatch; PICK_FOLDER branch to be completed here
From lib/storage.ts: sfxOriginMap (origin→X persistence; Phase 3)
From lib/routing.ts: resolveRoute / reconcileRegistry (origin→host one-time-prompt precedent to mirror for origin→folder)
From entrypoints/background.ts: onMessage switch (case→handler().then(sendResponse).catch→return true); handlePairNative (Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: native-host.ts PICK_FOLDER branch + path validation</name>
  <files>host/src/native-host.ts, host/test/native-host.test.ts</files>
  <read_first>
    - host/src/native-host.ts (Plan 02 — existing readNativeMessages dispatch with GET_TOKEN; add/complete the PICK_FOLDER branch)
    - .planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-PATTERNS.md ("Path-traversal guard" shared pattern — apply to validate the folder-picker result before use; native-host.ts message dispatch — PICK_FOLDER branch calling pickFolder then sendNativeMessage FOLDER_PICKED then exit; Pitfall 8 — PICK_FOLDER is a SEPARATE native message from GET_TOKEN so the token response is never blocked by the dialog)
    - host/src/folder-picker.ts (Plan 01 — pickFolder; import, do not reimplement)
    - host/src/security.ts (isInsideDir — reference for the confinement helper shape; note it does NOT apply here since the chosen folder is a NEW root, not a child of a pre-existing root — see action)
    - .planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-RESEARCH.md (Pattern 5 folder picker; Pitfall 8 dialog blocking; Native Host Entry Point example PICK_FOLDER branch)
  </read_first>
  <action>
    In host/src/native-host.ts, complete the `msg.type === 'PICK_FOLDER'` branch: await pickFolder('Choose a folder for ' + (msg.origin ?? 'this site')). The chosen folder becomes a NEW note root — it is NOT a child of any pre-existing root, so the traditional `isInsideDir` confinement guard (which asserts a target is inside an established root) does NOT apply here; state this explicitly in a code comment so a future reader does not "fix" it by adding an isInsideDir call against the wrong base. Instead, validate the returned path defensively before responding: (1) it is absolute (path.isAbsolute); (2) it exists (existsSync); (3) it is a directory (statSync(folder).isDirectory()); (4) it is NOT a sensitive system directory — reject `/`, `/System`, `/usr`, `/etc`, and on win32 `C:\Windows` and `C:\Program Files` (compare against a resolved, normalized, case-insensitive-on-win32 deny-list of absolute paths; reject if the chosen folder equals or is one of these roots). If any check fails (or the user cancelled), respond with folder=null (no silent drop, no crash). Respond sendNativeMessage({ type:'FOLDER_PICKED', origin: msg.origin, folder }) (folder=the validated absolute dir, or null), then process.exit(0). PICK_FOLDER is handled per-spawn separately from GET_TOKEN (Pitfall 8 — Chrome spawns a fresh process per sendNativeMessage, so the dialog never blocks a token fetch). Extend host/test/native-host.test.ts: inject/stub pickFolder and assert the PICK_FOLDER dispatch sends a FOLDER_PICKED frame with the origin echoed and folder=null when pickFolder resolves null, folder=<dir> for a valid chosen directory, AND folder=null when pickFolder resolves a system directory (e.g. `C:\Windows` on win32 or `/etc` otherwise) — proving the system-directory rejection.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.host.json &amp;&amp; node --test "dist/host/test/native-host.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: native-host.ts PICK_FOLDER branch imports pickFolder from folder-picker.js and responds with a FOLDER_PICKED frame echoing origin, then exits (Pitfall 8 / ONB-04).
    - SOURCE: the chosen folder is validated as absolute + existing + a directory (statSync isDirectory) before being returned, and a code comment states the chosen folder is a NEW root so isInsideDir confinement against a pre-existing root does NOT apply (path-traversal guard pattern reference).
    - SOURCE: the validation rejects sensitive system directories (`/`, `/System`, `/usr`, `/etc`, and on win32 `C:\Windows`, `C:\Program Files`) by responding folder:null.
    - SOURCE: a cancelled/invalid pick responds folder:null (no crash, no silent drop).
    - BEHAVIOR: native-host.test.ts asserts the FOLDER_PICKED response shape for a chosen folder, a null cancel, AND a rejected system-directory path resolving to folder:null (ONB-04 / 09-VALIDATION ONB-04 row).
  </acceptance_criteria>
  <done>The native host answers PICK_FOLDER with a validated FOLDER_PICKED frame (origin echoed, an absolute existing non-system directory or null) and exits; system directories are rejected.</done>
</task>

<task type="auto">
  <name>Task 2: SW handlePickFolder + PICK_FOLDER case + origin→folder persistence</name>
  <files>entrypoints/background.ts, lib/types.ts</files>
  <read_first>
    - .planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-PATTERNS.md (background.ts handlePickFolder shape — sendNativeMessage PICK_FOLDER + lastError guard + FOLDER_PICKED check; sfxOriginMap persist pattern lines 224-228/399-404; async case + return true)
    - entrypoints/background.ts (Plan 02 handlePairNative as the sibling shape; onMessage switch; existing origin→host resolveRoute usage — DO NOT alter origin-from-tab derivation)
    - lib/routing.ts (resolveRoute / reconcileRegistry — the origin→host one-time-prompt precedent the origin→folder map mirrors)
    - lib/storage.ts (sfxOriginMap — the storage item to extend for origin→folder)
    - lib/types.ts (SFX_MSG — add PICK_FOLDER)
  </read_first>
  <action>
    In lib/types.ts add `PICK_FOLDER: 'PICK_FOLDER'` to SFX_MSG. In entrypoints/background.ts add `handlePickFolder(origin: string): Promise<{ok:true; folder:string} | {ok:false; error:string}>` mirroring the PATTERNS handlePickFolder: wrap chrome.runtime.sendNativeMessage('com.stikfix.host',{type:'PICK_FOLDER',origin}, cb); on chrome.runtime.lastError or non-FOLDER_PICKED or empty folder resolve {ok:false,error:'No folder selected'}; on success re-read sfxOriginMap (Pitfall 1), set originMap[origin]=folder, write back (mirror the existing origin→host persist), resolve {ok:true,folder}. Add `case SFX_MSG.PICK_FOLDER:` to the onMessage switch with the mandatory `handlePickFolder(msg.origin).then(sendResponse).catch(err=>sendResponse({ok:false,error:String(err)})); return true;`. The origin passed MUST come from the SW's existing origin-from-tab derivation (chrome.tabs.get) — never from the message body (Phase 3/8 anti-spoof invariant). Do NOT modify handleSendAnnotation or resolveRoute.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: background.ts handlePickFolder calls sendNativeMessage('com.stikfix.host',{type:'PICK_FOLDER',origin}) and persists origin→folder to sfxOriginMap (ONB-04 / D-04).
    - SOURCE: the origin argument is derived from chrome.tabs.get (origin-from-tab), not the message body (REGRESSION — anti-spoof invariant; grep that no new code reads origin from the inbound message payload for routing).
    - SOURCE: a SFX_MSG.PICK_FOLDER case exists and returns true synchronously (MV3 async invariant).
    - BEHAVIOR: `npm run build` green; handleSendAnnotation untouched (REGRESSION guard).
  </acceptance_criteria>
  <done>The SW requests a folder over the native channel and persists the origin→folder mapping for silent reuse, with origin-from-tab and the send path intact.</done>
</task>

<task type="auto">
  <name>Task 3: SC-3 security proof — hostile-origin + no token endpoint + token-gate regression</name>
  <files>host/test/security-pairing.test.ts</files>
  <read_first>
    - .planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-RESEARCH.md ("SC-3: Security Proof" — the structural argument + the verification checklist: GET /token 404, GET /status omits token, no native API in content scripts; "Hostile-origin security test" Wave 0 item)
    - .planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-VALIDATION.md (Wave 0 "Hostile-origin security test" + ONB-03 row)
    - host/test/index.test.ts (analog: spin up the HTTP host on an ephemeral port via bindServer, http.request assertions, before/after teardown)
    - host/src/server.ts (handleStatus omits cfg.token; route table — confirm there is no /token or /pair route; checkToken 401 path)
    - host/src/security.ts (checkToken — timing-safe; the gate the test re-asserts)
    - host/src/bootstrap/register.ts (Plan 01 buildManifest — allowed_origins gate the test asserts: only the pinned extension ID is accepted)
  </read_first>
  <action>
    Create host/test/security-pairing.test.ts (index.test.ts harness — boot createHostServer on an ephemeral port). Assert the SC-3 proof points:
    (1) GET /token → 404 and GET /pair → 404 (no such routes exist — ONB-03 structural: token is never on an HTTP surface).
    (2) GET /status returns 200 and the JSON body does NOT contain the token value (handleStatus omits cfg.token).
    (3) POST /annotation with NO X-Stikfix-Token → 401; with a WRONG token → 401 (Phase 8 token-gate regression guard — proves pairing did not weaken auth).
    (4) Native-messaging allowed_origins gate (pure, no Chrome): buildManifest(<valid id>,...).allowed_origins === ['chrome-extension://<id>/'] and buildManifest rejects any other/malformed id — i.e. only the pinned extension ID would be authorized by Chrome; a hostile chrome-extension://<other-id>/ is not in allowed_origins (structural rejection).
    (5) Source-grep assertion (read the files via node:fs in the test): assert `connectNative`/`sendNativeMessage` do NOT appear under entrypoints/review.content/ (content scripts cannot reach the native channel — ONB-03 checklist item).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.host.json &amp;&amp; node --test "dist/host/test/security-pairing.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: GET /token and GET /pair return 404; GET /status body has no token; POST /annotation without/with-wrong token returns 401 (SC-3 / ONB-03 / Phase 8 regression).
    - BEHAVIOR: buildManifest pins allowed_origins to exactly the supplied extension ID and rejects other/malformed IDs (ONB-03 structural gate).
    - SOURCE: the test's fs-grep confirms no native-messaging API under entrypoints/review.content/ (ONB-03 checklist).
    - TEST: `node --test dist/host/test/security-pairing.test.js` exits 0.
  </acceptance_criteria>
  <done>An automated SC-3 proof shows the token is unreachable over HTTP and over native messaging from any non-pinned origin, with the Phase 8 token gate intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Live UAT — first-note folder dialog + hostile-origin probe</name>
  <what-built>D-04 zero-config mapping: the first note on an unmapped origin opens an OS folder dialog; the chosen folder is remembered and reused. Plus a live confirmation that a hostile web page cannot pair or write.</what-built>
  <how-to-verify>
    1. `npm run build` and start the paired setup from Plan 02 (extension loaded, native host registered).
    2. On an origin with NO existing mapping, drop the first note and Send. Confirm an OS "choose folder" dialog appears (Windows: FolderBrowserDialog). Pick a folder. Confirm the note `.md` lands in that folder's notes/ (ONB-04 / D-04).
    3. Drop a SECOND note on the same origin. Confirm NO dialog appears — the mapping is reused silently (D-04 one-time prompt).
    4. Hostile-origin probe (ONB-03): on a normal web page, open DevTools console and run `chrome.runtime.connectNative` / `chrome.runtime.sendNativeMessage` — confirm they are undefined (not available to page context). Then `fetch('http://127.0.0.1:<port>/token')` and `/pair` — confirm 404, and `/annotation` without a token — confirm 401. The token is never obtainable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step.</resume-signal>
  <acceptance_criteria>
    - MANUAL: first note → folder dialog → note on disk in the chosen folder; second note → silent reuse (ONB-04/D-04).
    - MANUAL: page context cannot call the native API and cannot extract the token over HTTP (ONB-03).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web origin → native channel | A hostile page attempting to invoke connectNative / pair |
| web origin → HTTP host | A hostile page attempting to read the token via an HTTP endpoint |
| folder-picker result → host | An externally-chosen folder path crosses into the note-write root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Spoofing | hostile origin → native messaging | mitigate | allowed_origins pinned to the exact extension ID (buildManifest); native API unavailable in page/content-script context — proven by security-pairing.test.ts (#4,#5) (ONB-03) |
| T-09-13 | Information Disclosure | token over HTTP | mitigate | No /token or /pair route; /status omits the token; /annotation token-gated — proven by security-pairing.test.ts (#1,#2,#3) (SC-3) |
| T-09-14 | Tampering | folder-picker path | mitigate | execFile arg-array no-shell (Plan 01); chosen folder validated (absolute + existsSync + isDirectory + system-dir deny-list) before becoming a note root; the folder dialog title carries no user-controlled interpolation |
| T-09-15 | Tampering | origin spoofing for routing | mitigate | origin derived from chrome.tabs.get (origin-from-tab), never from the message body (Phase 3/8 invariant preserved) |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (RESEARCH: Phase 9 adds none); builtins + existing deps only |
</threat_model>

<verification>
- `npm test` green incl. security-pairing.test.js and native-host.test.js.
- `npm run build` green.
- origin→folder persisted in sfxOriginMap; origin-from-tab + send path untouched.
- SC-3 proof: no /token|/pair, /status token-free, 401 gate intact, allowed_origins pinned, no native API in content scripts.
- Live UAT confirms the folder dialog + silent reuse + hostile-origin failure.
</verification>

<success_criteria>
- ONB-04 / D-04: first note on an unmapped origin opens a folder dialog; mapping persisted + reused silently.
- ONB-03 / SC-3: a scripted web origin can neither reach the native channel nor obtain the token over HTTP; Phase 8 token gate intact.
- No regression to origin-from-tab routing or the note-write path.
</success_criteria>

<output>
Create `.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-04-SUMMARY.md` when done.
</output>
</content>
