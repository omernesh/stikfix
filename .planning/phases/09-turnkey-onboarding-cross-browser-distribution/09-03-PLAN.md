---
phase: 09-turnkey-onboarding-cross-browser-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cross-browser.md
  - scripts/cross-browser-doc-check.mjs
  - README.md
autonomous: true
requirements: [ONB-06]
must_haves:
  truths:
    - "docs/cross-browser.md documents Edge as a supported Chromium drop-in (no build needed)"
    - "docs/cross-browser.md documents the Firefox packaging path (allowed_extensions, manifest locations, browser_specific_settings.gecko.id) as docs-only / FUT-01"
    - "docs/cross-browser.md documents the Safari packaging path (app-bundled, safari-web-extension-converter, App Store) as docs-only / FUT-01"
    - "An automated doc-assertion check confirms the required sections exist"
    - "README links to docs/cross-browser.md"
  artifacts:
    - path: "docs/cross-browser.md"
      provides: "Edge/Firefox/Safari packaging documentation (D-05)"
      min_lines: 60
      contains: "allowed_extensions"
    - path: "scripts/cross-browser-doc-check.mjs"
      provides: "section-presence assertion for the doc"
  key_links:
    - from: "README.md"
      to: "docs/cross-browser.md"
      via: "markdown link"
      pattern: "cross-browser.md"
---

<objective>
Satisfy ONB-06 (D-05): ship a documented cross-browser packaging path covering Edge (supported drop-in now), Firefox, and Safari (documented-only, FUT-01) — with an automated section-presence check so the doc requirement has a real verify gate, not just a manual eyeball.

Purpose: ONB-06 is explicitly a documentation deliverable (no FF/Safari build in v1.0). This plan is fully independent of the pairing slices (no shared files), so it runs in Wave 1 in parallel with Plan 02.
Output: docs/cross-browser.md + a node doc-check script + a README link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-CONTEXT.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-RESEARCH.md
@.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/cross-browser.md — Edge / Firefox / Safari packaging paths</name>
  <files>docs/cross-browser.md</files>
  <read_first>
    - .planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-RESEARCH.md ("Cross-Browser Documentation (D-05 / ONB-06)" — full section: Edge registry paths + Chrome fallback; Firefox allowed_extensions + manifest locations + gecko.id; Safari app-bundle + safari-web-extension-converter + App Store)
    - .planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-CONTEXT.md (D-05; Deferred Ideas — FF/Safari builds are FUT-01 v2, docs only)
    - CLEAN-ROOM.md (tone/structure of existing docs for consistency)
  </read_first>
  <action>
    Create docs/cross-browser.md with three top-level sections matching RESEARCH "Cross-Browser Documentation": (1) "## Microsoft Edge — supported now" — Edge is a Chromium drop-in; the bootstrapper writes BOTH the Chrome and Edge HKCU registry keys on Windows; list the Edge macOS/Linux manifest paths; note the dual-store extension-ID caveat (both IDs in allowed_origins if published to both stores). (2) "## Firefox (documented path — FUT-01, not built in v1.0)" — Firefox uses `allowed_extensions` (addon IDs) NOT `allowed_origins`; the manifests are not interchangeable; list the FF manifest locations (macOS/Linux/Windows HKCU); note `browser_specific_settings.gecko.id` requirement; include the FF-specific manifest JSON example from RESEARCH. (3) "## Safari (documented path — FUT-01, not built in v1.0)" — fundamentally different architecture (app-bundled, no standalone CRX/XPI); the "native host" equivalent is the containing macOS app; `xcrun safari-web-extension-converter` as a starting point; requires Xcode + Apple Developer Program + App Store review. Add a header note that Edge is the only one supported/tested in v1.0; FF/Safari are documented paths tracked as FUT-01. Original prose only (clean-room — do not copy upstream/GPL text).
  </action>
  <verify>
    <automated>node scripts/cross-browser-doc-check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: docs/cross-browser.md exists with the three section headings (Edge / Firefox / Safari).
    - SOURCE: the doc contains `allowed_extensions` (Firefox key), `safari-web-extension-converter` (Safari path), and the Edge registry path `Microsoft\Edge\NativeMessagingHosts` (ONB-06 / 09-VALIDATION ONB-06 doc-assertion row).
    - SOURCE: the doc states FF/Safari are FUT-01 documented-only (no v1.0 build) — matches D-05.
  </acceptance_criteria>
  <done>docs/cross-browser.md documents the Edge/Firefox/Safari packaging paths per D-05/RESEARCH, clean-room original.</done>
</task>

<task type="auto">
  <name>Task 2: doc-assertion check script + README link</name>
  <files>scripts/cross-browser-doc-check.mjs, README.md</files>
  <read_first>
    - scripts/clean-room-check.mjs (analog: a node:fs-based assertion script that exits 1 with a message on failure — mirror its structure/exit convention)
    - scripts/host-smoke-test.mjs (analog: exit-code + console.error convention used by `npm run check`)
    - README.md (find the install/usage area to add the cross-browser link; do not restructure existing content)
    - package.json (the `check` script chain — note whether to append this check; keep it cheap)
  </read_first>
  <action>
    Create scripts/cross-browser-doc-check.mjs (ESM, node:fs only): read docs/cross-browser.md; assert it exists and contains the required tokens — the three section headings (Edge/Firefox/Safari), `allowed_extensions`, `safari-web-extension-converter`, `Microsoft\\Edge\\NativeMessagingHosts`; on any missing token console.error a specific message and process.exit(1); else log OK and exit 0. Mirror clean-room-check.mjs structure/exit convention. Add a single markdown link to docs/cross-browser.md in README.md (e.g. under install/usage: "Edge/Firefox/Safari: see docs/cross-browser.md") — additive only, no restructuring (REGRESSION: existing README content preserved per DOC-01/DOC-02).
  </action>
  <verify>
    <automated>node scripts/cross-browser-doc-check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: scripts/cross-browser-doc-check.mjs exits 0 when the doc has all required sections and exits 1 (with a descriptive message) when a section is missing.
    - SOURCE: README.md contains a link to docs/cross-browser.md (`grep -c "cross-browser.md" README.md` returns >=1).
    - BEHAVIOR: existing README install/usage content + demo GIF reference unchanged (REGRESSION guard on DOC-01/DOC-02).
  </acceptance_criteria>
  <done>An automated check gates the cross-browser doc's required sections and the README links to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation only) | This plan adds Markdown + a read-only assertion script; no runtime trust boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Information Disclosure | cross-browser doc content | accept | Doc contains only public packaging guidance (registry paths, public API names); no secrets |
| T-09-11 | Tampering | clean-room provenance | mitigate | Doc prose is original (clean-room §13); no GPL/upstream text copied — covered by the existing clean-room grep gate in `npm run check` |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; doc + node:fs script only (RESEARCH: Phase 9 adds no packages) |
</threat_model>

<verification>
- `node scripts/cross-browser-doc-check.mjs` exits 0.
- README links to docs/cross-browser.md.
- Existing clean-room grep gate stays green (no GPL text introduced).
</verification>

<success_criteria>
- ONB-06: a documented Edge (supported) + Firefox/Safari (FUT-01 docs-only) packaging path exists and is automatically section-checked.
</success_criteria>

<output>
Create `.planning/phases/09-turnkey-onboarding-cross-browser-distribution/09-03-SUMMARY.md` when done.
</output>
