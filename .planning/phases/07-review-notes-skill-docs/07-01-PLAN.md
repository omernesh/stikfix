---
phase: 07-review-notes-skill-docs
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - lib/review-notes.ts
  - lib/test/review-notes.test.ts
  - tsconfig.lib.json
  - package.json
autonomous: true
requirements: [SKILL-02, SKILL-04, SKILL-05]
must_haves:
  truths:
    - "Globbing a notes/ listing returns only unread .md files (excludes *.read.md) in ascending serial order"
    - "Computing a read-marker name turns 0001-ts.md into 0001-ts.read.md"
    - "An empty unread queue is reported cleanly (zero results)"
    - "An ambiguous note classifies as 'flagged'; a note whose screenshot file is absent classifies as 'text-only' not 'flagged'"
  artifacts:
    - path: "lib/review-notes.ts"
      provides: "Pure testable backbone for the skill prose: selectUnread, markReadName, classifyNote"
      exports: ["selectUnread", "markReadName", "classifyNote"]
      min_lines: 40
    - path: "lib/test/review-notes.test.ts"
      provides: "node:test coverage for SKILL-02/04/05 processing contract"
      contains: "describe('selectUnread'"
  key_links:
    - from: "package.json test:lib script"
      to: "dist/lib/lib/test/review-notes.test.js"
      via: "node --test argument list"
      pattern: "review-notes\\.test\\.js"
    - from: "tsconfig.lib.json include"
      to: "lib/review-notes.ts"
      via: "include array entry"
      pattern: "lib/review-notes\\.ts"
---

<objective>
Extract the skill's processing contract into pure, unit-tested TypeScript helpers so SKILL-02 (glob/sort/exclude), SKILL-04 (read-marker + idempotency), and SKILL-05 (empty/ambiguous/missing-screenshot classification) have automated coverage. The prose skill (Plan 02) is then a faithful mirror of code that is proven correct.

Purpose: The skill itself is markdown prose an agent follows — but its core decisions (which files are unread, what the read name is, how to classify a note) are deterministic and must not regress. This plan gives that logic an automated backbone.
Output: lib/review-notes.ts (3 exported pure functions) + lib/test/review-notes.test.ts (node:test) wired into tsconfig.lib.json + the test:lib npm script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-review-notes-skill-docs/07-RESEARCH.md
@.planning/phases/07-review-notes-skill-docs/07-PATTERNS.md

<interfaces>
<!-- The host's read-side contract this plan mirrors. Extracted from host/src/read-note.ts. -->
<!-- Host listing glob (INCLUDES *.read.md): readdirSync(notesDir).filter(f => f.endsWith('.md')) -->
<!-- Skill must EXCLUDE *.read.md — this is the deliberate divergence (RESEARCH Pitfall 1). -->
<!-- Serial extraction (NEVER from fm.id): file.slice(0, 4) — see read-note.ts:77 -->
<!-- Read-marker rename direction: mdPath.replace(/\.md$/, '.read.md') -->

PinDescriptor-relevant frontmatter keys (from host/src/write-note.ts buildFrontmatter):
  id:int, created:ISO, mode:'free'|'element', url:string, title:string,
  viewport:{width,height,dpr}, selector?(element), react_component?(element),
  rect?(element), note_position?(free), screenshots:string[], status:'unread'|'read'|'flagged'

node:test import + tmpdir lifecycle pattern (from lib/test/pin-position.test.ts + host/test/read-note.test.ts):
  import { test, describe } from 'node:test';
  import assert from 'node:assert/strict';
  import { selectUnread, markReadName, classifyNote } from '../review-notes.js';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test selectUnread + markReadName (SKILL-02, SKILL-04)</name>
  <files>lib/review-notes.ts, lib/test/review-notes.test.ts</files>
  <read_first>
    - lib/review-notes.ts (being created — confirm it does not yet exist)
    - lib/test/review-notes.test.ts (being created)
    - host/src/read-note.ts (lines 49-111 — listAnnotations glob `endsWith('.md')` at line 73, serial = file.slice(0,4) at line 77 — the host INCLUDES *.read.md deliberately; the skill must EXCLUDE it)
    - lib/test/pin-position.test.ts (node:test import block + describe/test structure to mirror — pure-function style, no DOM/chrome)
    - .planning/phases/07-review-notes-skill-docs/07-PATTERNS.md ("Glob Exclusion Pattern" + "Serial Extraction from Filename" + "Rename Mechanics" sections)
  </read_first>
  <behavior>
    - selectUnread(['0002-t.md','0001-t.md','0001-t.read.md','x.png','0010-t.md']) → ['0001-t.md','0002-t.md','0010-t.md'] (excludes *.read.md and non-.md; ascending lexicographic order — zero-padded 4-digit prefix guarantees serial order)
    - selectUnread([]) → [] (empty queue, SKILL-05 empty case)
    - selectUnread(['0001-t.read.md','0002-t.read.md']) → [] (all already read — idempotent re-run, SKILL-04)
    - selectUnread does NOT mutate its input array
    - markReadName('0001-20260603-010538.md') → '0001-20260603-010538.read.md' (SKILL-04)
    - markReadName is idempotent-safe: it only appends .read before .md when not already present — markReadName('0001-t.read.md') → '0001-t.read.md' (no double .read.read.md)
  </behavior>
  <action>
    Create lib/review-notes.ts exporting two pure functions with no DOM/chrome/fs surface (operate on string arrays/strings only — fs is the agent's job per the prose skill). selectUnread(files: string[]): string[] filters to names ending in `.md` AND NOT ending in `.read.md`, returns a NEW sorted array (ascending, default string sort — the zero-padded 4-digit serial prefix makes lexicographic === serial order, mirror read-note.ts intent). markReadName(name: string): string returns the name with `.read.md` suffix; if the name already ends in `.read.md` return it unchanged (guard against double-suffix). Do NOT derive serial from any `id` field — serial is always filename.slice(0,4) per RESEARCH Pitfall 3 (document this in a comment but do not expose a serial helper unless a test needs it). Add the two test groups to lib/test/review-notes.test.ts following the pin-position.test.ts import/describe/test shape; import from '../review-notes.js'. Tests are RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>npm run test:lib</automated>
  </verify>
  <done>selectUnread + markReadName exported from lib/review-notes.ts; their test groups pass under `npm run test:lib`; input arrays are not mutated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define and test classifyNote edge classifier (SKILL-05) + wire build</name>
  <files>lib/review-notes.ts, lib/test/review-notes.test.ts, tsconfig.lib.json, package.json</files>
  <read_first>
    - lib/review-notes.ts (created in Task 1 — appending classifyNote)
    - lib/test/review-notes.test.ts (created in Task 1 — appending classifyNote group)
    - tsconfig.lib.json (the include array — add lib/review-notes.ts; lib/test/**/*.ts is already globbed)
    - package.json (the test:lib script line 11 — append dist/lib/lib/test/review-notes.test.js to the node --test argument list, preserving every existing test file)
    - .planning/phases/07-review-notes-skill-docs/07-RESEARCH.md (Idempotency Mechanics section: D-08 ambiguous vs D-09 missing-screenshot distinction — a missing PNG is NOT ambiguous)
  </read_first>
  <behavior>
    - classifyNote({ status:'read' }, anyExisting) → 'read' (already-processed; skill skips)
    - classifyNote({ status:'flagged' }, anyExisting) → 'flagged' (previously flagged; skill skips, stays visible per D-08)
    - classifyNote({ status:'unread', screenshots:['0001-t+1.png'] }, existingFiles=['0001-t+1.png']) → 'fixable' (all referenced screenshots present)
    - classifyNote({ status:'unread', screenshots:['0001-t+1.png'] }, existingFiles=[]) → 'text-only' (referenced screenshot missing → D-09 proceed text-only, NOT flagged)
    - classifyNote({ status:'unread', screenshots:[] }, anyExisting) → 'fixable' (no screenshots referenced is normal, not text-only)
    - classifyNote treats absent/undefined screenshots field same as [] (graceful — UAT notes vary, RESEARCH A1)
  </behavior>
  <action>
    Append classifyNote(fm: { status?: string; screenshots?: string[] }, existingScreenshotNames: string[]): 'read' | 'flagged' | 'fixable' | 'text-only' to lib/review-notes.ts. Logic: if status==='read' return 'read'; if status==='flagged' return 'flagged'; treat screenshots as (fm.screenshots ?? []); if that list is non-empty and ANY referenced name is absent from existingScreenshotNames return 'text-only' (D-09 — missing image is text-only, never flagged); otherwise return 'fixable'. Note in a comment: 'ambiguous' is a RUNTIME judgement the agent makes from instruction clarity (D-08) and is NOT decidable from frontmatter alone — classifyNote only handles the deterministic screenshot/status cases; the prose skill owns the ambiguity call. Add the classifyNote test group covering the six behaviors above. Then wire the build: add `"lib/review-notes.ts"` to the tsconfig.lib.json include array, and append `dist/lib/lib/test/review-notes.test.js` to the existing space-separated file list in the package.json `test:lib` script (do NOT remove any existing entry — regression guard).
  </action>
  <verify>
    <automated>npm run test:lib</automated>
  </verify>
  <done>classifyNote exported and its 6-case test group passes; `npm run test:lib` runs review-notes.test.js alongside all pre-existing lib tests (count increases, none removed); tsconfig.lib.json includes lib/review-notes.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds pure string-processing functions + tests. No I/O, no network, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | lib/review-notes.ts logic regression (skill silently re-processes read notes) | mitigate | selectUnread *.read.md exclusion is unit-tested (idempotency case); a regression turns the test RED before merge |
| T-07-SC | Tampering | npm installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A); nothing to verify |

**Verdict:** Minimal surface — pure functions, no executable runtime change, no network, no auth. No high-severity threats (honest assessment per phase security note).
</threat_model>

<verification>
- `npm run test:lib` green, with review-notes.test.js in the run and all pre-existing lib tests still listed/passing.
- `tsc --noEmit -p tsconfig.lib.json` clean (no type errors in the new exports).
</verification>

<success_criteria>
- selectUnread, markReadName, classifyNote exported from lib/review-notes.ts as pure functions (no fs/DOM/chrome).
- SKILL-02 (glob/exclude/sort), SKILL-04 (read-marker + idempotent empty re-scan), SKILL-05 (empty/missing-screenshot classification) each covered by a passing test case.
- `npm run test:lib` runs the new test file alongside every existing lib test with zero removals.
</success_criteria>

<output>
Create `.planning/phases/07-review-notes-skill-docs/07-01-SUMMARY.md` when done.
</output>
