---
phase: 07-review-notes-skill-docs
plan: 03
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - scripts/clean-room-check.mjs
  - CLEAN-ROOM.md
  - README.md
  - docs/demo-placeholder.png
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "A developer can follow the README quickstart top-to-bottom and reach a note-on-disk in under 5 minutes"
    - "The README documents the 127.0.0.1 + token + origin-trust security model and the review-notes skill install"
    - "CLEAN-ROOM.md states MIT provenance and embeds the live grep audit result (clean-room-check passes)"
    - "node scripts/clean-room-check.mjs exits 0 with CLEAN-ROOM.md present (the doc does not trip its own audit)"
  artifacts:
    - path: "scripts/clean-room-check.mjs"
      provides: "SKIP_FILENAMES extended with CLEAN-ROOM.md (pre-req before writing the provenance doc)"
      contains: "CLEAN-ROOM.md"
    - path: "CLEAN-ROOM.md"
      provides: "MIT provenance + clean-room narrative + live grep audit output (DOC-02)"
      min_lines: 30
    - path: "README.md"
      provides: "Quickstart-first README with skill install, security model, troubleshooting, demo slot (DOC-01)"
      contains: "## Quickstart"
    - path: "docs/demo-placeholder.png"
      provides: "Demo GIF placeholder asset (D-10)"
  key_links:
    - from: "README.md"
      to: "CLEAN-ROOM.md"
      via: "License & provenance section link"
      pattern: "CLEAN-ROOM\\.md"
    - from: "README.md quickstart"
      to: "review-notes skill"
      via: "skill install + trigger-phrase section"
      pattern: "review-notes"
---

<objective>
Deliver the documentation slice: a quickstart-first README that gets a developer to a note-on-disk in under 5 minutes (DOC-01) and a CLEAN-ROOM.md that records MIT provenance plus the live GPL grep audit result (DOC-02). A mandatory pre-req: add CLEAN-ROOM.md to the clean-room-check skip list BEFORE writing it, so the provenance doc (which names the banned identifiers it documents being absent) does not trip its own audit.

Purpose: <5-minutes-to-working is the README's success bar and the project's adoption gate; CLEAN-ROOM.md formalizes the already-true clean-room state (the audit currently passes) and establishes the baseline Phase 8 re-runs as a release gate.
Output: scripts/clean-room-check.mjs (skip-list edit), CLEAN-ROOM.md (new), README.md (rewritten quickstart-first, existing accurate content preserved), docs/demo-placeholder.png.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-review-notes-skill-docs/07-CONTEXT.md
@.planning/phases/07-review-notes-skill-docs/07-RESEARCH.md
@.planning/phases/07-review-notes-skill-docs/07-PATTERNS.md

<interfaces>
<!-- clean-room-check.mjs: SKIP_FILENAMES currently {PRD.md, README.md, CLAUDE.md, LICENSE}. Add 'CLEAN-ROOM.md'. -->
<!-- Banned identifiers (named by DESCRIPTION in CLEAN-ROOM.md, not by literal value): -->
<!--   upstream private-API prefix (__ + opc + _), upstream project name (open+code), upstream author handle (Jodus+Nodus). -->
<!-- Scan exts: .ts .js .mjs .cjs .json .html .css .md. Skipped dirs: node_modules .git .output dist .wxt .planning notes private .claude .qmd-memory -->
<!-- Host start (from package.json): npm run host -- --root <dir> --origin <url>  (host script = node dist/host/src/index.js) -->
<!-- Existing README content to PRESERVE verbatim: product description (lines 1-13), Windows PowerShell variants (lines 22-39), architecture one-liner (lines 45-50), license/clean-room note (lines 52-56). Replace ONLY the "Status: Pre-build" block (lines 41-44). -->
<!-- Extension load path: .output/chrome-mv3/ (chrome://extensions → Load unpacked). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLEAN-ROOM.md to skip-list, then write CLEAN-ROOM.md (DOC-02)</name>
  <files>scripts/clean-room-check.mjs, CLEAN-ROOM.md</files>
  <read_first>
    - scripts/clean-room-check.mjs (the SKIP_FILENAMES Set at lines 49-54 — add 'CLEAN-ROOM.md' alongside the existing four; the BANNED fragment construction at lines 17-24 and the scan scope at 26-44 are what the doc describes)
    - CLEAN-ROOM.md (being created — confirm absent)
    - .planning/phases/07-review-notes-skill-docs/07-RESEARCH.md ("CLEAN-ROOM.md Design" section — the 6-part structure + the captured audit output)
    - .planning/phases/07-review-notes-skill-docs/07-PATTERNS.md ("CLEAN-ROOM.md (NEW)" section — describe banned identifiers BY DESCRIPTION not literal value; the SKIP_FILENAMES pre-req)
    - LICENSE (confirm MIT holder/year for the provenance declaration)
  </read_first>
  <action>
    FIRST edit scripts/clean-room-check.mjs: add the string `'CLEAN-ROOM.md'` to the SKIP_FILENAMES Set (same rationale as README.md/PRD.md — it is an attribution/provenance doc that legitimately names the upstream project for legal notice). Do NOT alter BANNED, SCAN_EXTS, SKIP_DIRS, or the walk logic — only extend SKIP_FILENAMES (regression guard). THEN run the audit to capture its current live output: `node scripts/clean-room-check.mjs` (expect "clean-room audit: PASS — no banned identifiers found", exit 0). THEN write CLEAN-ROOM.md with the D-12 6-part structure: (1) MIT provenance declaration (original work, MIT © the holder/year from LICENSE); (2) clean-room method narrative (studied the upstream GPL project's architecture, wrote original code from spec, zero copy-paste, sfx-* identifier namespace enforced from Phase 1); (3) GPL upstream acknowledgment WITHOUT inclusion — you may name the upstream repo + its GPL-3.0 license for attribution (CLEAN-ROOM.md is now skip-listed so it won't self-trip); (4) the grep audit section: describe the three banned identifier CLASSES by their purpose (upstream private-API prefix, upstream project name, upstream author handle) rather than only the literal strings, name the command `node scripts/clean-room-check.mjs`, and embed the captured PASS output + exit 0; (5) scope of audit (extensions scanned; dirs/files excluded and WHY — gitignored local trees + attribution docs); (6) a note that Phase 8 SC-4 re-runs the same audit (expanded with upstream selector constants) as a release gate. Do NOT fabricate the audit output — use what the live run prints.
  </action>
  <verify>
    <automated>node scripts/clean-room-check.mjs && node -e "const fs=require('fs');const s=fs.readFileSync('scripts/clean-room-check.mjs','utf8');if(!/CLEAN-ROOM\.md/.test(s))throw new Error('CLEAN-ROOM.md not in skip list');const c=fs.readFileSync('CLEAN-ROOM.md','utf8');if(!/MIT/.test(c))throw new Error('missing MIT provenance');if(!/clean-room-check\.mjs/.test(c))throw new Error('missing audit command');if(!/PASS/.test(c))throw new Error('missing audit result');console.log('clean-room doc OK');"</automated>
  </verify>
  <done>scripts/clean-room-check.mjs SKIP_FILENAMES includes 'CLEAN-ROOM.md' (and nothing else changed); `node scripts/clean-room-check.mjs` exits 0 WITH CLEAN-ROOM.md present; CLEAN-ROOM.md has all 6 D-12 sections including the live PASS output.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README quickstart-first + demo placeholder (DOC-01)</name>
  <files>README.md, docs/demo-placeholder.png</files>
  <read_first>
    - README.md (the existing 56-line file — identify the EXACT blocks to preserve verbatim: product description lines 1-13, Windows PowerShell host-start variants lines 22-39, architecture one-liner lines 45-50, license/clean-room note lines 52-56; the ONLY block to replace is "Status: Pre-build" lines 41-44)
    - docs/demo-placeholder.png (being created — confirm absent)
    - CLEAN-ROOM.md (created in Task 1 — README's License & provenance section links to it)
    - skill/SKILL.md (created in Plan 02 — README's skill-install section references the portable file + the .claude/skills wrapper path)
    - .planning/phases/07-review-notes-skill-docs/07-RESEARCH.md ("README Structure" + "5-Step Quickstart" — host command, load-unpacked, token, Review Mode, run skill)
    - .planning/phases/07-review-notes-skill-docs/07-PATTERNS.md ("README.md (MODIFY existing)" section — preserve/replace/add map; Pitfall 6 regression guard)
  </read_first>
  <action>
    Rewrite README.md preserving every accurate existing block VERBATIM (product description, the full Windows PowerShell host-start variants incl. the STIKFIX_ORIGINS comma-separated note, architecture one-liner, license + clean-room paragraph) — Pitfall 6: losing the Windows variants is a regression. Replace ONLY the "Status: 🚧 Pre-build" block with a `## Quickstart (< 5 minutes)` section: 5 copy-pasteable steps — (1) `npm run build` (extension + host); (2) start the host `npm run host -- --root <project-dir> --origin <page-origin>` (cross-reference the existing Windows PowerShell variants section just below); (3) load the unpacked extension — chrome://extensions → Developer Mode → Load unpacked → select `.output/chrome-mv3/`; (4) pair the token — copy the token printed by the host startup output → paste into the extension popup; (5) enter Review Mode on any page, drop a note, then tell your AI agent "read my notes" to run the review-notes skill. Add these sections after the quickstart: `## review-notes Skill` — install (copy skill/SKILL.md into a project's `.claude/skills/review-notes/SKILL.md`, or just point any folder-reading agent at the portable `skill/SKILL.md`), the trigger phrase ("read my notes"), and what it does (processes unread notes in serial order, fixes each, renames to *.read.md, idempotent); `## Security model` — host binds 127.0.0.1 only (not reachable off-box), POST /annotation requires the X-Stikfix-Token, origin-trust mapping persists per origin; `## Troubleshooting` — token mismatch (re-copy from host output), port scan range 39240-39260, Windows npm flag-parsing (use the equals-sign / env-var / node-direct variants); `## Demo` — embed `docs/demo-placeholder.png` and the "how to record" steps (D-10: name two free Windows tools, LICEcap and ScreenToGif, no install mandated; record the 5-step flow, save as docs/demo.gif, replace this placeholder); `## License & provenance` — one line stating MIT + clean-room, linking to CLEAN-ROOM.md (DOC-02). Then create docs/demo-placeholder.png — a minimal valid PNG placeholder (a small image; a 1x1 or simple solid PNG is acceptable since Omer replaces it with the real recording later per D-10). Ensure the markdown image reference path matches the committed file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('README.md','utf8');if(!/## Quickstart/.test(r))throw new Error('missing Quickstart');if(/Status:.*Pre-build/.test(r))throw new Error('Pre-build status block not removed');if(!/npm run host -- --root/.test(r))throw new Error('missing host start command');if(!/PowerShell/.test(r))throw new Error('REGRESSION: Windows PowerShell variants lost');if(!/review-notes/.test(r))throw new Error('missing review-notes skill section');if(!/127\.0\.0\.1/.test(r))throw new Error('missing security model');if(!/CLEAN-ROOM\.md/.test(r))throw new Error('missing provenance link');if(!/demo-placeholder\.png/.test(r))throw new Error('missing demo placeholder ref');if(!fs.existsSync('docs/demo-placeholder.png'))throw new Error('missing placeholder asset');console.log('README OK');"</automated>
  </verify>
  <done>README has a 5-step `## Quickstart` (host/load/token/Review Mode/run skill), a review-notes skill install section, a 127.0.0.1+token+origin security model, troubleshooting, a demo slot referencing docs/demo-placeholder.png + record steps, and a License & provenance link to CLEAN-ROOM.md; the Windows PowerShell host-start variants and other accurate existing content are preserved (no regression); docs/demo-placeholder.png exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → published repo | README + CLEAN-ROOM.md are public, MIT-attribution docs. The risk is mis-stated provenance or leaked secrets, not code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Repudiation | CLEAN-ROOM.md audit result falsified / stale | mitigate | The PASS output is captured from a LIVE `node scripts/clean-room-check.mjs` run in-task (not self-reported); Phase 8 SC-4 re-runs it as a release gate |
| T-07-06 | Information Disclosure | README quickstart leaks the host token or a real secret | mitigate | Quickstart references the token only as "copy from host startup output" — no literal token is embedded; no real credentials in any doc |
| T-07-07 | Tampering | clean-room-check.mjs skip-list edit weakens the audit beyond the doc | mitigate | Only `'CLEAN-ROOM.md'` is added to SKIP_FILENAMES; BANNED/SCAN_EXTS/SKIP_DIRS/walk untouched; the audit still scans all first-party source |

**Verdict:** Low surface — static documentation + a one-line skip-list addition. No high-severity threats.
</threat_model>

<verification>
- `node scripts/clean-room-check.mjs` exits 0 with CLEAN-ROOM.md present (Task 1 gate).
- README grep gate passes: Quickstart present, Pre-build removed, host command present, PowerShell variants preserved (regression guard), skill/security/provenance/demo sections present, placeholder asset exists.
</verification>

<success_criteria>
- DOC-01: README is quickstart-first (<5-min 5-step flow), documents the review-notes skill install + trigger, security model, troubleshooting, and a demo slot with record instructions; accurate existing content (incl. Windows PowerShell variants) preserved.
- DOC-02: CLEAN-ROOM.md states MIT provenance + clean-room narrative + the live grep audit PASS output; clean-room-check.mjs skip-lists CLEAN-ROOM.md so the doc does not self-trip and the audit still exits 0.
</success_criteria>

<output>
Create `.planning/phases/07-review-notes-skill-docs/07-03-SUMMARY.md` when done.
</output>
