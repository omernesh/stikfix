---
phase: 07-review-notes-skill-docs
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - skill/SKILL.md
  - .claude/skills/review-notes/SKILL.md
  - test/fixtures/notes/0001-20260101-120000.md
  - test/fixtures/notes/0001-20260101-120000+1.png
  - test/fixtures/notes/0002-20260101-120001.md
  - test/fixtures/notes/0003-20260101-120002.md
  - test/fixtures/notes/0004-20260101-120003.md
  - test/fixtures/notes/0099-20260101-120099.read.md
  - .planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md
autonomous: false
requirements: [SKILL-01, SKILL-02, SKILL-03, SKILL-04, SKILL-05]
must_haves:
  truths:
    - "Any folder-reading AI agent can read skill/SKILL.md and process unread notes in serial order without Claude-only frontmatter"
    - "The skill processes 0001 before 0002 and renames each handled note to *.read.md (idempotent re-run)"
    - "A clear note is auto-fixed then renamed; an ambiguous note is flagged and left unread; a missing screenshot proceeds text-only"
    - "Claude Code auto-invokes the wrapper, which points at skill/SKILL.md without duplicating logic"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Portable, agent-agnostic source-of-truth skill prose (no frontmatter required)"
      min_lines: 50
      contains: "Element context"
    - path: ".claude/skills/review-notes/SKILL.md"
      provides: "Thin Claude Code wrapper pointing at skill/SKILL.md"
      contains: "skill/SKILL.md"
    - path: "test/fixtures/notes/0099-20260101-120099.read.md"
      provides: "Pre-read fixture proving idempotent exclusion"
      contains: "status: read"
    - path: ".planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md"
      provides: "Runbook for human verification of SKILL-02..05 against fixtures"
  key_links:
    - from: ".claude/skills/review-notes/SKILL.md"
      to: "skill/SKILL.md"
      via: "pointer sentence in wrapper body"
      pattern: "skill/SKILL\\.md"
    - from: "skill/SKILL.md glob step"
      to: "lib/review-notes.ts selectUnread contract"
      via: "explicit exclude *.read.md prose mirrors selectUnread"
      pattern: "\\.read\\.md"
---

<objective>
Ship the portable review-notes skill and its Claude Code wrapper, plus the fixture set and human-UAT runbook that prove the skill's processing contract. The portable skill/SKILL.md is the agent-agnostic source of truth; the .claude/skills wrapper is a thin pointer (D-01). Fixtures give a human (Omer) a concrete way to run the skill and confirm SKILL-02..05 outcomes.

Purpose: This is the phase's core value — a dropped note reliably becomes a fixed-then-renamed .md, killing the screenshot-paste-describe ping-pong. The prose must read like instructions an agent follows autonomously, mirroring the unit-tested helpers from Plan 01.
Output: skill/SKILL.md (portable prose), .claude/skills/review-notes/SKILL.md (wrapper), 6 fixture files under test/fixtures/notes/, and 07-HUMAN-UAT.md runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-review-notes-skill-docs/07-CONTEXT.md
@.planning/phases/07-review-notes-skill-docs/07-RESEARCH.md
@.planning/phases/07-review-notes-skill-docs/07-PATTERNS.md
@.planning/phases/07-review-notes-skill-docs/07-01-SUMMARY.md

<interfaces>
<!-- The note on-disk format the skill consumes. Authoritative producer: host/src/write-note.ts buildFrontmatter/buildNoteBody. -->
<!-- Frontmatter keys: id, created, mode('free'|'element'), url, title, viewport{width,height,dpr}, -->
<!--   selector?(element), react_component?(element), rect?(element), note_position?(free), screenshots:string[], status -->
<!-- Body: <comment> then (element only) "## Element context" + curated computed-styles table + truncated outerHTML, then optional "### Screenshots". -->
<!-- Serial: filename.slice(0,4) — NEVER fm.id (loses zero-padding). -->
<!-- Glob: include *.md, EXCLUDE *.read.md, sort ascending (mirror lib/review-notes.ts selectUnread). -->
<!-- Read-marker: rename .md→.read.md AND set status:read (D-07). Both work (read-note.ts:73 listAnnotations still lists .read.md). -->
<!-- Wrapper analog frontmatter: .claude/skills/claudios.md (name + description block). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the portable skill/SKILL.md (SKILL-01..05) + Claude Code wrapper</name>
  <files>skill/SKILL.md, .claude/skills/review-notes/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (being created — confirm absent)
    - .claude/skills/review-notes/SKILL.md (being created — confirm absent)
    - .claude/skills/claudios.md (the in-repo wrapper frontmatter analog: name + description block shape)
    - host/src/write-note.ts (buildFrontmatter + buildNoteBody — the authoritative note format the prose must describe reading: all frontmatter keys, element vs free body structure, ### Screenshots block)
    - host/src/read-note.ts (lines 73-111 — confirms listAnnotations lists *.read.md so D-07 rename+status are both safe; serial = slice(0,4))
    - lib/review-notes.ts (the unit-tested helpers — the prose's glob/rename/classify steps must match selectUnread/markReadName/classifyNote behavior exactly)
    - .planning/phases/07-review-notes-skill-docs/07-PATTERNS.md ("Portable body structure", "Claude Code wrapper" sections, and the Critical Anti-Patterns table)
  </read_first>
  <action>
    Create skill/SKILL.md as portable agent-agnostic markdown with NO frontmatter (must be readable by Claude Code, Cursor, Codex, any folder-reading agent). Structure: title, one-paragraph purpose (replace screenshot-paste-describe ping-pong with a durable file loop), "When to use" / "When NOT to use", "Preconditions" (notes dir = `./notes` relative to project root per D-02, overridable by an explicit path argument; agent can read/write/rename files there), then numbered "Steps". Step 1 — discover unread notes: list files ending in `.md` but NOT `.read.md`, sort ascending (state explicitly the 4-digit zero-padded serial prefix makes lexicographic order = serial order, mirroring selectUnread); if none, report "no unread notes" and stop (SKILL-05 empty). Step 2 (per note, serial order, SKILL-03): (a) read file, parse YAML frontmatter between the first `---` pair, note mode/selector/rect/screenshots/status, read the body's leading comment as the developer instruction; derive serial from filename.slice(0,4) NOT fm.id (state the zero-padding pitfall); (b) if mode element, read the `## Element context` section (selector, computed styles, truncated outerHTML) to locate the code; (c) if screenshots[] non-empty AND the sibling `<base>+N.png` files exist (paths are relative to the notes dir, not project root — resolve by joining with notes dir), open them with vision when text+context is insufficient — but if a referenced screenshot file is missing, emit one line `WARN: <filename> not found — proceeding text-only` and continue (D-09, NOT ambiguous); (d) apply the fix to the relevant source file(s) (D-04 auto-fix); if genuinely ambiguous even with element context, do NOT fix — set frontmatter `status: flagged`, append a `> flagged: <reason>` blockquote line to the body, LEAVE the filename unchanged, skip to next (D-08); (e) ONLY after a successful fix: set frontmatter `status: unread`→`read` AND rename `<name>.md`→`<name>.read.md` (D-06 rename is the LAST action; D-07 both belt+suspenders — describe updating the status key by parsing/restringifying the YAML block, note the repo's `yaml` lib is available). Step 3 — report a terse summary: N processed, M flagged, K skipped. Explicitly forbid the anti-patterns from PATTERNS.md (glob without excluding *.read.md; serial from id; rename-before-fix; treating missing screenshot as ambiguous). Do NOT instruct any network/host call — the skill is disk-only. Then create .claude/skills/review-notes/SKILL.md as a thin wrapper: frontmatter `name: review-notes` + a `description` block (trigger phrases: "read my notes", "process review notes", "fix sticky notes", "run review-notes", "what notes do I have"; one-line summary that it reads each unread *.md, fixes it, renames to *.read.md, idempotent). Wrapper body = exactly one pointer sentence: read and follow skill/SKILL.md in this repo; notes dir defaults to ./notes. NO step logic in the wrapper (D-01 — no duplication). No `allowed-tools` needed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('skill/SKILL.md','utf8');const w=fs.readFileSync('.claude/skills/review-notes/SKILL.md','utf8');if(/^---/.test(s))throw new Error('portable skill must NOT have frontmatter');if(!/\.read\.md/.test(s))throw new Error('missing read-marker rule');if(!/Element context/.test(s))throw new Error('missing element-context step');if(!/flagged/.test(s))throw new Error('missing ambiguous/flagged step');if(!/proceeding text-only/.test(s))throw new Error('missing missing-screenshot rule');if(!/name:\s*review-notes/.test(w))throw new Error('wrapper missing name frontmatter');if(!/skill\/SKILL\.md/.test(w))throw new Error('wrapper must point at skill/SKILL.md');console.log('skill prose OK');"</automated>
  </verify>
  <done>skill/SKILL.md exists with no frontmatter and covers glob-exclude, serial-from-filename, element-context read, screenshot-with-text-only fallback, auto-fix, ambiguous-flag, after-fix rename+status. Wrapper has name+description frontmatter and a single pointer sentence to skill/SKILL.md with zero step logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create fixture notes/ set + human-UAT runbook (SKILL-02..05)</name>
  <files>test/fixtures/notes/0001-20260101-120000.md, test/fixtures/notes/0001-20260101-120000+1.png, test/fixtures/notes/0002-20260101-120001.md, test/fixtures/notes/0003-20260101-120002.md, test/fixtures/notes/0004-20260101-120003.md, test/fixtures/notes/0099-20260101-120099.read.md, .planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/07-review-notes-skill-docs/07-PATTERNS.md ("test/fixtures/notes/" section — full element + free frontmatter schemas, element body template, the 6-file table)
    - .planning/phases/07-review-notes-skill-docs/07-RESEARCH.md (Fixture Set table + Expected outcomes after skill run)
    - host/src/write-note.ts (buildFrontmatter/buildNoteBody — fixtures must match the real on-disk shape exactly so the skill parses them like production notes)
    - skill/SKILL.md (created in Task 1 — the UAT runbook drives a human through running this skill against the fixtures)
  </read_first>
  <action>
    Create test/fixtures/notes/ with 6 files matching the real write-note.ts output shape. (1) 0001-20260101-120000.md — element mode, valid frontmatter (id, created, mode: element, url, title, viewport, selector, optional react_component, rect, screenshots: ["0001-20260101-120000+1.png"], status: unread), body = a CLEAR actionable instruction + a `## Element context` block (Selector / Tag·role / Text / Rect lines + a small curated computed-styles table + a short truncated outerHTML) + a `### Screenshots` line referencing +1.png. (2) 0001-20260101-120000+1.png — a tiny valid PNG (1x1 transparent is fine; write the known 67-byte 1x1 transparent PNG byte sequence, or copy any existing repo PNG asset — it only needs to exist and be openable). (3) 0002-20260101-120001.md — free mode (note_position, no selector/rect), screenshots: [], body = a clear instruction, no `## Element context`. (4) 0003-20260101-120002.md — element mode, screenshots references a +1.png that is INTENTIONALLY NOT created (missing-screenshot test, D-09). (5) 0004-20260101-120003.md — free mode, screenshots: [], body = an intentionally AMBIGUOUS instruction (e.g. vague "make this better" with no actionable target) to trigger the flagged path (D-08). (6) 0099-20260101-120099.read.md — free mode with `status: read` in frontmatter (pre-existing read note that must NOT be processed on a run — proves SKILL-04 exclusion). Then write .planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md: a short runbook listing the 6 fixtures, the exact steps for Omer to point an agent at a COPY of test/fixtures/notes/ and run the review-notes skill, and the expected outcome per fixture (0001/0002 → renamed *.read.md + status: read; 0003 → renamed *.read.md after a WARN line, text-only; 0004 → stays *.md with status: flagged + a `> flagged:` line; 0099 → unchanged), plus the idempotency check (re-run reports "no unread notes"). Note in the runbook to copy the fixtures to a scratch dir first since the skill mutates files.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='test/fixtures/notes/';for(const f of ['0001-20260101-120000.md','0001-20260101-120000+1.png','0002-20260101-120001.md','0003-20260101-120002.md','0004-20260101-120003.md','0099-20260101-120099.read.md']){if(!fs.existsSync(d+f))throw new Error('missing fixture '+f);}const read=fs.readFileSync(d+'0099-20260101-120099.read.md','utf8');if(!/status:\s*read/.test(read))throw new Error('0099 must be status: read');const el=fs.readFileSync(d+'0001-20260101-120000.md','utf8');if(!/## Element context/.test(el))throw new Error('0001 must have element context');if(!fs.existsSync('.planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md'))throw new Error('missing UAT runbook');const png=fs.statSync(d+'0001-20260101-120000+1.png');if(png.size<1)throw new Error('PNG fixture empty');console.log('fixtures OK');"</automated>
  </verify>
  <done>All 6 fixtures exist with correct shapes (0001 element+real PNG, 0002 free, 0003 missing-PNG, 0004 ambiguous-free, 0099 pre-read); the +1.png is a real openable PNG; 07-HUMAN-UAT.md lists per-fixture expected outcomes + the idempotency re-run check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — run skill against fixtures (SKILL-03)</name>
  <what-built>The portable review-notes skill (skill/SKILL.md), its Claude Code wrapper, a 6-file fixture notes/ dir, and a UAT runbook. Plan 01's pure helpers already prove the glob/rename/classify contract under `npm run test:lib`; this checkpoint confirms the prose skill actually drives an end-to-end note fix (SKILL-03), which cannot be unit-tested deterministically.</what-built>
  <how-to-verify>
    1. Copy `test/fixtures/notes/` to a scratch directory (the skill mutates files).
    2. Open `.planning/phases/07-review-notes-skill-docs/07-HUMAN-UAT.md` and follow it: point an AI agent at the scratch notes dir and run the review-notes skill.
    3. Confirm: 0001 and 0002 become `*.read.md` with `status: read`; 0003 becomes `*.read.md` after a one-line `WARN: ... proceeding text-only`; 0004 stays `*.md` with `status: flagged` and a `> flagged: <reason>` line; 0099 is unchanged.
    4. Re-run the skill on the same scratch dir — confirm it reports "no unread notes" (idempotent).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what diverged from the expected outcomes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill prose → agent file operations | The skill instructs an agent to read/rename/rewrite files under the project `notes/` dir. The expectation is path-confined to that dir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | skill instructs writes outside notes/ | mitigate | Prose scopes ALL file operations to the project `notes/` dir (default ./notes) and the source files being fixed; it instructs no writes outside the project; it makes no host/network call |
| T-07-03 | Information Disclosure | skill leaks note content over a network | mitigate | Skill is explicitly disk-only — no network/host call instructed anywhere (RESEARCH Security Domain: no new endpoints) |
| T-07-04 | Tampering (reliability) | rename-before-fix silently drops a note (core-value regression) | mitigate | D-06 enforced in prose: rename is the LAST action, only after fix success; Plan 01 markReadName/selectUnread idempotency is unit-tested |

**Verdict:** Low surface — prose + fixtures, no executable runtime, no network, no auth. No high-severity threats.
</threat_model>

<verification>
- The Task 1 automated grep gate passes (portable skill has no frontmatter and covers all five rules; wrapper points at skill/SKILL.md).
- The Task 2 fixture existence + shape gate passes.
- Human UAT (checkpoint) confirms SKILL-03 end-to-end fix + SKILL-02/04/05 outcomes against fixtures.
</verification>

<success_criteria>
- skill/SKILL.md is portable (no frontmatter), self-contained, and documents serial order, auto-fix, after-fix rename+status:read, ambiguous-flag, and missing-screenshot text-only fallback.
- .claude/skills/review-notes/SKILL.md is a thin wrapper (name+description frontmatter, single pointer sentence, no logic).
- The 6 fixtures + 07-HUMAN-UAT.md let a human verify SKILL-02..05; the human UAT checkpoint confirms SKILL-03.
</success_criteria>

<output>
Create `.planning/phases/07-review-notes-skill-docs/07-02-SUMMARY.md` when done.
</output>
