---
phase: 06-region-capture-visual-design
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - entrypoints/background.ts
  - entrypoints/review.content/pin.ts
  - entrypoints/review.content/index.ts
  - entrypoints/review.content/card.ts
  - entrypoints/review.content/styles.css
autonomous: false
requirements: [PIN-01, PIN-02, PIN-03, PIN-04, PIN-05, PIN-06]
must_haves:
  truths:
    - "On entering Review Mode the extension fetches notes for the current page (exact URL path, query ignored) via GET /annotations and renders one pin per note (PIN-01)"
    - "Element pins anchor to the stored selector (re-queried) and reposition on throttled scroll/resize; free pins float at stored viewport coords (PIN-02)"
    - "An orphaned pin (selector matches nothing) renders greyed/dashed at its last-known page-absolute rect with a tooltip — never hidden (PIN-03)"
    - "Pins encode mode color (free amber / element blue) + unread(red)/read(green) dot; hover shows a textContent note-text preview (PIN-04)"
    - "Clicking a pin opens a view card with Edit / Delete / Close (PIN-05)"
    - "Edit saves via PUT /annotation/<serial> (re-marks unread); Delete via DELETE behind an inline Confirm/Keep footer; the pin updates/disappears (PIN-06)"
    - "Pins re-fetch + re-render after each Send so a just-sent note appears as a pin; all scroll/resize listeners are torn down on Review-Mode exit (no leak)"
    - "Existing free + element Send paths and the Plan-02 camera flow keep working (regression guard)"
  artifacts:
    - path: "entrypoints/review.content/pin.ts"
      provides: "mountPins, teardownPins, openPinCard, computePinPosition (window-bound), throttled reposition"
      exports: ["mountPins", "teardownPins"]
    - path: "entrypoints/background.ts"
      provides: "handleListAnnotations/handleEditAnnotation/handleDeleteAnnotation + 3 onMessage cases (return true)"
      contains: "handleListAnnotations"
    - path: "entrypoints/review.content/index.ts"
      provides: "onMount mountPins after tabId; onRemove teardownPins; re-fetch after Send"
      contains: "mountPins"
    - path: "entrypoints/review.content/styles.css"
      provides: ".sfx-pin/.sfx-pin-orphaned/.sfx-pin-dot/.sfx-pin-read/.sfx-pin-preview + pin card delete-confirm classes"
      contains: "sfx-pin"
  key_links:
    - from: "index.ts onMount"
      to: "pin.ts mountPins → SFX_LIST_ANNOTATIONS"
      via: "SW relay → GET /annotations"
      pattern: "mountPins"
    - from: "background.ts handlers"
      to: "host GET/PUT/DELETE"
      via: "chrome.tabs.get-derived URL (never message body)"
      pattern: "chrome.tabs.get"
    - from: "pin.ts openPinCard"
      to: "SFX_EDIT_ANNOTATION / SFX_DELETE_ANNOTATION"
      via: "SW relay → PUT/DELETE"
      pattern: "SFX_EDIT_ANNOTATION"
    - from: "index.ts onRemove"
      to: "pin.ts teardownPins"
      via: "scroll/resize listener cleanup"
      pattern: "teardownPins"
---

<objective>
Deliver the Persistent Pins vertical slice: on Review-Mode entry, rehydrate notes from disk (GET /annotations), render one on-page pin per note (element pins anchored to their selector, free pins floating, orphaned pins greyed at last-known rect), with mode color + read-state dot + hover preview, and a click-to-open card that views/edits (PUT)/deletes (DELETE) the note. Three new SW relay handlers carry the CRUD; pins re-fetch after each Send and tear down cleanly on exit.

Purpose: PIN-01..06 — the durable, revisitable map of feedback backed by the files on disk (D-01/D-08). Wires the host CRUD + message types from Plan 01 into live UI.
Output: New `pin.ts`; SW handlers in `background.ts`; onMount/onRemove + after-Send wiring in `index.ts`; pin/preview/delete-confirm CSS; the openPinCard surface (card.ts reuse).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-region-capture-visual-design/06-RESEARCH.md
@.planning/phases/06-region-capture-visual-design/06-PATTERNS.md
@.planning/phases/06-region-capture-visual-design/06-UI-SPEC.md
@.planning/phases/06-region-capture-visual-design/06-01-SUMMARY.md
@.planning/phases/06-region-capture-visual-design/06-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 0). Use directly — do not redefine. -->
lib/types.ts:
  SFX_LIST_ANNOTATIONS, SFX_EDIT_ANNOTATION, SFX_DELETE_ANNOTATION (as const)
  MsgListAnnotations { type; tabId:number }
  MsgEditAnnotation  { type; tabId:number; serial:string; comment:string }
  MsgDeleteAnnotation{ type; tabId:number; serial:string }
lib/pin-position.ts:
  matchesUrlPath(noteUrl, pageUrl): boolean   // (selector match happens in pin.ts via document.querySelector)
host PinDescriptor (returned by GET /annotations, mirror its shape in the CS):
  { serial:string; mode:'free'|'element'; status:string; url:string; text:string; selector?:string; rect?:{x,y,width,height}; viewportCoords?:{x,y}; screenshots:string[] }

New CS module to create:
  entrypoints/review.content/pin.ts
    export async function mountPins(container: HTMLElement, tabId: number, showToastFn:(msg:string,isErr:boolean)=>void): Promise<void>;
    export function teardownPins(): void;
    // internal: openPinCard, computePinPosition (uses window.scrollX/getBoundingClientRect), throttled scroll/resize reposition
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SW relay handlers (list/edit/delete) in background.ts</name>
  <files>entrypoints/background.ts</files>
  <read_first>
    - entrypoints/background.ts (FULL — handleSendAnnotation ~283-357 for the resolveRoute + chrome.tabs.get + fetch pattern; handleCaptureTab ~433-444; onMessage switch ~482-577 with the `return true` invariant; the SFX_CAPTURE_TAB IDOR sender-binding guard ~541-556; imports block ~19-37)
    - lib/types.ts (the three new consts + Msg interfaces from Plan 01)
    - host/src/read-note.ts PinDescriptor (the JSON shape GET returns)
    - 06-PATTERNS.md section "entrypoints/background.ts (MOD)" (handler skeleton, IDOR guard, onMessage cases, imports)
    - 06-RESEARCH.md Pattern 6 (SW handler bodies) + anti-pattern "Trusting pageUrl from message body" + Security Domain T-06-01/06
  </read_first>
  <action>
    Add imports for `SFX_LIST_ANNOTATIONS`/`SFX_EDIT_ANNOTATION`/`SFX_DELETE_ANNOTATION` and the three Msg types. Add `handleListAnnotations(tabId)`, `handleEditAnnotation(tabId, serial, comment)`, `handleDeleteAnnotation(tabId, serial)` mirroring handleSendAnnotation: re-read storage (`loadStorageState`), derive URL via `chrome.tabs.get(tabId)` (NEVER trust a url in the message body), compute origin, `resolveRoute(origin, state)`, require `host` + `host.token`, then fetch `http://127.0.0.1:${host.port}/annotations?url=${encodeURIComponent(tab.url)}` (GET, token header) / `PUT` / `DELETE http://127.0.0.1:${host.port}/annotation/${encodeURIComponent(serial)}` (token header; PUT body JSON `{comment}`). Return `{ok:true, pins}` / `{ok:true}` or `{ok:false, error}`; wrap fetch in try/catch for "Host unreachable". Add three onMessage cases before `default:`: SFX_LIST_ANNOTATIONS calls the handler `.then(sendResponse)`; SFX_EDIT_ANNOTATION and SFX_DELETE_ANNOTATION FIRST apply the sender-tab IDOR guard (`if (sender.tab?.id == null || sender.tab.id !== m.tabId) { sendResponse({ok:false,error:'forbidden'}); return true; }`) then call their handler. Every async case MUST `return true`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` (extension) → 0 errors; `npm run build` succeeds
    - All three handlers derive URL from `chrome.tabs.get` — `grep -c "chrome.tabs.get" entrypoints/background.ts` increased by ≥ 3; no handler reads a `url`/`pageUrl` field off the message
    - Edit + Delete cases apply the sender-tab IDOR guard (grep `sender.tab` present in both new mutating cases)
    - Each of the three new onMessage cases ends with `return true`
    - GET uses `/annotations?url=` and PUT/DELETE use `/annotation/${...serial...}` (grep both URL shapes)
  </acceptance_criteria>
  <done>Three SW relay handlers route list/edit/delete to the host with tab-derived URLs + IDOR guards + return-true; build green.</done>
</task>

<task type="auto">
  <name>Task 2: pin.ts (mount/teardown/reposition/openPinCard) + pin/preview/delete-confirm CSS</name>
  <files>entrypoints/review.content/pin.ts, entrypoints/review.content/styles.css, entrypoints/review.content/card.ts</files>
  <read_first>
    - entrypoints/review.content/picker.ts (analog — module-level state, getRootNode().host shadow-host exclusion, getBoundingClientRect, cleanup-array teardown, textContent label)
    - entrypoints/review.content/chip.ts (teardown-via-cleanup-array pattern)
    - entrypoints/review.content/card.ts (openCard/openElementCard surface to reuse for openPinCard; _doSend SW-relay callback shape ~295-328; setSfxVisibility; thumbnail renderer from Plan 02)
    - entrypoints/review.content/toast.ts (textContent + slice(0,200) pattern)
    - lib/pin-position.ts (matchesUrlPath) and lib/types.ts (the three message consts)
    - 06-PATTERNS.md section "entrypoints/review.content/pin.ts" (module state, scroll/resize teardown, shadow-host exclusion, textContent, SW relay, throttle, imports) + "card.ts (MOD)"
    - 06-RESEARCH.md Pattern 5 (pin positioning + orphaned fallback + throttle) + Pitfall 6 (listener leak) + Pitfall 8 (data-URL img)
    - 06-UI-SPEC.md sections "On-Page Pin Marker", "Pin View/Edit/Delete Card", "Pin Lifecycle State Machine", Z-order, Keyboard, Scroll/Resize Repositioning, Copywriting
  </read_first>
  <action>
    Create `entrypoints/review.content/pin.ts`. Module-level state: array of `{pin:HTMLElement; el:Element|null; data:PinDescriptor}` and a cleanup-fns array. `mountPins(container, tabId, showToastFn)`: `chrome.runtime.sendMessage({type:SFX_LIST_ANNOTATIONS, tabId}, resp)`; on error/`!resp.ok` call `showToastFn('Could not load pins — '+err, true)`. For each PinDescriptor build a `div.sfx-pin` (createElement only): apply `sfx-pin` + mode class (free=amber, element=blue per UI-SPEC colors), a `.sfx-pin-dot` (red unread / green via `sfx-pin-read` when status==='read'), a 📌 glyph, and a `.sfx-pin-preview` child whose `textContent` is `(status==='read'?'[read] ':'')+text.slice(0,200)` shown on mouseenter/hidden on mouseleave (flip above/below per 130px-from-top rule). Position via internal `computePinPosition(el, storedRect)`: element pins → `document.querySelector(selector)` (page element; exclude the sfx shadow host via getRootNode().host); if found use `getBoundingClientRect()` (position:fixed coords); if null → orphaned: `sfx-pin-orphaned` class, `title` = "Element not found on this page — click to view or delete", position from stored rect minus scroll. Free pins use stored viewportCoords (fallback to a sensible default corner if absent). Register ONE throttled (100ms timestamp-delta, no lodash) scroll+resize listener that repositions all anchored element pins; push its removers into the cleanup array. Pin click → `openPinCard(container, tabId, data, showToastFn)`. `openPinCard` reuses the card surface in a `sfx-card-pin` VIEW mode (note body text via textContent + read-only thumbnails) with footer [Edit][Delete][Close]; Edit → in-place EDIT mode (textarea pre-filled, Save styled like Send) → `SFX_EDIT_ANNOTATION` relay (disable+"Saving…"; on ok toast "Note saved", set pin dot unread/red, update preview; on err re-enable + toast); Delete → inline confirm footer ("Delete this note and its screenshots?" + "Confirm delete"/"Keep", no window.confirm) → `SFX_DELETE_ANNOTATION` relay (on ok remove the pin element, close card, toast "Note deleted"; on err restore footer + toast). `teardownPins()` runs all cleanup fns (removes scroll/resize listeners), removes every pin element, resets state. Add the `openPinCard` entry to card.ts if the card surface needs a pin-mode branch; otherwise keep openPinCard in pin.ts using card.ts helpers. In `styles.css` add `.sfx-pin` (position:fixed; 24x24; border-radius:4px 4px 4px 0; z-index:2; box-shadow), free/element color variants, `.sfx-pin-orphaned` (grey #e5e7eb/#9ca3af dashed, opacity 0.75), `.sfx-pin-dot` (8px, top/right -4px, #dc2626, white ring) + `.sfx-pin-read .sfx-pin-dot{background:#16a34a}`, `.sfx-pin-preview` (z-index:3; rgba(17,17,17,0.90); #fff; 13px; max-width:240px; white-space:pre-wrap; display:none), `.sfx-pin-body-text`, `.sfx-btn-delete`, `.sfx-btn-delete-confirm`, `.sfx-del-confirm-text` per 06-UI-SPEC. px only, no rem, no innerHTML, no host var().
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` (extension) → 0 errors; `npm run build` succeeds
    - pin.ts exports `mountPins` and `teardownPins`; `grep -c innerHTML entrypoints/review.content/pin.ts` returns 0
    - Orphaned fallback present: pin.ts queries `document.querySelector` and on null applies `sfx-pin-orphaned` + a `title` (grep both)
    - Exactly one throttled scroll/resize listener registered and removed in teardown: `grep -c addEventListener entrypoints/review.content/pin.ts` matches the removeEventListener count for scroll/resize (no leak)
    - Hover preview + read prefix use textContent: `grep -n "textContent" entrypoints/review.content/pin.ts` shows the preview assignment; no innerHTML
    - styles.css contains `.sfx-pin`, `.sfx-pin-orphaned`, `.sfx-pin-dot`, `.sfx-pin-read`, `.sfx-pin-preview`, `.sfx-btn-delete`, `.sfx-del-confirm-text`; no new rem (`grep -nE "[0-9](rem|em)\b"` shows no new offenders)
    - DELETE uses an inline confirm footer, not window.confirm: `grep -c "window.confirm" entrypoints/review.content/pin.ts` returns 0
  </acceptance_criteria>
  <done>pin.ts mounts anchored/floating/orphaned pins with dots + hover preview + view/edit/delete card via SW relays; teardown removes listeners; pin CSS present; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pins into index.ts lifecycle (onMount mount, onRemove teardown, re-fetch after Send)</name>
  <files>entrypoints/review.content/index.ts</files>
  <read_first>
    - entrypoints/review.content/index.ts (FULL — onMount ~41-93 getTabId().then FAB/chip mount; the onSent callback hook in openElementCard/openCard wiring; onRemove ~95-103 teardownChip/closeCard/exitPickMode)
    - entrypoints/review.content/pin.ts (mountPins/teardownPins from Task 2)
    - 06-PATTERNS.md section "entrypoints/review.content/index.ts (MOD)" (onMount/onRemove/after-Send patterns)
    - 06-RESEARCH.md Open Question 3 decision (re-fetch all pins after each Send)
  </read_first>
  <action>
    In `index.ts` onMount, inside the existing `getTabId().then(tabId => { ... })` block AFTER the FAB mounts, call `mountPins(container, tabId, toast).catch(err => toast('Could not load pins — '+String(err), true))`. Wire the after-Send re-fetch: in the `onSent` callback passed to the card open functions, call `teardownPins()` then `mountPins(container, resolvedTabId, toast)` so a just-sent note appears as a pin and external `*.read.md` renames are reflected (guard on a non-null tabId). In onRemove, add `teardownPins()` alongside the existing `teardownChip` / `closeCard` / `exitPickMode`. Do not change FAB/chip/picker behavior — additive only.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` (extension) → 0 errors; `npm run build` succeeds
    - onMount calls `mountPins` after tabId resolves (grep present inside the getTabId().then block)
    - onRemove calls `teardownPins()` (grep present alongside teardownChip)
    - The Send-success path re-fetches pins: `grep -c "mountPins" entrypoints/review.content/index.ts` ≥ 2 (initial + after-Send)
    - REGRESSION: existing teardownChip/closeCard/exitPickMode lines in onRemove and FAB/chip mounts in onMount are unchanged
  </acceptance_criteria>
  <done>Pins mount on Review-Mode entry, re-render after each Send, and tear down (listeners removed) on exit; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Chrome UAT — persistent pins end to end (PIN-01..06)</name>
  <what-built>
    On Review-Mode entry, notes for the current URL path are fetched from disk (GET /annotations) and rendered as on-page pins: element pins anchored to their selector (reposition on scroll/resize), free pins floating, orphaned pins greyed/dashed at last-known rect. Pins show mode color + unread(red)/read(green) dot + hover text preview. Clicking a pin opens a view card with Edit (PUT, re-marks unread) / Delete (DELETE behind Confirm/Keep) / Close.
  </what-built>
  <how-to-verify>
    Prereqs: `npm run build`, reload extension, host running on a project that already has a couple of notes for a specific page URL (create them first via free + element Send if needed).
    1. Navigate to that exact page URL, enter Review Mode → one pin appears per note for that path. Element pins sit on their element; free pins float. Mode colors match (free amber / element blue). Each pin has a red (unread) or green (read) dot.
    2. Add `?tab=2` to the URL (same path) → re-enter Review Mode → the same pins still appear (query ignored, D-02). Navigate to a different path → those pins do NOT appear.
    3. Scroll and resize the page → element pins follow their elements; free pins stay at their viewport coords.
    4. Hover a pin → a text preview of the note appears (read notes prefixed `[read]`). No layout breakage near the top of the viewport (preview flips below).
    5. Remove/alter a pinned element (e.g., delete it via devtools, or navigate to a variant where it is gone) and re-enter → the pin shows greyed/dashed at its last-known position with the "Element not found" tooltip; it is still clickable.
    6. Click a pin → view card opens (note text + thumbnails read-only) with Edit/Delete/Close. Edit → change text → Save → toast "Note saved"; the note's `.md` is overwritten in place (same serial/filename) and its status is re-marked `unread` (dot goes red). 
    7. Click a pin → Delete → inline "Confirm delete"/"Keep" footer; Keep cancels; Confirm delete → toast "Note deleted", the pin disappears, and on disk the `.md` AND its `+N.png` are gone.
    8. Send a NEW note while in Review Mode → it appears as a new pin without re-entering Review Mode.
    9. Exit Review Mode and scroll → no console errors (scroll/resize listeners were torn down).
    10. REGRESSION: free + element Send (Plan 4-of-Phase-4/5 paths) and the Plan-02 camera capture still work.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (missing pin, wrong anchor, orphaned not shown, edit/delete failure, listener leak, regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CS → SW (list/edit/delete) | serial + tabId cross here; SW must derive host/URL from tab, not message |
| host note text → pin DOM | user-authored text rendered in preview; must be textContent |
| stored selector → document.querySelector | selector re-queried against the live (untrusted) page |

## STRIDE Threat Register (extends 05-SECURITY.md)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | IDOR | SFX_EDIT/DELETE_ANNOTATION → PUT/DELETE | mitigate | SW derives host+URL from chrome.tabs.get(tabId) (never message body); serial resolves only inside that host's notesDir (Plan 01 isInsideDir) |
| T-06-05 | Tampering (XSS) | pin hover-preview / orphaned tooltip | mitigate | preview via textContent slice(0,200); orphaned tooltip via title attr; zero innerHTML in pin.ts |
| T-06-06 | Spoofing | CS sends serial for wrong note | mitigate | sender-tab IDOR guard on edit/delete cases (Task 1); host route only reaches the routed project |
| T-06-09 | DoS | scroll/resize listener leak after exit | mitigate | teardownPins removes all listeners in onRemove (Pitfall 6) |
| T-06-08 | Supply chain | npm installs | accept | zero new packages this slice |
</threat_model>

<verification>
- `npm run build` succeeds; `tsc --noEmit` 0 errors
- `npm run check` green (foundation tests from Plan 01 still pass)
- Chrome UAT (Task 4) approved
- No new innerHTML; no window.confirm; no rem; no new npm packages; no listener leak
</verification>

<success_criteria>
Phase-6 success criteria 5 (one pin per note for the exact URL path; element anchored + scroll/resize reposition; free floating; orphaned greyed at last-known rect, never hidden) and 6 (click → view/edit/delete; edit = PUT overwrite-in-place re-marks unread; delete = DELETE file + screenshots behind confirm; both token-gated + path-confined) met. Existing Send + camera paths regression-guarded.
</success_criteria>

<output>
Create `.planning/phases/06-region-capture-visual-design/06-04-SUMMARY.md` when done
</output>
