---
phase: 06-region-capture-visual-design
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - entrypoints/review.content/styles.css
  - entrypoints/review.content/card.ts
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "Post-it cards have a warm paper aesthetic — free-note cards cream (#fefce8), element-note cards light blue (#eff6ff) — with a deepened shadow (UI-02)"
    - "The card header strip is mode-coded: amber (#fde68a/#f59e0b) for free, blue (#dbeafe/#93c5fd) for element (UI-03)"
    - "Success and error toasts are styled with a deepened shadow and existing stripe/icon/dismiss (UI-04)"
    - "All Phase-6 CSS uses px only (zero rem/em/% font-size) and adds no new :host property, preserving shadow-DOM isolation (UI-01)"
    - "The sfx-card-element modifier class is applied to element-note cards so the element palette resolves (UI-03 wiring)"
    - "Card drag remains smooth (no CSS transition on top/left); free + element Send paths and the Phase-2 camera surfaces are unchanged (regression guard)"
  artifacts:
    - path: "entrypoints/review.content/styles.css"
      provides: "paper card bg + mode header strips + deepened card/toast shadow"
      contains: "#fefce8"
    - path: "entrypoints/review.content/card.ts"
      provides: "sfx-card-element modifier applied on element-note card open (if not already)"
      contains: "sfx-card-element"
  key_links:
    - from: "card.ts openElementCard"
      to: "#sfx-card.sfx-card-element CSS"
      via: "modifier class drives element palette"
      pattern: "sfx-card-element"
---

<objective>
Deliver the Visual Design polish slice: the warm paper-aesthetic sticky-note look — mode-coded card backgrounds and header strips, deepened card/toast shadows — entirely as CSS in `styles.css`, plus the small `card.ts` wiring to ensure element-note cards carry the `sfx-card-element` modifier. Shadow-DOM isolation (px-only, `all: initial`, no host var()) is preserved exactly.

Purpose: UI-01..04. The capture-surface CSS (cam button, scrim, thumbnails) landed in Plan 02; this slice is the aesthetic pass on the card/header/toast vocabulary that Plan 04's pins will visually inherit.
Output: Paper/header/shadow CSS in `styles.css`; element-modifier wiring in `card.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-region-capture-visual-design/06-UI-SPEC.md
@.planning/phases/06-region-capture-visual-design/06-PATTERNS.md
@.planning/phases/06-region-capture-visual-design/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paper aesthetic + mode header strips + shadow upgrades (styles.css) + element modifier wiring (card.ts)</name>
  <files>entrypoints/review.content/styles.css, entrypoints/review.content/card.ts</files>
  <read_first>
    - entrypoints/review.content/styles.css (FULL — existing #sfx-card, .sfx-card-header, .sfx-toast rules; confirm zero rem; the :host { all: initial } block must NOT change; existing box-shadow values to upgrade)
    - entrypoints/review.content/card.ts (openElementCard — confirm whether it already adds `sfx-card-element` to #sfx-card; if present from Phase 5, do not duplicate; openCard free path must NOT get the modifier)
    - 06-UI-SPEC.md sections "Color → Phase 6 New Color Tokens", "Visual Design Polish on Existing Card", "Shadow-DOM Isolation Contract", Z-order Contract (do not regress existing z-index)
    - 06-PATTERNS.md sections "entrypoints/review.content/card.ts (MOD)" (camera/element-modifier notes), Pattern 7 (paper CSS)
  </read_first>
  <action>
    In `styles.css`: set free-note card `#sfx-card { background: #fefce8; }` and element variant `#sfx-card.sfx-card-element { background: #eff6ff; }`; upgrade card `box-shadow` to `0 4px 20px rgba(0,0,0,0.22)`. Mode header strips: `.sfx-card-header { background:#fde68a; border-bottom-color:#f59e0b; }` and `#sfx-card.sfx-card-element .sfx-card-header { background:#dbeafe; border-bottom-color:#93c5fd; }` — preserve the existing `cursor:grab`. Upgrade `.sfx-toast` box-shadow to `0 4px 16px rgba(0,0,0,0.20)` (keep existing stripe/icon/dismiss + the `opacity 200ms ease`). Do NOT add a CSS transition on card `top`/`left` (would jitter interactjs drag). Do NOT add or modify any `:host` property and introduce no `var(--*)` from the host page and no `rem`/`em`/`%` font-size. In `card.ts`: ensure `openElementCard` applies the `sfx-card-element` class to `#sfx-card` (add it only if Phase 5 did not already) and that `openCard` (free) does NOT — this modifier is the sole switch between the amber and blue palettes. Touch nothing else in card.ts (camera/thumbnail code from Plan 02 and the Send pipelines stay intact).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` (extension) → 0 errors; `npm run build` succeeds
    - styles.css contains `#fefce8`, `#eff6ff`, `#fde68a`, `#dbeafe`, `0 4px 20px rgba(0,0,0,0.22)`, `0 4px 16px rgba(0,0,0,0.20)` (grep each present)
    - `:host` block unchanged: `grep -A3 ":host" entrypoints/review.content/styles.css` still shows `all: initial` with no new properties
    - No rem/em font-size introduced in Phase-6 rules: `grep -nE "[0-9](rem|em)\b" entrypoints/review.content/styles.css` shows no new offending lines; `grep -c "var(--" entrypoints/review.content/styles.css` returns 0
    - element-note cards resolve the blue palette via `sfx-card-element` and free cards keep amber: code review confirms openElementCard sets the class, openCard does not
    - REGRESSION: no CSS transition added to `#sfx-card` top/left; Plan 02 cam/thumbnail rules untouched
  </acceptance_criteria>
  <done>Warm paper cards, mode-coded header strips, deepened card/toast shadows; element modifier wired; isolation preserved; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Chrome UAT — paper aesthetic + CSS isolation (UI-01..04)</name>
  <what-built>
    Warm paper sticky-note cards (free = cream, element = light blue), mode-coded header strips (free = amber, element = blue), deepened card + toast shadows — all px-only inside the shadow root with no CSS bleed to/from the host page.
  </what-built>
  <how-to-verify>
    Prereqs: `npm run build`, reload the unpacked extension, host running, enter Review Mode.
    1. Open a free note → card has a warm cream background and an amber header strip; the card visibly floats above the page (deeper shadow). Drag the card by its header → motion is smooth, no jitter.
    2. Pick an element (🎯) → element-note card has a light-blue background and a blue header strip (clearly distinct from the free note's amber).
    3. Trigger a success toast (Send a note) and an error toast (stop the host, then Send) → both are styled with a clear stripe (green/red) and a deeper shadow; dismiss works.
    4. CSS isolation: load the extension on `https://tailwindcss.com` (Tailwind-heavy) AND on `https://meyerweb.com/eric/tools/css/reset/` (CSS-reset-heavy). On BOTH pages the card/header/toast render identically — no inherited font-size shrink/grow, no inherited colors, no reset interference. The host page's own styling is also unaffected by the extension.
    5. REGRESSION: confirm the Plan-02 camera button, scrim, crosshair, and thumbnail strip still look and behave correctly (unchanged).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issue (wrong color, CSS bleed, jitter, toast regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host page CSS ↔ shadow root | host styles must not leak into the sfx UI and vice-versa |

## STRIDE Threat Register (extends 05-SECURITY.md)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering (style injection / UI redress) | shadow-root CSS isolation | mitigate | px-only, no new :host property, no host var(); `all: initial` preserved; verified on Tailwind + reset pages (UI-01 UAT) |
| T-06-08 | Supply chain | npm installs | accept | zero new packages this slice (CSS + tiny wiring only) |
</threat_model>

<verification>
- `npm run build` succeeds; `tsc --noEmit` 0 errors
- Chrome UAT (Task 2) approved on Tailwind + reset pages
- px-only, no host var(), :host unchanged
</verification>

<success_criteria>
Phase-6 success criterion 4 met: warm paper cards, smooth drag, mode-coded header strips, styled toasts, no CSS bleed verified on a Tailwind-heavy and a CSS-reset-heavy page.
</success_criteria>

<output>
Create `.planning/phases/06-region-capture-visual-design/06-03-SUMMARY.md` when done
</output>
