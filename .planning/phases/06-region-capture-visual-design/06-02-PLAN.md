---
phase: 06-region-capture-visual-design
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - entrypoints/review.content/marquee.ts
  - entrypoints/review.content/card.ts
  - entrypoints/review.content/styles.css
autonomous: false
requirements: [CAM-01, CAM-02, CAM-03, CAM-04, CAM-05, CAM-06]
must_haves:
  truths:
    - "A 📷 camera button appears in the header of both free-note and element-note cards (CAM-01)"
    - "Clicking it dims the page with a 35% scrim and switches the cursor to a crosshair (CAM-02)"
    - "Dragging ≥6px and releasing captures a DPR-correct region crop; Esc or a sub-6px drag cancels with no capture (CAM-03/04)"
    - "Each crop attaches as a deletable thumbnail; multiple crops stack as +1, +2, … and renumber on delete (CAM-05)"
    - "Send maps the thumbnail array into the screenshots payload; the host writes +N.png and records them (CAM-06)"
    - "The scrim is removed BEFORE setSfxVisibility(false)→waitTwoRafs→captureTab so no sfx UI leaks into the crop (T-06-07)"
    - "Existing free-note Send (openCard/_doSend) and element-note Send (openElementCard/_doElementSend) still write correct .md files (regression guard)"
  artifacts:
    - path: "entrypoints/review.content/marquee.ts"
      provides: "enterMarqueeMode(container, onCapture, onCancel) → cleanup(); scrim + interact.js + Esc"
      contains: "enterMarqueeMode"
    - path: "entrypoints/review.content/card.ts"
      provides: "camera button + thumbnail strip + capture wiring; thumbnails[] state; screenshots payload mapping"
      contains: "sfx-cam-btn"
    - path: "entrypoints/review.content/styles.css"
      provides: ".sfx-cam-btn, .sfx-cam-scrim, .sfx-cam-rect, .sfx-thumb-strip/.sfx-thumb-wrap/.sfx-thumb-img/.sfx-thumb-del"
      contains: "sfx-cam-scrim"
  key_links:
    - from: "card.ts camera click"
      to: "marquee.ts enterMarqueeMode"
      via: "onCapture callback → capture trio"
      pattern: "enterMarqueeMode"
    - from: "marquee.ts"
      to: "lib/marquee.ts buildMarqueeRect/isBelowThreshold"
      via: "interact.js drag listeners"
      pattern: "buildMarqueeRect"
    - from: "card.ts capture path"
      to: "lib/capture.ts captureTab/waitTwoRafs/cropToRect"
      via: "DPR-correct crop trio"
      pattern: "cropToRect"
---

<objective>
Deliver the Region Capture vertical slice: a 📷 camera button on every note card that opens a full-page scrim, lets the developer drag a marquee, crops the captured tab to that rect (DPR-correct), and attaches the result as a deletable thumbnail — then Send writes the thumbnails as +N.png via the existing host pipeline.

Purpose: Region crops are the final capture modality (CAM-01..06). The marquee composes the proven `lib/capture.ts` trio with `interactjs` on a shadow-root scrim.
Output: New `marquee.ts`, extended `card.ts` (button + thumbnail strip + wiring), camera/thumbnail CSS in `styles.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-region-capture-visual-design/06-RESEARCH.md
@.planning/phases/06-region-capture-visual-design/06-PATTERNS.md
@.planning/phases/06-region-capture-visual-design/06-UI-SPEC.md
@.planning/phases/06-region-capture-visual-design/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 0). Use directly. -->
lib/marquee.ts:
  export const MARQUEE_MIN_PX = 6;
  export function buildMarqueeRect(sx,sy,ex,ey): {x,y,width,height};
  export function isBelowThreshold(rect:{width,height}): boolean;

lib/capture.ts (existing — the trio):
  captureTab(tabId): Promise<string>   // SW relay → captureVisibleTab
  waitTwoRafs(): Promise<void>
  cropToRect(dataUrl, rect, dpr): Promise<string>   // accepts CSS viewport coords

New module to create:
  entrypoints/review.content/marquee.ts
    export function enterMarqueeMode(container: HTMLElement, onCapture:(rect:{x,y,width,height})=>void, onCancel:()=>void): () => void;  // returns cleanup()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: marquee.ts (scrim + interact.js + Esc) and scrim/thumbnail CSS</name>
  <files>entrypoints/review.content/marquee.ts, entrypoints/review.content/styles.css</files>
  <read_first>
    - entrypoints/review.content/picker.ts (analog — enterPickMode/exitPickMode lifecycle, container.classList for cursor mode, keydown Esc cleanup array, createElement-only DOM)
    - entrypoints/review.content/card.ts lines ~194-231 (interact(directElementRef).draggable() pattern — NO context:shadowRoot, NO selector string)
    - entrypoints/review.content/styles.css (existing token + z-index structure; confirm zero rem; add new rules only)
    - lib/marquee.ts (buildMarqueeRect, isBelowThreshold, MARQUEE_MIN_PX from Plan 01)
    - 06-PATTERNS.md section "entrypoints/review.content/marquee.ts"
    - 06-RESEARCH.md Pattern 1 (full enterMarqueeMode body) + Pitfall 2 (Esc must call interactable.unset()) + Pitfall 1/9 (scrim z-index + scrim must be gone before capture)
    - 06-UI-SPEC.md sections "Capture Scrim + Marquee Rect", "Thumbnail Strip", Z-order Contract
  </read_first>
  <action>
    Create `entrypoints/review.content/marquee.ts` exporting `enterMarqueeMode(container, onCapture, onCancel): () => void`. Build a `div.sfx-cam-scrim` containing a `div.sfx-cam-rect` (createElement only), append to container, add `sfx-cam-mode` class to container for the crosshair cursor. Wire `interact(scrim).draggable()` with a DIRECT element reference (per card.ts:195 — no `context:shadowRoot`, no selector string): on `start` capture startX/startY from `event.clientX/Y` and show the rect; on `move` set the rect via `buildMarqueeRect`; on `end` compute rect, run `cleanup()`, then `isBelowThreshold` → `onCancel()` else `onCapture(rect)`. Register a capture-phase `keydown` Escape listener on document that calls `cleanup()` then `onCancel()`. `cleanup()` removes the Esc listener, calls `interactable.unset()`, removes the scrim, removes the `sfx-cam-mode` class. Return `cleanup`. In `styles.css` add `.sfx-cam-scrim` (position:fixed; inset:0; background:rgba(0,0,0,0.35); pointer-events:auto; cursor:crosshair; z-index:1), `.sfx-cam-rect` (position:absolute; border:2px solid #1d6ed8; background:rgba(29,110,216,0.12); display:none; box-sizing:border-box; pointer-events:none), `.sfx-cam-btn` (28px circle, #f0f0f0 bg, #ccc border, font-size:16px, margin-left:auto, flex-shrink:0, pointer-events:auto; :hover #e0e0e0; disabled opacity:0.5/cursor:not-allowed), and the thumbnail strip rules `.sfx-thumb-strip`/`.sfx-thumb-wrap`/`.sfx-thumb-img`/`.sfx-thumb-del` exactly per 06-UI-SPEC values. px units only — no rem.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` (extension) → 0 errors; `npm run build` succeeds
    - marquee.ts uses `interact(scrim)` with a direct element reference (grep: `grep -n "interact(scrim" entrypoints/review.content/marquee.ts` matches; `grep -c "context:" entrypoints/review.content/marquee.ts` returns 0)
    - marquee.ts Esc handler calls `interactable.unset()` (grep `unset` present) and is registered with capture true
    - styles.css contains `.sfx-cam-scrim`, `.sfx-cam-rect`, `.sfx-cam-btn`, `.sfx-thumb-strip`, `.sfx-thumb-del`; grep for ` rem` (with leading space/number) in the new rules returns nothing: `grep -nE "[0-9]rem|: *[0-9.]+rem" entrypoints/review.content/styles.css` shows no Phase-6 additions
    - marquee.ts builds DOM via createElement/textContent only — `grep -c innerHTML entrypoints/review.content/marquee.ts` returns 0
  </acceptance_criteria>
  <done>enterMarqueeMode creates a draggable scrim with live rect + Esc cancel + sub-6px guard; scrim/cam-btn/thumbnail CSS present; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Camera button + thumbnail strip + capture wiring in card.ts (both card modes)</name>
  <files>entrypoints/review.content/card.ts</files>
  <read_first>
    - entrypoints/review.content/card.ts (FULL — openCard/_doSend free path AND openElementCard/_doElementSend element path; the setSfxVisibility helper ~571-584; the capture sequence ~586-627; header construction; screenshots payload field)
    - entrypoints/review.content/marquee.ts (enterMarqueeMode from Task 1)
    - lib/capture.ts (captureTab, waitTwoRafs, cropToRect — the trio)
    - 06-PATTERNS.md section "entrypoints/review.content/card.ts (MOD)" (camera button insertion, renderThumbnails, capture sequence, setSfxVisibility coverage note)
    - 06-RESEARCH.md Pattern 2 (renderThumbnails + screenshots payload mapping) + Pitfall 9 (cleanup scrim BEFORE setSfxVisibility) + Pitfall 8 (data-URL img.src — accept for v1, note as Phase 8)
    - 06-UI-SPEC.md "Camera Button", "Thumbnail Strip", "Camera Tool State Machine", Copywriting (aria-labels, "Screenshot capture failed")
  </read_first>
  <action>
    Add a `📷` `button.sfx-cam-btn` (aria-label "Capture region") to BOTH card headers (openCard free + openElementCard element), as the last header child (margin-left:auto pushes it right). Add a card-scoped `thumbnails: {kind:string; dataUrl:string}[]` array and a `.sfx-thumb-strip` container inside `.sfx-card-body` below the textarea — render via a `renderThumbnails(container, items)` helper that `replaceChildren()` then rebuilds each `.sfx-thumb-wrap` (img with `src=dataUrl`, `× ` delete button with aria-label `Remove screenshot {N}`) via createElement/textContent; the × handler splices the entry, renumbers `kind` to `+${j+1}`, and re-renders; hide the strip when empty. Camera click → disable the button (opacity 0.5 / not-allowed) → `enterMarqueeMode(container, onCapture, onCancel)`. onCapture(rect): the scrim is already removed by marquee cleanup → `setSfxVisibility(false)` → `await waitTwoRafs()` → `await captureTab(tabId)` → `await cropToRect(dataUrl, rect, window.devicePixelRatio)` → `setSfxVisibility(true)` → push `{kind:'+'+(thumbnails.length+1), dataUrl:cropped}` → renderThumbnails → re-enable button. On crop error: setSfxVisibility(true), re-enable button, `showToast('Screenshot capture failed', true)`. onCancel: re-enable button (no toast). In BOTH _doSend and _doElementSend, map `screenshots: thumbnails.map(t => ({ kind:t.kind, mime:'image/png', dataUrl:t.dataUrl }))` — for element notes, the existing +1 auto-highlight remains kind '+1' and region thumbnails continue from there (renumber so element auto-highlight stays first). Do NOT add the scrim to setSfxVisibility (marquee cleanup removes it first). Preserve every existing line of the free + element Send pipelines.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` → 0 errors; `npm run build` succeeds
    - card.ts adds `.sfx-cam-btn` to both openCard and openElementCard headers (grep `sfx-cam-btn` count ≥ 2 button-creation sites OR a shared helper used by both)
    - The capture path calls cleanup/marquee BEFORE setSfxVisibility(false): manual code review confirms onCapture runs after marquee cleanup (scrim removed) and the order setSfxVisibility(false)→waitTwoRafs→captureTab→cropToRect→setSfxVisibility(true) is intact
    - `grep -c innerHTML entrypoints/review.content/card.ts` returns 0 (no new innerHTML)
    - Both _doSend and _doElementSend map `thumbnails` into the screenshots payload (grep `thumbnails.map` ≥ 1; existing element +1 path preserved)
    - REGRESSION: existing free-note and element-note send-payload construction lines are unchanged except the screenshots field
  </acceptance_criteria>
  <done>Camera button + deletable thumbnail strip on both cards; capture trio wired; thumbnails flow into the Send payload; free + element Send paths preserved; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Chrome UAT — region capture end to end (CAM-01..06)</name>
  <what-built>
    A 📷 camera button on free + element note cards that opens a dimming scrim with crosshair cursor; drag-marquee region capture (DPR-correct crop via captureTab+cropToRect); deletable multi-thumbnail strip (+1/+2…); thumbnails sent as +N.png through the existing host pipeline.
  </what-built>
  <how-to-verify>
    Prereqs: `npm run build`, then start the host (`npm run host -- --root <a test project dir>`), load the unpacked extension, enter Review Mode on an HTTPS page, set the token in the popup.
    1. Open a free note (+ FAB). Confirm a 📷 button is in the card header. Click it: the page dims (35% scrim) and the cursor becomes a crosshair.
    2. Press Esc → scrim disappears, card returns, NO thumbnail added. Click 📷 again, do a tiny (<6px) click-drag → cancels, NO thumbnail.
    3. Click 📷, drag a real rectangle over page content, release → a thumbnail appears in the strip showing the cropped region (verify the crop matches the dragged area and contains NO sfx UI / no scrim outline).
    4. Drag a second region → a second thumbnail (+2). Click the × on the first thumbnail → it is removed and the remaining one renumbers.
    5. Type a note, Send → success toast with filename. On disk: the `.md` frontmatter + body reference the remaining `+N.png`, and the PNG file(s) exist next to the `.md` and visually match the captured region(s).
    6. Repeat 1–5 using an element note (🎯 pick an element first): confirm the element auto-highlight `+1.png` is still produced AND region thumbnails stack after it.
    7. REGRESSION: send one plain free note (no camera) and one plain element note (no camera) — both still write correct `.md` files as before.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (wrong crop, scrim in image, missing PNG, broken Send, regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page pointer events → scrim drag | marquee rect derived from browser-dispatched clientX/Y (untrusted but only used as crop coords) |
| captured tab pixels → +N.png | the screenshot must not contain sfx UI |

## STRIDE Threat Register (extends 05-SECURITY.md)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Info disclosure | marquee scrim captured in screenshot | mitigate | marquee cleanup() removes scrim DOM BEFORE setSfxVisibility(false)→waitTwoRafs→captureTab (Pitfall 9 / T-05-13 precedent) |
| T-06-09 | DoS | scrim pointer-events blocks Exit | mitigate | scrim at z-index:1 below chip; Esc always cancels; camera button re-enabled on cancel/error |
| T-06-05 | Tampering (XSS) | thumbnail label/alt | mitigate | createElement/textContent only; img.src is a data: URI (no XSS); no innerHTML |
| T-06-08 | Supply chain | npm installs | accept | zero new packages (interactjs pre-audited) |
</threat_model>

<verification>
- `npm run build` succeeds; `tsc --noEmit` (extension) 0 errors
- Chrome UAT (Task 3) approved
- No new innerHTML; no rem in new CSS; no new npm packages
</verification>

<success_criteria>
Phase-6 success criteria 1 (scrim + crosshair + Esc/sub-6px cancel) and 2 (drag → thumbnail, second drag → +2) and 3 (sent .md records +N.png, host writes PNGs) met. Existing Send paths regression-guarded.
</success_criteria>

<output>
Create `.planning/phases/06-region-capture-visual-design/06-02-SUMMARY.md` when done
</output>
