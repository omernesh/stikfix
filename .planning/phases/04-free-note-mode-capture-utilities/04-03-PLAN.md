---
phase: 04-free-note-mode-capture-utilities
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - entrypoints/background.ts
autonomous: false
requirements: [FREE-01]
must_haves:
  truths:
    - "A content script can request a viewport capture via SFX_CAPTURE_TAB and the SW returns a PNG dataURL"
    - "The SW derives windowId from chrome.tabs.get(tabId), never from the message body"
    - "waitTwoRafs + cropToRect round-trip a captured dataURL into a cropped PNG, own-UI absent"
    - "The capture trio is proven standalone and is NOT wired into the free-note Send (screenshots stays [])"
  artifacts:
    - path: "entrypoints/background.ts"
      provides: "handleCaptureTab + SFX_CAPTURE_TAB router case (SW is sole captureVisibleTab caller)"
      contains: "captureVisibleTab"
  key_links:
    - from: "entrypoints/background.ts onMessage router"
      to: "handleCaptureTab"
      via: "case SFX_CAPTURE_TAB with return true"
      pattern: "SFX_CAPTURE_TAB"
    - from: "lib/capture.ts captureTab"
      to: "SW handleCaptureTab"
      via: "chrome.runtime.sendMessage round-trip"
      pattern: "captureVisibleTab"
---

<objective>
Build the SW half of the capture utility trio and prove the full round-trip — the reusable
foundation Phases 5 (element `+1`) and 6 (region `+N`) consume.

Add `handleCaptureTab(tabId)` + a `case SFX_CAPTURE_TAB:` router branch to background.ts. The
service worker is the ONLY caller of `chrome.tabs.captureVisibleTab` (INVARIANT B / D-05 —
content scripts lack the privilege). The handler derives `windowId` from
`chrome.tabs.get(tabId)` (never the message body — anti-spoof) and returns
`{ ok, dataUrl }`. Then prove the complete trio live: `captureTab()` (04-01) → SW →
`captureVisibleTab` → `waitTwoRafs()` flush → `cropToRect()` produces a clean cropped PNG.

CRITICAL (D-06): this is a STANDALONE integration proof. The capture trio is NOT wired into
the free-note Send. Free-note payloads keep `screenshots: []`. Do not touch card.ts.

Purpose: De-risk the hardest pixel-fidelity path (SW privilege boundary, DPR, own-UI flush)
early, before Phase 5/6 depend on it.
Output: background.ts (modified) + a recorded manual integration proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-free-note-mode-capture-utilities/04-CONTEXT.md
@.planning/phases/04-free-note-mode-capture-utilities/04-RESEARCH.md
@.planning/phases/04-free-note-mode-capture-utilities/04-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly. -->

From entrypoints/background.ts (top imports line 19-25 — add SFX_CAPTURE_TAB + MsgCaptureTab):
```typescript
import { SFX_MSG, SFX_SET_ROUTE, SFX_GET_TAB_ID } from '../lib/types.js';
import type { SfxMessage, SfxResponse, HostEntry, AnnotationPayload } from '../lib/types.js';
```

From entrypoints/background.ts onMessage router (lines 392-455) — the return-true async pattern + the listener signature union to extend:
```typescript
(msg: SfxMessage | MsgSetRoute | MsgGetTabId, sender, sendResponse): true | void => {
  switch (msg.type) {
    case SFX_MSG.SEND_ANNOTATION:
      handleSendAnnotation(msg.tabId, msg.payload).then(sendResponse).catch(...);
      return true;   // MANDATORY for every async branch
    ...
    default: return;
  }
}
```
Note: MsgSetRoute/MsgGetTabId are defined LOCALLY in background.ts (lines 366/378). Follow that convention — import the MsgCaptureTab interface from lib/types.js (it lives there per 04-01), and add it to the router signature union.

From lib/capture.ts (04-01) — the content-side relay + helpers already built:
```typescript
export function captureTab(tabId: number): Promise<string>;   // sends SFX_CAPTURE_TAB
export function waitTwoRafs(): Promise<void>;
export function cropToRect(dataUrl, rect, dpr): Promise<string>;
```

From 04-PATTERNS.md "background.ts — ADD handleCaptureTab" — the exact handler body:
```typescript
async function handleCaptureTab(tabId: number):
  Promise<{ ok: true; dataUrl: string } | { ok: false; error: string }> {
  try {
    const tab = await chrome.tabs.get(tabId);
    if (!tab.windowId) return { ok: false, error: 'No windowId for tab' };
    const dataUrl = await chrome.tabs.captureVisibleTab(tab.windowId, { format: 'png' });
    return { ok: true, dataUrl };
  } catch (e: unknown) { return { ok: false, error: String(e) }; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: handleCaptureTab + SFX_CAPTURE_TAB router case in background.ts</name>
  <files>entrypoints/background.ts</files>
  <read_first>
    - entrypoints/background.ts (imports lines 19-25; handleSendAnnotation handler structure; the onMessage router lines 392-455; local Msg interfaces lines 366-380)
    - 04-PATTERNS.md section "entrypoints/background.ts — ADD handleCaptureTab + router case" (handler, router case, import + signature updates)
    - 04-RESEARCH.md Pattern 2 (captureVisibleTab relay), Pitfall 1 (return true), Pitfall 8 (windowId from chrome.tabs.get)
    - lib/types.ts (SFX_CAPTURE_TAB + MsgCaptureTab from 04-01)
  </read_first>
  <action>
    In background.ts: add `SFX_CAPTURE_TAB` to the value import from '../lib/types.js' and `MsgCaptureTab` to the type import.
    Add `async function handleCaptureTab(tabId: number)` returning `{ ok: true; dataUrl: string } | { ok: false; error: string }` exactly per the interface excerpt above — derive `windowId` from `await chrome.tabs.get(tabId)` (NEVER trust windowId from the message body — Pitfall 8 / anti-spoof), guard missing windowId, call `chrome.tabs.captureVisibleTab(tab.windowId, { format: 'png' })`, try/catch → `{ ok:false, error:String(e) }`.
    Add the router signature union member `MsgCaptureTab` alongside `MsgSetRoute | MsgGetTabId` (line 394).
    Add a `case SFX_CAPTURE_TAB:` branch before `default:` mirroring the SEND_ANNOTATION case: `handleCaptureTab((msg as MsgCaptureTab).tabId).then(sendResponse).catch(err => sendResponse({ ok:false, error:String(err) })); return true;` — the `return true` is MANDATORY (captureVisibleTab is async — Pitfall 1).
    Do NOT modify any other handler. Do NOT wire capture into handleSendAnnotation (D-06).
  </action>
  <verify>
    <automated>tsc --noEmit && tsc --noEmit -p tsconfig.host.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - background.ts contains `async function handleCaptureTab` calling `chrome.tabs.captureVisibleTab(tab.windowId, ...)`.
    - windowId is sourced from `chrome.tabs.get(tabId)` — grep confirms no `captureVisibleTab(msg.` / `captureVisibleTab((msg` (windowId never from the message body).
    - A `case SFX_CAPTURE_TAB:` branch exists and ends with `return true;` before `default:`.
    - `handleSendAnnotation` is unchanged (no capture/screenshot logic added — grep `captureVisibleTab` appears exactly once, inside handleCaptureTab).
    - `tsc --noEmit` ×2 + `npm run build` exit 0.
  </acceptance_criteria>
  <done>The SW exposes a SFX_CAPTURE_TAB relay that returns a viewport PNG dataURL; it is the sole captureVisibleTab caller; free-note Send is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Integration proof — capture trio round-trip (standalone)</name>
  <files>entrypoints/review.content/index.ts (optional temporary debug hook — removed after proof)</files>
  <action>
    Manual integration verification of the full capture trio round-trip. The full trio is now in
    place: `captureTab()` (content → SFX_CAPTURE_TAB → SW), `waitTwoRafs()` (own-UI paint flush),
    and `cropToRect()` (DPR-correct canvas crop) in lib/capture.ts, plus the `handleCaptureTab` SW
    handler in background.ts. This checkpoint proves the round-trip works end-to-end — WITHOUT
    wiring it into the free-note Send (D-06). Any temporary debug hook added to drive the proof
    MUST be removed before the plan is marked complete.
  </action>
  <how-to-verify>
    1. Build + load: `npm run build`, then load/reload the unpacked extension in chrome://extensions.
    2. Start a host (`npm run host -- --root <a test dir>`), open any HTTPS page, enter Review Mode so the shadow-root UI mounts.
    3. From the page's devtools console (or a temporary debug hook in index.ts you remove after), invoke the trio against the active tab:
       - hide the shadow-root UI (e.g. set the host element `visibility:hidden`),
       - `await waitTwoRafs()`,
       - `const url = await captureTab(tabId)` — confirm it resolves to a `data:image/png;base64,...` string,
       - `const cropped = await cropToRect(url, {x:0,y:0,width:200,height:120}, window.devicePixelRatio)` — confirm a cropped data URL,
       - restore the UI (`visibility:''`).
    4. Open the captured dataURL in a new tab: confirm it is real page pixels at device resolution AND that NO stikfix UI (chip/FAB) appears in the shot (double-rAF flush worked).
    5. Confirm the free-note flow is UNCHANGED: send a free note and verify the written .md still has empty screenshots (capture was NOT wired in — D-06).
  </how-to-verify>
  <verify>
    <human-check>captureTab round-trips via the SW to a PNG dataURL; the captured image contains no own-UI (chip/FAB absent — double-rAF flush verified); cropToRect returns a correctly sized DPR-correct crop; a free note sent in the same session still writes `screenshots: []`.</human-check>
  </verify>
  <acceptance_criteria>
    - captureTab resolves to a PNG dataURL via the SW (not called directly from the content script).
    - The captured image shows no own-UI (chip/FAB absent) — double-rAF flush verified.
    - cropToRect returns a correctly sized crop at the page's actual DPR.
    - A free note sent during this session still writes `screenshots: []` (capture not wired into Send).
    - Any temporary debug hook is removed; `npm run check` exits 0.
  </acceptance_criteria>
  <done>The capture trio round-trip is proven standalone (SW-only captureVisibleTab, own-UI flushed, DPR crop correct) and is NOT wired into the free-note Send.</done>
  <resume-signal>Type "approved" once the round-trip and the no-own-UI / DPR crop are confirmed, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content script → SW (SFX_CAPTURE_TAB) | A privileged capture is requested across the message channel |
| SW → chrome.tabs API | captureVisibleTab reads rendered pixels of the target window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | SFX_CAPTURE_TAB message | mitigate | SW derives `windowId` from `chrome.tabs.get(tabId)`, never from the message body — a forged windowId cannot redirect the capture; asserted by grep gate. |
| T-04-08 | Information Disclosure | captureVisibleTab capturing off-screen/other-tab content | mitigate | captureVisibleTab captures ONLY the active tab's visible viewport for the resolved windowId; no scrolling/off-screen capture; trio is standalone and not auto-fired on free notes (no implicit captures). |
| T-04-09 | Elevation of Privilege | content script attempting captureVisibleTab directly | mitigate | API is SW-only; content script relays via SFX_CAPTURE_TAB (INVARIANT B) — captureVisibleTab appears exactly once in the codebase, inside the SW handler. |
| T-04-SC | Tampering | (no new package installs in this plan) | accept | No installs; interactjs already vetted in 04-01. |
</threat_model>

<verification>
- `tsc --noEmit` ×2 + `npm run build` exit 0.
- `node scripts/clean-room-check.mjs` exits 0; `npm run check` exits 0.
- HUMAN-UAT integration: captureTab round-trips through the SW; own-UI absent from the shot; cropToRect DPR-correct; free-note screenshots still [].
</verification>

<success_criteria>
- SW is the sole captureVisibleTab caller; SFX_CAPTURE_TAB relay returns a PNG dataURL.
- Success Criterion 4 (runtime half): double-rAF flush eliminates own-UI; DPR-correct crop proven live (the math half was unit-tested in 04-01).
- Capture trio proven standalone; NOT wired into free-note Send (D-06 honored).
</success_criteria>

<output>
Create `.planning/phases/04-free-note-mode-capture-utilities/04-03-SUMMARY.md` when done.
</output>
