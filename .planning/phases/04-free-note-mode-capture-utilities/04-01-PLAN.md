---
phase: 04-free-note-mode-capture-utilities
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tsconfig.lib.json
  - lib/types.ts
  - lib/capture.ts
  - lib/test/capture.test.ts
  - lib/test/card-state.test.ts
  - entrypoints/review.content/card-state.ts
autonomous: true
requirements: [FREE-02]
must_haves:
  truths:
    - "computeCropCoords(rect, dpr) returns integer pixel coords correct at DPR=1, 1.25, 2"
    - "Single-active-card guard rejects a second open while one card is active"
    - "interactjs@1.10.27 is installed and resolves for the extension build"
    - "npm run test:lib runs the new capture + card-state tests and exits 0"
  artifacts:
    - path: "lib/capture.ts"
      provides: "Pure computeCropCoords + browser-only captureTab/waitTwoRafs/cropToRect"
      contains: "export function computeCropCoords"
    - path: "lib/test/capture.test.ts"
      provides: "DPR=1/1.25/2 unit coverage (Success Criterion 4)"
      contains: "computeCropCoords"
    - path: "entrypoints/review.content/card-state.ts"
      provides: "DOM-free single-active-card state machine (FREE-02 testable core)"
      exports: ["tryOpenCard", "closeCardState", "isCardActive"]
    - path: "lib/types.ts"
      provides: "SFX_CAPTURE_TAB constant + MsgCaptureTab interface"
      contains: "SFX_CAPTURE_TAB"
  key_links:
    - from: "package.json test:lib"
      to: "dist/lib/lib/test/capture.test.js"
      via: "node --test argument list"
      pattern: "capture\\.test\\.js"
---

<objective>
Wave-0 Nyquist gate for Phase 4. Build the pure, unit-testable foundation that the
free-note slice (04-02) and the capture relay (04-03) sit on top of:

1. Install `interactjs@1.10.27` (locked stack; required before any drag code compiles).
2. Add the `SFX_CAPTURE_TAB` message constant + `MsgCaptureTab` interface to `lib/types.ts`
   (side-effect-free module — INVARIANT A: never define this in background.ts).
3. Create `lib/capture.ts` exporting the **pure** `computeCropCoords(rect, dpr)` (no DOM)
   alongside the browser-only `captureTab`, `waitTwoRafs`, `cropToRect` (declared here,
   exercised live in 04-03).
4. Extract the single-active-card state machine into a DOM-free `card-state.ts` so FREE-02's
   core guard is unit-testable.
5. Wire both new test files into `npm run test:lib`.

Purpose: De-risk the hardest pixel-fidelity path (DPR crop math) and the FREE-02 guard with
pure node:test coverage before any Chrome-runtime UI is built. This is the Wave-0 test
scaffold the rest of the phase depends on.

Output: interactjs in package.json; `lib/capture.ts`, `lib/test/capture.test.ts`,
`entrypoints/review.content/card-state.ts`, `lib/test/card-state.test.ts`; updated
`lib/types.ts`, `tsconfig.lib.json`, `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-free-note-mode-capture-utilities/04-CONTEXT.md
@.planning/phases/04-free-note-mode-capture-utilities/04-RESEARCH.md
@.planning/phases/04-free-note-mode-capture-utilities/04-PATTERNS.md
@.planning/phases/04-free-note-mode-capture-utilities/04-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly; no exploration. -->

From host/src/types.ts (Screenshot.rect shape — the rect computeCropCoords/cropToRect accept):
```typescript
rect?: { x: number; y: number; width: number; height: number };
```

From lib/types.ts (existing side-effect-free constants to mirror — lines 76-77):
```typescript
export const SFX_SET_ROUTE = 'SFX_SET_ROUTE' as const;
export const SFX_GET_TAB_ID = 'SFX_GET_TAB_ID' as const;
```
The import-hazard comment block (lines 66-78) explains WHY SFX_CAPTURE_TAB must live here, not in background.ts. Add the new constant immediately after line 77.

From package.json (the exact test:lib script to extend — note the double lib/lib/ compiled path is the existing convention):
```
"test:lib": "tsc -p tsconfig.lib.json && node --test dist/lib/lib/test/routing.test.js dist/lib/lib/test/discovery.test.js"
```

From lib/test/routing.test.ts (the node:test file shape to mirror — header + imports):
```typescript
import { test, describe } from 'node:test';
import assert from 'node:assert/strict';
import { resolveRoute, reconcileRegistry } from '../routing.js';   // .js ESM extension
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install interactjs + add SFX_CAPTURE_TAB to lib/types.ts</name>
  <files>package.json, lib/types.ts</files>
  <read_first>
    - package.json (the dependencies block + test:lib script)
    - lib/types.ts (the SFX_SET_ROUTE/SFX_GET_TAB_ID block + the lines 66-78 import-hazard comment)
    - 04-PATTERNS.md section "lib/types.ts — ADD SFX_CAPTURE_TAB"
  </read_first>
  <action>
    Run `npm install interactjs@1.10.27` so it lands in package.json `dependencies` (NOT devDependencies — it bundles into the extension). Confirm the resolved version is exactly 1.10.27.
    In lib/types.ts, immediately after the existing `export const SFX_GET_TAB_ID = 'SFX_GET_TAB_ID' as const;` (line 77), add `export const SFX_CAPTURE_TAB = 'SFX_CAPTURE_TAB' as const;` and an exported `interface MsgCaptureTab` with fields `type: typeof SFX_CAPTURE_TAB` and `tabId: number`. Do NOT add MsgCaptureTab to the `SfxMessage` union — it is a separate top-level const handled by its own router case (same pattern as SFX_SET_ROUTE). Keep the side-effect-free invariant: no imports from background.ts, no top-level runtime code.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies.interactjs!=='1.10.27') process.exit(1)" && node --test --test-name-pattern=NOTHING 2>$null; tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies.interactjs` === `"1.10.27"` (asserted by the node -e check above; exit 0).
    - `lib/types.ts` contains `export const SFX_CAPTURE_TAB` and `export interface MsgCaptureTab` with `tabId: number`.
    - `MsgCaptureTab` is NOT a member of the `SfxMessage` union type.
    - `tsc --noEmit` exits 0 (no type errors introduced).
  </acceptance_criteria>
  <done>interactjs@1.10.27 installed; SFX_CAPTURE_TAB + MsgCaptureTab exported from lib/types.ts as side-effect-free constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/capture.ts + DPR unit tests (Success Criterion 4)</name>
  <files>lib/capture.ts, lib/test/capture.test.ts</files>
  <behavior>
    - computeCropCoords({x:10,y:20,width:100,height:50}, 1) === {sx:10,sy:20,sw:100,sh:50}
    - computeCropCoords(sameRect, 1.25) === {sx:13,sy:25,sw:125,sh:63} (Math.round of 12.5/25/125/62.5)
    - computeCropCoords(sameRect, 2) === {sx:20,sy:40,sw:200,sh:100}
  </behavior>
  <read_first>
    - 04-PATTERNS.md section "lib/capture.ts — NEW pure utility module" (full source excerpts for all four exports)
    - 04-PATTERNS.md section "lib/test/capture.test.ts" (test file shape)
    - lib/test/routing.test.ts (the node:test header/import convention to mirror)
    - lib/discovery.ts (a sibling pure lib module — import style reference)
  </read_first>
  <action>
    Create `lib/capture.ts` exporting four functions, factored so the math is DOM-free:
    (a) `computeCropCoords(rect: {x,y,width,height}, dpr: number): {sx,sy,sw,sh}` — pure; each coord is `Math.round(value * dpr)`. Math.round after the multiply is MANDATORY (Windows 125% DPR=1.25 produces fractional pixels — PRD §7.3 / RESEARCH Pitfall 4).
    (b) `waitTwoRafs(): Promise<void>` — two nested `requestAnimationFrame` calls (browser-only; exercised in 04-03).
    (c) `cropToRect(dataUrl, rect, dpr): Promise<string>` — canvas drawImage using `computeCropCoords` internally (browser-only).
    (d) `captureTab(tabId: number): Promise<string>` — `chrome.runtime.sendMessage({type: SFX_CAPTURE_TAB, tabId}, ...)` Promise-wrapped; guard `chrome.runtime.lastError || !resp`; resolve `resp.dataUrl` or reject `resp.error`. Mirror the getTabId() callback shape from chip.ts lines 505-520. Import `SFX_CAPTURE_TAB` from `./types.js`.
    The module must have NO top-level `chrome.*` / `document.*` / `window.*` access — those live inside function bodies only, so `computeCropCoords` imports cleanly under node:test.
    Create `lib/test/capture.test.ts`: mirror routing.test.ts header; import `{ computeCropCoords }` from `../capture.js`; one `describe('computeCropCoords')` with three `test` cases asserting the behavior above via `assert.deepStrictEqual`.
  </action>
  <verify>
    <automated>tsc -p tsconfig.lib.json && node --test dist/lib/lib/test/capture.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `lib/capture.ts` exports `computeCropCoords`, `waitTwoRafs`, `cropToRect`, `captureTab`.
    - `grep -v '^ *\*' lib/capture.ts` shows no top-level `chrome.`/`document.`/`window.` reference outside a function body (all browser API use is inside fn bodies).
    - `node --test dist/lib/lib/test/capture.test.js` runs 3 tests, 0 failures (DPR=1/1.25/2), exit 0.
    - The DPR=1.25 case asserts sx=13, sh=63 specifically (proves Math.round, not truncation).
  </acceptance_criteria>
  <done>computeCropCoords is pure and DPR-correct at 1/1.25/2 under node:test; the three browser-only capture helpers compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: card-state.ts single-active-card guard + tests, wire test:lib</name>
  <files>entrypoints/review.content/card-state.ts, lib/test/card-state.test.ts, package.json, tsconfig.lib.json</files>
  <behavior>
    - Fresh state: isCardActive() === false
    - tryOpenCard() when inactive → returns 'opened' and isCardActive() becomes true
    - tryOpenCard() when already active → returns 'focus-existing' (does NOT open a second)
    - closeCardState() → isCardActive() returns false again; next tryOpenCard() returns 'opened'
  </behavior>
  <read_first>
    - 04-PATTERNS.md section "entrypoints/review.content/card.ts" (the module-level activeCard guard pattern — extract the STATE half only)
    - 04-UI-SPEC.md §2 "Single-card enforcement (D-02)"
    - lib/test/routing.test.ts (test shape)
    - tsconfig.lib.json (confirm which globs it compiles so card-state.ts + its test are included)
  </read_first>
  <action>
    Create `entrypoints/review.content/card-state.ts` — a DOM-free state machine isolating the single-active-card decision from card.ts DOM building (so it is node:test-safe). Export: `tryOpenCard(): 'opened' | 'focus-existing'` (sets active flag and returns 'opened' when inactive; returns 'focus-existing' when already active without changing state), `closeCardState(): void` (clears the flag), `isCardActive(): boolean`. Use a module-level `let active = false`. No imports of DOM/chrome.
    Create `lib/test/card-state.test.ts` importing from `../../entrypoints/review.content/card-state.js`. Cover the four behaviors above; call `closeCardState()` in a beforeEach (or at the start of each test) to reset module state between tests.
    Update `tsconfig.lib.json` so it compiles both `entrypoints/review.content/card-state.ts` and the two new test files (verify the include globs already cover them; widen if not — keep rootDir consistent with the existing dist/lib/lib/ output convention).
    Update `package.json` `test:lib`: append `dist/lib/lib/test/capture.test.js` and the compiled card-state test path to the `node --test` argument list. Confirm the compiled path for card-state.test.js matches wherever tsconfig.lib.json emits it (mirror the existing dist/lib/lib/test/ prefix).
  </action>
  <verify>
    <automated>npm run test:lib</automated>
  </verify>
  <acceptance_criteria>
    - `entrypoints/review.content/card-state.ts` exports `tryOpenCard`, `closeCardState`, `isCardActive` and has zero `document`/`window`/`chrome` references.
    - `npm run test:lib` compiles via tsconfig.lib.json and runs routing + discovery + capture + card-state tests with 0 failures, exit 0.
    - `package.json` `test:lib` argument list includes both `capture.test.js` and the card-state test compiled path.
    - Re-running `tryOpenCard()` twice without `closeCardState()` yields `'opened'` then `'focus-existing'` (asserted in the test).
  </acceptance_criteria>
  <done>FREE-02 core guard is unit-tested and DOM-free; both new test files run under npm run test:lib and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → install | `interactjs@1.10.27` install crosses into the build supply chain |
| (no runtime boundary in this plan) | Wave-0 is pure logic + install; no Chrome/HTTP surface exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | `npm install interactjs@1.10.27` | accept | Package is vetted in 04-RESEARCH.md Package Legitimacy Audit and locked in CLAUDE.md stack table (Approved disposition, not [ASSUMED]/[SUS]); ~9-year-old established MIT lib. No blocking checkpoint required per gate rules. |
| T-04-01 | Tampering | `computeCropCoords` integer math | mitigate | `Math.round` on all four coords prevents fractional-pixel crop drift; asserted in capture.test.ts at DPR=1.25. |
| T-04-02 | Information Disclosure | `lib/capture.ts` module surface | mitigate | No top-level chrome/DOM access; `computeCropCoords` is pure — no data egress path exists in Wave-0. |
</threat_model>

<verification>
- `tsc --noEmit` and `tsc --noEmit -p tsconfig.host.json` both exit 0.
- `npm run test:lib` green (routing + discovery + capture + card-state).
- `node scripts/clean-room-check.mjs` exits 0 (sfx-* namespace preserved; no GPL identifiers).
- `npm run check` exits 0 end-to-end.
</verification>

<success_criteria>
- interactjs@1.10.27 in package.json dependencies.
- computeCropCoords proven DPR-correct at 1 / 1.25 / 2 (Success Criterion 4 unit half).
- Single-active-card guard (FREE-02 core) proven under node:test.
- SFX_CAPTURE_TAB + MsgCaptureTab exported from lib/types.ts (side-effect-free).
- npm run check exits 0.
</success_criteria>

<output>
Create `.planning/phases/04-free-note-mode-capture-utilities/04-01-SUMMARY.md` when done.
</output>
