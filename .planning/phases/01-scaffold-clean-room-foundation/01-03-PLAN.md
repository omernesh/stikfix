---
phase: 01-scaffold-clean-room-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/clean-room-check.mjs
  - .gitignore
autonomous: true
requirements: [BUILD-01, BUILD-04, BUILD-05]
must_haves:
  truths:
    - "`node scripts/clean-room-check.mjs` exits 0 on the clean tree and prints a PASS line"
    - "The clean-room gate exits non-zero when a banned identifier (__opc_/opencode/JodusNodus) is present in a scanned source file"
    - "The gate skips node_modules, .git, .output, dist, .wxt, and .planning so third-party/research text never trips it"
    - "`npm run check` runs both tsc passes + clean-room gate + host smoke test and exits 0"
    - "`npm run build` produces .output/chrome-mv3/ and dist/host/index.js on Windows"
    - ".gitignore ignores .output/, dist/, .wxt/, node_modules/, and .stickyfix-token"
  artifacts:
    - path: "scripts/clean-room-check.mjs"
      provides: "Cross-platform Node grep gate: walks tree, fails on banned identifiers"
      contains: "JodusNodus"
    - path: ".gitignore"
      provides: "Ignores build outputs and WXT-generated dirs"
      contains: ".output/"
  key_links:
    - from: "scripts/clean-room-check.mjs"
      to: "repo source tree"
      via: "recursive fs.readdirSync walk with SKIP_DIRS"
      pattern: "SKIP_DIRS|node_modules"
    - from: "package.json check script"
      to: "scripts/clean-room-check.mjs + scripts/host-smoke-test.mjs"
      via: "&&-chained npm run check"
      pattern: "clean-room-check"
---

<objective>
Close the Walking Skeleton: implement the cross-platform clean-room grep gate, finalize .gitignore for WXT/host build outputs, and verify the whole pipeline end-to-end — `npm run check` (both tsc passes + clean-room audit + host smoke test) and `npm run build` both green on Windows. After this plan, the scaffold rung is provably complete: a fresh clone can install, build a loadable extension + runnable host, and pass the structural gate that keeps GPL artifacts out from commit one.

Purpose: The clean-room gate is the structural mechanism that makes BUILD-04 verifiable on every run (not a one-time check), and the full `check`/`build` chains are the BUILD-01/BUILD-05 gates. (BUILD-01, BUILD-04, BUILD-05)
Output: scripts/clean-room-check.mjs, updated .gitignore, verified `npm run check` + `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-clean-room-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-clean-room-foundation/01-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/phases/01-scaffold-clean-room-foundation/01-01-SUMMARY.md
@.planning/phases/01-scaffold-clean-room-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Exact file contents live in 01-RESEARCH.md. Key contracts: -->
<!-- scripts/clean-room-check.mjs (RESEARCH Pattern 5): BANNED=[/__opc_/i,/opencode/i,/JodusNodus/i]; SCAN_EXTS={.ts,.js,.mjs,.cjs,.json,.html,.css,.md}; SKIP_DIRS={node_modules,.git,.output,dist,.wxt, AND .planning (per Pitfall 5 — research files legitimately mention these)}; recursive readdirSync walk; collect hits; if hits>0 print each + process.exit(1) else print PASS. -->
<!-- .gitignore (RESEARCH Pattern 8): add .output/, dist/, .wxt/ to the EXISTING .gitignore (do not delete current stickyfix-specific rules: notes/, private/, .claude/, .stickyfix-token). -->
<!-- package.json scripts are already written by plan 01-01; this plan only verifies they run end to end. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the cross-platform clean-room grep gate</name>
  <read_first>
    - .planning/phases/01-scaffold-clean-room-foundation/01-RESEARCH.md (Pattern 5 — clean-room-check.mjs full source; Pitfall 5 — must skip .output/dist/.planning; Pitfall 7 — ESM no __dirname)
    - .planning/research/PITFALLS.md (Pitfall 13 — GPL clean-room IP hygiene; banned identifiers)
    - .planning/phases/01-scaffold-clean-room-foundation/01-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Create scripts/clean-room-check.mjs per RESEARCH Pattern 5: BANNED = [/__opc_/i, /opencode/i, /JodusNodus/i]; SCAN_EXTS = {.ts,.js,.mjs,.cjs,.json,.html,.css,.md}; SKIP_DIRS = {node_modules, .git, .output, dist, .wxt, .planning}. Note: .planning MUST be in SKIP_DIRS (per Pitfall 5) because the research/context files legitimately reference the banned upstream identifiers — scanning them would self-trip the gate. Recursively walk process.cwd() with readdirSync({withFileTypes:true}); for matching files, readFileSync utf8 and test each BANNED pattern, collecting {file, match}. If any hits: print "CLEAN-ROOM VIOLATION — banned identifiers found:" plus each hit, then process.exit(1). Else print "clean-room audit: PASS — no banned identifiers found". Do not use __dirname (ESM).

    Prove the gate fails-closed before declaring it done — this gate IS the BUILD-04 enforcement mechanism, so a non-failing gate is a silent regression. Carry out an explicit RED→GREEN check: (RED) write a temporary file under a scanned path containing a planted banned token (e.g. `__sfx_clean_room_probe.ts` containing the literal "JodusNodus"), run `node scripts/clean-room-check.mjs`, and confirm it exits NON-ZERO and names the offending file in its output. (GREEN) delete the probe file, re-run the gate, and confirm it now exits 0 and prints the PASS line. Leave the tree clean — no probe file may remain after this task.
  </action>
  <verify>
    <automated>node scripts/clean-room-check.mjs && node -e "const fs=require('fs');const {execFileSync}=require('child_process');fs.writeFileSync('__sfx_clean_room_probe.ts','const x = \"JodusNodus\";');let failed=false;try{execFileSync(process.execPath,['scripts/clean-room-check.mjs'],{stdio:'pipe'})}catch(e){failed=true}fs.unlinkSync('__sfx_clean_room_probe.ts');if(!failed){console.error('gate did NOT fail on probe');process.exit(1)}execFileSync(process.execPath,['scripts/clean-room-check.mjs'],{stdio:'pipe'});console.log('clean-room gate verified RED+GREEN')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/clean-room-check.mjs SKIP_DIRS includes node_modules, .git, .output, dist, .wxt, and .planning
    - RED proven: with a scanned file containing "JodusNodus" present, `node scripts/clean-room-check.mjs` exits NON-ZERO and names the offending file
    - GREEN proven: `node scripts/clean-room-check.mjs` exits 0 on the clean tree and prints the PASS line
    - After removing the probe file, the gate returns to exit 0 (no residue — no probe file left in the tree)
    - The script does not reference __dirname
  </acceptance_criteria>
  <done>scripts/clean-room-check.mjs committed; proven via an explicit RED (planted banned token → exit non-zero) and GREEN (clean tree → exit 0) check to fail-closed and pass correctly, skipping node_modules/.output/dist/.wxt/.planning.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize .gitignore and verify the full check + build pipeline</name>
  <read_first>
    - .planning/phases/01-scaffold-clean-room-foundation/01-RESEARCH.md (Pattern 8 — .gitignore entries; Pattern 7 — full scripts; Pitfall 4 — wxt prepare on fresh clone; Pitfall 6 — build before check)
    - .gitignore (current file — preserve existing stickyfix-specific rules)
    - .planning/phases/01-scaffold-clean-room-foundation/01-VALIDATION.md (sampling: build && check must both exit 0)
  </read_first>
  <action>
    Update the existing .gitignore to add the WXT/host build-output entries from RESEARCH Pattern 8 that are not already present: .output/, dist/ (confirm present), and .wxt/ (WXT-generated, regenerated by postinstall). Preserve all current project-specific rules already in the file (notes/ with !notes/.gitkeep, private/, .claude/, .stickyfix-token, *.log, editor dirs) — do NOT remove any existing functionality. Then run the full pipeline to verify wiring across all three plans: `npm run build` (must produce .output/chrome-mv3/ and dist/host/index.js, exit 0) followed by `npm run check` (tsc --noEmit on extension + tsc --noEmit -p tsconfig.host.json + clean-room gate + host smoke test, all exit 0). This is the first point where the full `npm run check` chain from 01-01 resolves end-to-end (tsconfig.host.json from 01-02 and both scripts now exist). Both commands must be green on Windows with no sips/Bun/rm -rf/Unix-glob steps (BUILD-01). If anything fails, fix the wiring (e.g., a missing .js import extension in host, a tsconfig path) and re-run until both are green.
  </action>
  <verify>
    <automated>npm run build && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains .output/, dist/, and .wxt/ AND still contains the pre-existing notes/, private/, .stickyfix-token rules
    - `npm run build` exits 0 and produces .output/chrome-mv3/manifest.json and dist/host/index.js
    - `npm run check` exits 0 (both tsc passes + clean-room PASS + smoke test PASS)
    - No build step invokes sips, Bun, rm -rf, or a Unix-only glob (BUILD-01 Windows-safe)
  </acceptance_criteria>
  <done>.gitignore finalized without dropping existing rules; `npm run build` and `npm run check` both green end-to-end on Windows — the full BUILD-01/04/05 gate passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 1 scaffold: WXT MV3 extension that builds to .output/chrome-mv3/ with four pre-sized icons, a runnable host stub, the clean-room grep gate, and a green `npm run check`. This checkpoint covers the one thing that cannot be automated this phase — loading the unpacked extension in Chrome (BUILD-02 manual verification per 01-VALIDATION.md).</what-built>
  <how-to-verify>
    1. Run `npm run build` (if not already run this session). Confirm it exits 0.
    2. Open Chrome and navigate to chrome://extensions.
    3. Enable "Developer mode" (top-right toggle).
    4. Click "Load unpacked" and select the folder: D:\docker\stickyfix\.output\chrome-mv3
    5. Confirm the extension appears as "stickyfix" with NO manifest errors (no red error box).
    6. Confirm the stickyfix icon renders in the extensions list (and pin it — confirm a toolbar icon shows).
    7. Clicking the toolbar icon opens the placeholder popup (it may just show "stickyfix — loading..." text — that is expected for Phase 1).
  </how-to-verify>
  <resume-signal>Type "approved" if the extension loads with no errors and icons render, or describe the manifest error / missing icon you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/AI → committed repo | GPL-derived identifiers must never enter the MIT tree |
| build outputs → git | Generated artifacts (.output, dist, .wxt) must not be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Repudiation | GPL identifier leakage (license violation) | mitigate | clean-room-check.mjs greps for __opc_/opencode/JodusNodus on every `npm run check`; fails the gate non-zero on any match (the structural BUILD-04 mechanism) |
| T-01-07 | Information Disclosure | secrets / private docs committed | mitigate | .gitignore excludes .stickyfix-token and private/; no tokens exist in Phase 1 code |
| T-01-08 | Tampering | clean-room gate false-negative (skips a real source dir) | mitigate | SKIP_DIRS limited to build/vendor/research dirs only; all first-party source (entrypoints/, host/, scripts/, wxt.config.ts, tsconfig*) is scanned; RED test proves the gate actually fails on a planted token |

Note: This is a structural license/secrets gate, not a runtime security surface. Host network threats are Phase 2.
</threat_model>

<verification>
- `node scripts/clean-room-check.mjs` exits 0 on clean tree, non-zero with a planted banned token.
- `npm run build` produces .output/chrome-mv3/ + dist/host/index.js.
- `npm run check` runs both tsc passes + clean-room + smoke test, all green.
- .gitignore ignores build outputs without dropping existing project rules.
- Manual: unpacked extension loads in Chrome with no manifest errors, icons render (checkpoint).
</verification>

<success_criteria>
- scripts/clean-room-check.mjs and the finalized .gitignore are committed.
- `npm run check` and `npm run build` both exit 0 on Windows.
- A planted banned identifier makes the gate fail — proving BUILD-04 is enforced on every run.
- The built extension loads cleanly in Chrome (human-verified).
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-clean-room-foundation/01-03-SUMMARY.md` when done.
</output>
