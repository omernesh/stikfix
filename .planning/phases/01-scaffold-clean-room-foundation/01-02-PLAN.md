---
phase: 01-scaffold-clean-room-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.host.json
  - host/src/index.ts
  - scripts/host-smoke-test.mjs
autonomous: true
requirements: [BUILD-05]
must_haves:
  truths:
    - "`tsc -p tsconfig.host.json` compiles host/src/index.ts to runnable ESM at dist/host/index.js"
    - "Running the built stub with `--root <dir>` prints a JSON startup line including app:'stikfix' and the root"
    - "Running the built stub without --root exits non-zero with a clear error"
    - "The host smoke test spawns the stub, asserts the startup JSON, and exits 0"
  artifacts:
    - path: "tsconfig.host.json"
      provides: "NodeNext ESM host tsconfig, types:[node], outDir dist/host"
      contains: '"module": "NodeNext"'
    - path: "host/src/index.ts"
      provides: "Host CLI stub: parseArgs(--root + Phase 2 flags), prints startup JSON, exits"
      contains: "parseArgs"
    - path: "scripts/host-smoke-test.mjs"
      provides: "spawnSync-based smoke test asserting app==stikfix and root match"
      contains: "spawnSync"
  key_links:
    - from: "scripts/host-smoke-test.mjs"
      to: "dist/host/index.js"
      via: "spawnSync(process.execPath, ['dist/host/index.js','--root',tmp])"
      pattern: "dist/host/index.js"
    - from: "host/src/index.ts"
      to: "process startup JSON"
      via: "console.log(JSON.stringify({app:'stikfix',...}))"
      pattern: "app.*stikfix"
---

<objective>
Build the host half of the Walking Skeleton: a tsc-compiled (NodeNext ESM) host stub that parses --root via util.parseArgs and prints a JSON startup line, plus a spawn-and-assert smoke test. After this plan, `tsc -p tsconfig.host.json` emits a runnable dist/host/index.js and the smoke test proves it starts and reports app:'stikfix'.

Purpose: This is the runnable-host rung of the skeleton and the seam Phase 2 extends into the real HTTP server. The smoke test is the BUILD-05 host check inside `npm run check`. (BUILD-05)
Output: tsconfig.host.json, host/src/index.ts, scripts/host-smoke-test.mjs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-clean-room-foundation/01-CONTEXT.md
@.planning/phases/01-scaffold-clean-room-foundation/01-RESEARCH.md

<interfaces>
<!-- Exact file contents live in 01-RESEARCH.md. Key contracts: -->
<!-- tsconfig.host.json (RESEARCH Pattern 2): target ES2022, module NodeNext, moduleResolution NodeNext, outDir dist/host, rootDir host/src, strict, types:[node], esModuleInterop, skipLibCheck, include host/src/**/*.ts. -->
<!-- host/src/index.ts (RESEARCH Pattern 4): import { parseArgs } from 'node:util'; options root/origin(multiple)/name/notes-dir/port/token, strict:false; if !root -> console.error + exit(1); else console.log(JSON.stringify({app:'stikfix', name, root, port:null, token:null, notesDir:null})). Use top-level import { basename } from 'node:path' (NOT inline require). All intra-host imports MUST carry .js extensions (Pitfall 2). -->
<!-- scripts/host-smoke-test.mjs (RESEARCH Pattern 6): mkdtempSync temp root, spawnSync(process.execPath,['dist/host/index.js','--root',tmp],{timeout:5000}), assert status 0, parse stdout JSON, assert app==='stikfix' && root===tmp, rmSync finally, exit 0/1. -->
<!-- IMPORTANT: this plan does NOT define package.json scripts — plan 01-01 owns package.json. The host/check scripts already reference dist/host/index.js and scripts/host-smoke-test.mjs. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tsconfig.host.json and the host CLI stub</name>
  <read_first>
    - .planning/phases/01-scaffold-clean-room-foundation/01-RESEARCH.md (Pattern 2 — tsconfig.host.json; Pattern 4 — host/src/index.ts; Pitfall 2 — .js import extensions under NodeNext; Open Question 2 — verify node runs the emitted ESM)
    - .planning/phases/01-scaffold-clean-room-foundation/01-CONTEXT.md (D-05, D-06, D-10, D-11)
  </read_first>
  <action>
    Create tsconfig.host.json per RESEARCH Pattern 2: target ES2022, module "NodeNext", moduleResolution "NodeNext", outDir "dist/host", rootDir "host/src", strict:true, types:["node"], esModuleInterop:true, skipLibCheck:true, include ["host/src/**/*.ts"]. Create host/src/index.ts per RESEARCH Pattern 4: import { parseArgs } from 'node:util' and import { basename } from 'node:path' at the top (no inline require — per Pattern 4 note). Define parseArgs options root(string), origin(string,multiple:true), name(string), 'notes-dir'(string), port(string), token(string) with strict:false (supports Phase 2 flags + HOST-13 repeatable --origin). If values.root is missing, console.error("stikfix-host: --root is required") and process.exit(1). Otherwise compute projectName = values.name ?? basename(values.root) and console.log(JSON.stringify({ app:'stikfix', name:projectName, root:values.root, port:null, token:null, notesDir:null })). Keep it a stub that exits after printing — Phase 2 replaces it with the long-running server. Then run `tsc -p tsconfig.host.json` and `node dist/host/index.js --root .` to confirm the emitted ESM runs (Open Question 2 / Pitfall 2). This compile step is what produces dist/host/index.js, which Task 2's smoke test depends on existing.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.host.json && node -e "const {execFileSync}=require('child_process');const o=execFileSync(process.execPath,['dist/host/index.js','--root','.'],{encoding:'utf8'});const j=JSON.parse(o.trim());if(j.app!=='stikfix')process.exit(1);console.log('host stub ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.host.json contains "module": "NodeNext", "outDir": "dist/host", "types": ["node"]
    - host/src/index.ts imports parseArgs from 'node:util' and basename from 'node:path' (top-level, no require())
    - `tsc -p tsconfig.host.json` exits 0 and emits dist/host/index.js
    - `node dist/host/index.js --root .` prints JSON with app:"stikfix" and exits 0
    - `node dist/host/index.js` (no --root) prints the required-error and exits non-zero
  </acceptance_criteria>
  <done>tsconfig.host.json and host/src/index.ts committed; the stub compiles to runnable ESM at dist/host/index.js and prints the startup JSON; missing --root exits non-zero.</done>
</task>

<task type="auto">
  <name>Task 2: Write the spawn-and-assert host smoke test</name>
  <read_first>
    - .planning/phases/01-scaffold-clean-room-foundation/01-RESEARCH.md (Pattern 6 — host-smoke-test.mjs; Pitfall 6 — smoke test running before host is built; Pitfall 7 — ESM has no __dirname)
    - .planning/phases/01-scaffold-clean-room-foundation/01-VALIDATION.md (smoke test row; runtime budget)
  </read_first>
  <action>
    Precondition: dist/host/index.js already exists — it was compiled in Task 1 via `tsc -p tsconfig.host.json`. This task does NOT recompile; it only writes the smoke test and runs it against the already-built stub (compile success and smoke-test success are kept as separate exit codes). Create scripts/host-smoke-test.mjs per RESEARCH Pattern 6 (ESM, .mjs): import spawnSync from 'node:child_process', mkdtempSync/rmSync from 'node:fs', tmpdir from 'node:os', join from 'node:path'. Make a temp root via mkdtempSync(join(tmpdir(),'sfx-smoke-')). spawnSync(process.execPath, ['dist/host/index.js','--root',tmpRoot], { encoding:'utf8', timeout:5000 }). If result.error or result.status !== 0, print stderr and exit(1). Parse result.stdout JSON; assert parsed.app === 'stikfix' (exit 1 with message otherwise) and parsed.root === tmpRoot. On success print "smoke test: PASS". Clean up the temp dir in a finally with rmSync(recursive,force). Add a guard at the top: if dist/host/index.js does not exist (existsSync), print a helpful message instructing `npm run build` first and exit 1 (Pitfall 6). Do not use __dirname (Pitfall 7) — operate from process.cwd()/relative dist path as Pattern 6 does.
  </action>
  <verify>
    <automated>node scripts/host-smoke-test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/host-smoke-test.mjs uses spawnSync(process.execPath, ['dist/host/index.js', ...]) with a 5000ms timeout
    - Running `node scripts/host-smoke-test.mjs` (against the dist/host/index.js compiled in Task 1) prints "smoke test: PASS" and exits 0
    - The script asserts parsed.app === 'stikfix' and parsed.root === the temp root
    - The script exits 1 with a helpful "run npm run build first" message when dist/host/index.js is absent
    - The temp dir is removed after the run (no sfx-smoke-* left in the OS temp dir)
  </acceptance_criteria>
  <done>scripts/host-smoke-test.mjs committed; it spawns the built stub, asserts the startup JSON, cleans up, and exits 0 on success. The verify runs only the smoke test (no recompile) so a smoke failure is not masked by a compile pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → host stub | `--root` and other flags parsed from argv (untrusted-ish, but only printed this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | host stub stdout | accept | Stub only echoes --root and a derived name; no secrets exist this phase (token/port are null placeholders). Real /status response-shape hardening is Phase 2 (Pitfall 11) |
| T-01-04 | Elevation of Privilege | host network listener | accept | No listener exists this phase — the stub parses args and exits. 127.0.0.1-only binding + path-traversal containment land in Phase 2 (Pitfalls 2, 10) |
| T-01-05 | Denial of Service | smoke test spawn hang | mitigate | spawnSync uses timeout:5000 so a hung stub fails fast rather than blocking `npm run check` |

Note: The host's real security surface (token auth, 127.0.0.1 bind, path safety, body cap, CORS) is Phase 2 — out of scope for this stub.
</threat_model>

<verification>
- `tsc -p tsconfig.host.json` emits a runnable dist/host/index.js (NodeNext ESM).
- `node dist/host/index.js --root <dir>` prints app:'stikfix' JSON; missing --root exits non-zero.
- `node scripts/host-smoke-test.mjs` prints "smoke test: PASS" and exits 0 after Task 1's compile.
</verification>

<success_criteria>
- tsconfig.host.json, host/src/index.ts, scripts/host-smoke-test.mjs exist and are committed.
- The host stub compiles to runnable ESM and the smoke test passes — satisfying the host half of BUILD-05.
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-clean-room-foundation/01-02-SUMMARY.md` when done.
</output>
